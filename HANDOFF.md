# HANDOFF — ApartamenTUal

> Status curent al proiectului. Citește la începutul fiecărei sesiuni noi (chat sau Claude Code) ca să intri rapid în context.
> Ultima actualizare: 1 august 2026, după-amiaza (**✅ EPISODUL 3 AL SERIALULUI, „TERENUL", PUBLICAT ȘI TRIMIS PRIN NEWSLETTER. ZERO cod atins, ZERO commituri, ZERO deploy — totul din panoul de admin și din Resend. Un singur fișier nou, NECOMIS: `email_templates/newsletter-episod-3-terenul.html`.** **ARTICOLUL:** titlu „Terenul: cum am căutat golurile din oraș", slug **`judetului-housing-episodul-3-terenul`**, categorie `prototip`, publicat 1 august, ora 11:05, cu poză principală (`articles/main-1785582296968.jpg`, vedere aeriană a zonei). Rezumat: „Sunt terenuri în București care rămân goluri în oraș: prea mici pentru un dezvoltator, prea scumpe pentru cine ar vrea o casă. Exact pe astea le căutam. Episodul 3: cum căutam, ce refuzam și cum a apărut terenul de pe Județului." **⚠️ REGULA SLUGULUI, valabilă la fiecare episod nou:** slug-ul trebuie să **înceapă** cu `judetului-housing-episodul-N`, altfel episodul apare izolat. Trei locuri din cod depind de asta: `news.html:623` (`indexOf(...) === 0` aprinde blocul de newsletter de la final), `news.html:598` și `:630` (numărul din slug generează navigarea „← Episodul N-1 / Episodul N+1 →" și decide teaserul), `index.html:2151` (cardul din secțiunea News de pe homepage). Toate trei au funcționat automat, fără deploy. Teaserul static „Episodul 2 vine în curând" e legat prin cod DOAR de episodul 1 (`news.html:642`), deci nu trebuie atins la episoadele următoare. **LINKURI CĂTRE GRUPURI ÎN FINALUL ARTICOLULUI (adăugate manual în HTML-ul din admin):** Parcul Circului → `/grup-details.html?id=75a1c2cf-6683-4802-8ff8-1a236661f82f`, Rond Coșbuc → `/grup-details.html?id=041eb6f5-ef07-4812-b56c-c325a84ee3f8`. Verificat în cod că `grup-details.html` **NU face redirect la login** (`checkAuth()` doar citește sesiunea, dacă există), iar `grupuri` e citibilă cu cheia anon, deci linkurile merg și pentru un cititor nelogat. **DE REPARAT CÂND SE NIMEREȘTE, în admin → Grupuri:** numele grupului „Parcul Circului**,**" are o virgulă rătăcită la final, iar „Rond **Cosbuc** - Palatul Parlamentului - Parc Carol" e fără diacritice. **NEWSLETTERUL — BROADCAST RESEND, trimis 1 august.** Fișierul `email_templates/newsletter-episod-3-terenul.html` e **teaser, nu articolul integral**, decizie deliberată: cu tot textul în email, omul citește în Gmail și nu ajunge niciodată pe site, deci se pierd statistica Plausible, formularul de newsletter de la finalul episodului, navigarea între episoade și linkurile către grupuri; în plus Gmail taie mesajele lungi cu „[Mesaj trunchiat]". Structura, refolosibilă la episodul 4 (se schimbă titlul, poza, cele trei paragrafe și linkul): antet pe brand (crem `#faf8f3`, teracotă `#c2604a`, `translate="no"` + `notranslate` pe „apartamenTUal" ca Gmail să nu-l traducă „apartamentul tău"), eyebrow + titlu, poza articolului trasă direct din storage-ul public Supabase, trei paragrafe (primele două aproape identice cu deschiderea articolului), buton „Citește episodul 3", casetă cu linkuri către episoadele 0/1/2, footer cu **`{{{RESEND_UNSUBSCRIBE_URL}}}` (TREI acolade, obligatoriu la broadcast)**. Subiect trimis: „Episodul 3: terenurile pe care nu le vrea nimeni"; preview text: „Prea mici pentru un dezvoltator, prea scumpe pentru o casă. Exact pe astea le căutam." **⚠️ CAPCANĂ DE PROCES, costă o ciornă:** HTML-ul se lipește DOAR în modul de cod al editorului Resend; lipit în editorul vizual, apare literal, cu tot cu tag-uri. Prima încercare a fost cu HTML-ul articolului din platformă (corpul textului, fără antet/buton/footer) — ciorna se aruncă din Broadcasts → meniul cu trei puncte → Delete. Fluxul corect în interfață: **„Test email"** (sus-dreapta, nu câmpul „To", care acceptă doar segmente) → **„Review"** → panoul „Ready to send?" → **„Slide to send"**. Testul nu închide ciorna și nu atinge lista. **CIFRELE, verificate în ambele sisteme înainte de trimitere (se potrivesc perfect):** Supabase `/admin-newsletter.html` → 27 în total = **16 confirmați**, 6 pending, 5 dezabonați; Resend → segment „All Contacts" 21, minus 5 dezabonați global = **16 destinatari**. Cei 21 din Resend sunt exact cei 16 confirmați + cei 5 dezabonați (contactul rămâne, doar marcat); cei 6 `pending` nu ajung niciodată în Resend, corect prin design (contactul se creează abia la confirmare). **Toți cei 5 dezabonați sunt conturile de test `luta.lucian.m+test01..05`, deci ZERO abonați reali pierduți** — proporția de 5 din 21 arată alarmant, dar nu e churn. **DE IGNORAT:** „AI content review" din Resend a semnalat un fals pozitiv („Typo 'terenurile' in subject, should be 'terenurile'", adică exact același cuvânt). **⏭️ DESCHIS, DE INVESTIGAT CÂNDVA: 6 din 27 de abonați rămân blocați pe `pending`**, adică s-au abonat dar n-au apăsat niciodată linkul de confirmare; cinci dintre ei în aceeași zi, 16 iulie, toți de pe banda de homepage (`homepage_band`). Un sfert din înscrieri se pierd la pasul de confirmare. De verificat dacă emailul de confirmare ajunge în spam sau dacă textul nu e destul de clar. **⏭️ DE DECIS:** dacă se comite `email_templates/newsletter-episod-3-terenul.html` ca tipar pentru episoadele viitoare. **LIMITARE CUNOSCUTĂ, reconfirmată azi:** poza episodului se vede DOAR în email; pe Facebook/WhatsApp preview-ul e identic la toate episoadele (og:image static în `news.html`, fiindcă articolele n-au URL propriu, sunt `/news.html#slug` iar partea de după `#` nu ajunge la crawler). **DE ȘTIUT LA URCAREA POZELOR:** admin-ul **nu comprimă nimic**, urcă fișierul exact cum e; poza principală se taie la max 400px înălțime cu `object-fit: cover`, deci imaginile orizontale sunt singurele care ies bine.)
> 1 august 2026, dimineața (**✅ EXPUNEREA PUBLICĂ A EMAILURILOR E ÎNCHISĂ. FAZA 2 (cod) + FAZA 3 (revocarea drepturilor) FĂCUTE, DEPLOYATE ȘI VERIFICATE DIN EXTERIOR. Commituri `76bf2ed` (cod) + `6b195a9` (SQL), ambele pushate; frontend deployat din cPanel de Lucian; SQL rulat de Lucian în Supabase.** Un vizitator fără cont nu mai poate citi emailuri, telefoane, nume reale, notițe sau flagurile de admin. **PROBA, făcută cu cheia anon din `frontend/js/supabase-config.js`, din afara site-ului:** `select=email`, `phone`, `notes`, `first_name`, `last_name`, `age`, `is_super_admin` și `select=*` întorc toate **`401`, `permission denied for table profiles`** — înainte întorceau `HTTP 206` cu `Content-Range: 0-4/80` și adrese reale. **FAZA 2 — CE S-A SCHIMBAT ÎN COD (2 fișiere, +30/−6):** regula aplicată e **vizitator NELOGAT → view-ul `profiles_public`; utilizator LOGAT → tabela `profiles`, exact ca înainte**. Concret: `frontend/js/profile-view-new.js:87` (sursa profilului afișat) + `frontend/grup-details.html:1387` (o pereche `profileSource`/`emailCol` folosită în ambele selecturi, la cererile `pending` și la membri). **DE CE NU EXACT CA ÎN PLANUL DIN 31 IULIE:** planul zicea „doar propriul profil și superadminul rămân pe `profiles`", dar citirea codului a arătat că varianta aia rupe trei trasee reale — invitația din pagina de profil (`sendProfileInvite`, `profile-view-new.js:1031`, se ramifică pe `profileData.email` și, fără el, **bagă omul direct ca `pending` în grup** în loc să-i trimită invitație), emailul de aprobare a unei cereri (`approveRequest`, `grup-details.html:3272`, primește adresa ca argument din selectul de la 1395) și anunțul cu linkul de WhatsApp către membri (`notifyMembersWhatsAppLink`, `:3666`, adună `m.profile.email`). **VERIFICAT ÎNAINTE DE REVOCARE, ca să nu rămână pagini goale:** toate interogările pe `profiles` din frontend au fost trecute una câte una — fiecare selectare de `email`/`phone`/`age` e în spatele unei verificări de login (`grupuri.js:584`, `comanda-analiza.js:98`, restul din `grup-details.html`/`grup-edit.html`, plus paginile de admin), iar paginile publice cer doar coloane rămase permise. **⚠️ CAPCANA CARE A COSTAT O RULARE DEGEABA — DE ȚINUT MINTE PENTRU ORICE SQL VIITOR:** editorul SQL din Supabase rulează **tot scriptul ca o singură tranzacție**, deci blocul de probă `BEGIN; SET LOCAL role anon; …; ROLLBACK;` de la finalul fișierului **a anulat și `REVOKE`/`GRANT`-urile de deasupra**, tăcut, după ce controalele afișaseră deja rezultate care păreau bune. Prima rulare „a mers" fără să schimbe nimic; s-a prins doar pentru că verificarea s-a făcut din exterior, cu cheia anon, nu în editor. Fișierul e reparat (`6b195a9`): proba „ca anon" e acum comentariu cu instrucțiunea s-o rulezi în alt tab, plus un pas 0 de diagnostic (dacă dreptul ar veni de la rolul `PUBLIC`, revocarea de la `anon` singură n-ar fi de ajuns) și un `GRANT SELECT … TO authenticated` ca plasă. **REZULTATUL RULĂRII:** `anon` mai are exact **20 de coloane** pe `profiles`. **PAGINILE PUBLICE, VERIFICATE DUPĂ REVOCARE** (interogările lor exacte, rulate ca anonim): `utilizatori.html` 63 de rânduri, banda de potriviri de pe homepage 80, agenții 1, autorul anunțului pe `teren-details` 80, membrii activi pe `grupuri.html` 80, membrii pe `grup-details.html` prin view 80. Prin `profiles_public`, `email` iese `null` la toți în afară de cei 13 legitimi (1 agenție + 12 cu `is_email_public` bifat). **EFECT SECUNDAR ACCEPTAT CONȘTIENT:** pentru un vizitator nelogat, un membru fără pseudonim apare „Utilizator" în loc de partea din față a emailului (`displayName()`, `grup-details.html:3730`). Confirmat vizual de Lucian pe „Parcul Circului". **DE ȘTIUT, ca să nu se caute bug inexistent:** pagina de profil a arătat dintotdeauna „Utilizator" pentru cine n-are pseudonim — `profile-view-new.js:259` folosește `pseudonym || 'Utilizator'`, fără fallback pe email, linie nemodificată din 27 ianuarie; fallback-ul pe email există doar în `grup-details.html`. Iar `radulesc` nu apare în „Utilizatori" pentru că lista filtrează profilurile fără pseudonim (`utilizatori.js:186`) — n-a apărut niciodată acolo. **⚠️ CE RĂMÂNE DESCHIS, DELIBERAT:** rolul `authenticated` citește în continuare **toate** emailurile și telefoanele — oricine își face cont și își confirmă adresa ajunge la ele. Închiderea cere ca `notify-admins` să primească `user_id`-uri și să rezolve emailurile pe server cu service role, fiindcă azi paginile de grup le adună în browser. **Sesiune separată.** **GAURĂ NOUĂ, GĂSITĂ AZI, ALTĂ ZONĂ:** vezi task-ul 🟠 din „DE ÎNCEPUT ÎN SESIUNEA URMĂTOARE" — lista de membri a oricărui grup e citibilă fără cont.)
> 31 iulie 2026, seara (**EMAILURILE ȘI TELEFOANELE TUTUROR UTILIZATORILOR SUNT CITIBILE PUBLIC, FĂRĂ CONT — DEMONSTRAT PRACTIC. FAZA 1 DIN REPARAȚIE E RULATĂ; GAURA E ÎNCĂ DESCHISĂ. ⏭️ RĂMÂN FAZA 2 (3 modificări de cod) + FAZA 3 (revocarea drepturilor). ZERO cod atins, ZERO commituri în sesiunea asta.** Pornit de la o întrebare banală: userul `radulesc@gmail.com`, cu profil necompletat, zicea că a dat join în grupul „Parcul Circului," dar Lucian nu-l vedea nicăieri. **RĂSPUNSUL LA ÎNTREBAREA INIȚIALĂ:** e membru `activ` din 24 iulie, aprobat de George, iar pagina ÎL AFIȘEAZĂ — ca „radulesc", pentru că `displayName()` (`grup-details.html:3720`) cade pe partea din față a emailului când lipsește pseudonimul. E rândul 12 din 15 membri, între paisprezece oameni cu prenume normale; a fost ratat vizual, nu ascuns tehnic. Verificat cu SQL: 15 activi în DB, 15 vizibili pe pagină, zero filtrate (CSV-uri în `screenshots/20260731/`). **DA, UN CONT CU PROFIL NECOMPLETAT POATE DA JOIN** — nimic nu-l oprește, nici în UI, nici în DB. **CE A IEȘIT LA IVEALĂ PE DRUM, MULT MAI GRAV:** tabela `public.profiles` e citibilă anonim în întregime. Politica `profiles_select_all` are `SELECT`, rol `public`, `qual = true`; politicile permisive se combină cu **OR**, deci `anon_view_profiles` (scrisă cu `account_type='activ' AND pseudonym IS NOT NULL`, evident ca să ascundă ceva) **nu restrânge absolut nimic**. **DEMONSTRAT, nu presupus:** `GET /rest/v1/profiles?select=email&limit=5` cu cheia anon din `frontend/js/supabase-config.js` (care ajunge prin design în browserul oricui) → `HTTP 206`, `Content-Range: 0-4/80`, emailuri reale în răspuns. Cu `select=*` vin **31 de coloane**: `email`, `phone`, `first_name`, `last_name`, `age`, `notes`, `is_admin`, `is_super_admin`. Există coloanele `is_email_public` / `is_age_public`, deci controlul de intimitate a fost gândit — dar se aplică în JavaScript (`profile-view-new.js:302`), după ce serverul a trimis deja datele. La fel `notes`, ascunse cu `if (isOwnProfile)` (`profile-view-new.js:447`) dar citibile de oricine. **DE CE NU SE REPARĂ CU RLS:** RLS filtrează RÂNDURI, nu coloane; singurul mecanism din Postgres care ascunde o coloană e `GRANT`/`REVOKE` pe coloană, iar PostgREST îl respectă. **REPARAȚIA, ÎN 3 FAZE, în `db_schema/securitate-profiles/`:** FAZA 1 = `1-creeaza-view-public.sql` (view `profiles_public`: fără telefon/nume real/notițe/flaguri de admin; email și vârstă doar cu bifa explicită — regulile de intimitate mutate pe server) — **RULATĂ de Lucian pe 31 iulie, rezultat `80 / 13 / 1`**: 80 de rânduri, 13 emailuri vizibile legitim (1 agenție + 12 cu `is_email_public` bifat conștient — bifa e opt-in, `profile-edit-new.html:114` n-are `checked`), 1 cont de agenție. Faza asta e **pur aditivă, n-a schimbat nimic pentru vizitatori**. FAZA 2 = codul, NEFĂCUTĂ. FAZA 3 = `3-revoca-coloanele-sensibile.sql`, NERULATĂ — **abia ea închide gaura**. **⚠️ ORDINEA E OBLIGATORIE** (view → cod pe cPanel → revocare); inversată, paginile publice rămân fără date. **CE NU REZOLVĂ NICI DUPĂ FAZA 3:** doar accesul ANONIM se închide. Rolul `authenticated` citește în continuare toate emailurile — oricine își face cont ajunge la ele. Motivul pentru care nu s-a făcut în același pas: paginile de grup își adună în browser emailurile membrilor și le pasează la `notify-admins`; ca să-i luăm lui `authenticated` dreptul pe `email`, edge function trebuie să primească `user_id`-uri și să rezolve emailurile pe server, cu service role. **DESCOPERIRE CONEXĂ, relevantă pentru regula de join:** din 77 de conturi active, 60 au profil complet și 17 sunt sub prag — iar cei 16 fără pseudonim n-au NIMIC (nici zone, nici taguri, nici preferințe). Cauza: `register.js:578-584` citește câmpuri (`is_email_public` etc.) care NU EXISTĂ în `register.html` — cod mort de pe vremea când înregistrarea colecta tot profilul. Azi înregistrarea doar creează contul și trimite omul la `profile-edit-new.html?welcome=1`, iar cine nu duce treaba la capăt rămâne cu un cont gol. **Pragul „profil completat" nu trebuie inventat:** formularul cere deja pseudonim + camere + suprafață + oraș (`required` în HTML) + cel puțin o zonă (`profile-edit-new.js:587`) + cel puțin un tag (`:592`). **radulesc e SINGURUL membru de grup cu profil necompletat din toată platforma** — ceilalți 16 nu sunt în niciun grup. **Rămâne decizia lui Lucian** ce se întâmplă cu el: rămâne cu etichetă, e ascuns până completează, sau iese cu email de înștiințare. **ALTE DOUĂ LUCRURI DE ȘTIUT:** (1) în „Parcul Circului," e o cerere de alăturare `pending` de la `rox.brustur@yahoo.com` din 27 iulie, neaprobată — adminul grupului e George (`gxg1313@yahoo.com`), nu Lucian, care îl vede doar ca superadmin; (2) `fabian_224@yahoo.com` are cont de auth cu email confirmat dar **fără rând în `profiles`** — singurul orfan real dintre cei 41 (restul sunt conturi de test `luta.lucian.m+testN@`).
> 30 iulie 2026, seara târziu (**PLATA CARE NU AJUNGEA LA NOI — CAUZA GĂSITĂ ȘI REPARATĂ. Commit `c7c0f1c`, DEPLOYAT + WEBHOOK-URI ÎNREGISTRATE. ⏭️ RĂMÂNE UN SINGUR LUCRU: proba pe o comandă reală de 99 RON, amânată deliberat pentru o sesiune curată.** Pornit de la simptomul din intrarea de mai jos: clientul a plătit, dar nu a primit de la noi emailul de confirmare. **CAUZA NU ERA UNA, ERAU CINCI BLOCAJE SUPRAPUSE** — fiecare singur ar fi rupt lanțul, deci reparând doar unul nu s-ar fi întâmplat nimic vizibil: **(1)** în Oblio **nu era înregistrat NICIUN webhook** (confirmat prin API, nu presupus — `list` a întors zero); codul de înregistrare nu exista nicăieri în repo, iar README-ul citat în capul funcției nu a existat niciodată. **(2)** funcția era deployată cu **Verify JWT pornit**; Oblio nu trimite token Supabase, deci poarta răspundea 401 **înainte** de codul nostru — și de aici lecția care a deblocat diagnosticul: **o cerere respinsă de poartă NU apare în logurile funcției**, deci „loguri goale" nu însemna „nu ne-a apelat nimeni", ci putea însemna și „ne-a apelat și a fost respins la ușă". Departajat cu un `curl` fără token: mesajul `UNAUTHORIZED_NO_AUTH_HEADER` vine de la poartă, `Invalid signature` de la codul nostru. **(3)** Oblio cere ca adresa să răspundă **200 cu base64(`X-Oblio-Request-Id`)**, și verifică asta chiar la înregistrare; noi răspundeam cu JSON, deci înregistrarea ar fi picat oricum. **(4)** `OBLIO_WEBHOOK_SECRET` **era deja setat** (descoperit din întâmplare, la salvare: „a secret with this name already exists"), iar codul vechi, când găsea un secret, cerea header-ul `x-oblio-signature` — **pe care Oblio nu-l trimite**, fiindcă webhook-ul lui are doar `cif`/`topic`/`endpoint`, fără câmp de secret. **(5)** lookup-ul căuta seria **facturii**, dar în `oblio_factura_serie/numar` stă **proforma** până la plată (bug-ul latent semnalat deja pe 29 iulie). **FIXUL (`supabase/functions/oblio-webhook/index.ts`, +146/−30, `deno check` curat; `creeaza-proforma-oblio` NEATINSĂ, zero migrații, zero atingeri de schemă/RLS/frontend):** ecou base64 calculat **înaintea** oricărei alte prelucrări, ca să răspundem corect inclusiv cererii de verificare de la înregistrare (care poate veni ca GET sau fără corp); cheia se citește din adresă (`?k=...`), fiindcă Oblio nu are unde să pună un secret; **fail closed** — fără `OBLIO_WEBHOOK_SECRET` setat funcția refuză să proceseze (503), decizie deliberată: mai bine se oprește zgomotos decât să lase un endpoint public prin care oricine declară comenzi ca plătite; **lookup în trei trepte** — seria din payload, seria facturii, apoi `order_id`-ul căutat cu regex în TOT corpul cererii (rezistă la orice formă de payload, fiindcă `order_id` apare atât în `mentions`, cât și în numele clientului de pe proformă); **actualizarea pe `paid` doar dacă starea e încă `pending_payment`** (`.eq('status','pending_payment').select('id')`) — dacă Oblio trimite două notificări pentru aceeași plată, a doua nu mai retrimite emailul clientului; `Collect/Inserted` recunoscut ca plată; payload-ul brut se loghează integral. **UNEALTĂ NOUĂ: `scripts/oblio-webhook/oblio-webhooks.mjs`** (list / register / delete, fără dependențe, chei doar din variabile de mediu, `register`/`delete` refuză fără `--confirm`). **DEPLOYAT** cu `npx supabase functions deploy oblio-webhook --no-verify-jwt` și verificat live prin `curl` (mesajul s-a schimbat din cel al porții în cel al codului nostru = JWT oprit + cod nou intrat). **ÎNREGISTRATE 2 topicuri:** `Collect/Inserted` (id `5129`) + `Invoice/SaveDraft` (id `5132`), al doilea ca plasă de siguranță dacă primul nu vine. **⚠️ INCIDENT DE SECURITATE, REZOLVAT:** la prima înregistrare, cheia a ajuns pe ecran și în chat — Oblio întoarce în răspuns endpoint-ul COMPLET, cu tot cu `?k=`, iar scriptul afișa răspunsul brut nemascat (mascase doar previzualizarea). Cheia a fost **rotită** (secret nou în Supabase, webhook vechi `5126` șters, ambele reînregistrate), iar scriptul maschează acum `k=` în răspunsul brut, în listare și în previzualizare. Tot atunci s-a reparat și un fals negativ: Oblio răspunde **201** la creare, iar scriptul verifica doar `200`, deci raporta „NEÎNREGISTRAT" deși reușise. **COMANDA LUI ALIN, REPARATĂ MANUAL:** `db_schema/reparatii/2026-07-30-comanda-APT-20260729-80d946.sql`, rulat de Lucian — `paid`, `paid_at = 2026-07-29 17:47:00+03`, factura `LTFB 0205`, plus un rând în jurnal care spune explicit că a fost reparare manuală și că emailul automat NU a plecat. **De ce manual și nu prin funcție:** funcția ar fi trimis și emailul „Comandă confirmată", iar **Lucian îi scrisese deja personal** clientului că plata a intrat și că analiza vine în 3-5 zile — al doilea mesaj automat ar fi fost derutant. Consecința de reținut: **traseul de trimitere a emailului de confirmare nu a rulat niciodată cu succes**, nici până acum, nici la reparație. **⏭️ DE FĂCUT ÎN SESIUNEA URMĂTOARE:** comanda de test de 99 RON — vezi blocul 🔵 din „DE ÎNCEPUT ÎN SESIUNEA URMĂTOARE".)
> 29 iulie 2026, seara (**DOUĂ LUCRURI, AMÂNDOUĂ ÎNCHISE ȘI DEPLOYATE ÎN ACEEAȘI SEARĂ. (1) LINK PARTAJABIL CĂTRE CREAREA DE GRUP — commit `f0fb9cc`, pushat, DEPLOYAT din cPanel + verificat live de Lucian.** Cerere: un link de pus într-un comentariu la o postare, care să ducă omul direct la formularul de creare grup. Link-ul e pur și simplu **`https://apartamentual.ro/grup-nou.html`** — nu s-a inventat URL scurt (ar fi cerut folder nou de deployat la fiecare rundă, câștig doar cosmetic; Lucian a ales varianta directă). Pagina se apăra deja singură: nelogat → overlay „Autentificare necesară" + modal, iar `login-modal.js` face `location.reload()`, deci omul rămâne pe formular. Trei modificări: **(a)** `grup-nou.html` n-avea deloc Open Graph → link-ul apărea în postare fără imagine și cu titlu sec; adăugate og/twitter (titlu „Pornește un grup de construcție — ApartamenTUal", aceeași randare ca pe restul site-ului, 1200×1200) + `canonical`. **(b)** sub „Conectează-te" adăugat „Nu ai cont? Creează-l în doi pași" → `/register.html?redirect=%2Fgrup-nou.html`; `register.js` folosește parametrul ăsta ca `emailRedirectTo`, deci **linkul de confirmare din email aduce omul înapoi pe formularul de creare grup, deja logat**, nu pe homepage. **(c)** `register.js`: `emailRedirectTo` accepta doar URL absolut (invitațiile trimit `window.location.href`) → acum normalizează și relativele cu `new URL(..., origin)`, altfel confirmarea s-ar fi rupt; plus eticheta butonului de succes se potrivește cu destinația („creează grupul" vs „acceptă invitația"). **SUPABASE:** `https://apartamentual.ro/**` era deja în Redirect URLs (deci fluxul mergea), **dar `www` NU** — verificat live că `https://www.apartamentual.ro/` răspunde 200 **fără redirect** către varianta fără www, deci `window.location.origin` devine `www.` și `emailRedirectTo` pica pe Site URL (homepage) — exact tiparul problemei din 24 iulie. Lucian a adăugat `https://www.apartamentual.ro/**` (Total URLs: 6). **⚠️ DESCOPERIRE COLATERALĂ, DE DISCUTAT SEPARAT: Render e ÎNCĂ LIVE și încă face deploy automat din repo** — `apartamentual.onrender.com/grup-nou.html` avea meta-tag-urile noi deși Lucian deployase doar în cPanel. Deci există **două copii publice ale site-ului** (risc de conținut duplicat la Google, oameni care aterizează pe copie). **NU șterge cele 4 intrări `onrender` din Supabase Redirect URLs cât timp Render e viu** — ar rupe autentificarea acolo. **(2) DESCRIEREA TERENULUI AJUNGEA TRUNCHIATĂ ÎN EMAILUL DE COMANDĂ — commit `1eaf54d`, pushat, ambele edge functions DEPLOYATE de Lucian.** Simptom: la prima comandă reală de analiză preliminară (`APT-20260729-80d946`, Alin Tortolea, 99 RON, proforma Oblio `APT 0013`, teren Str. Constantin Bosianu nr. 32, Sector 4), rândul „Descriere teren" din emailul intern se oprea în mijlocul frazei, fără niciun semn de tăiere. **CAUZĂ:** `creeaza-proforma-oblio/index.ts` tăia la `substring(0, 200)` în payload-ul către `notify-admins`, iar `notify-admins` punea „…" doar dacă textul avea **fix** 250 (plafonul lui) — primind 200, îl credea complet. **Dovadă:** textul din email avea exact 200 de caractere, identice cu primele 200 din DB (316 în total). **Nimic nu s-a pierdut din date** — `comenzi_analize.descriere_teren` salvează integral; ce lipsea din email era exact partea utilă: repartiția suprafețelor (`2 x 100-120 mp; 4 x 60-80 mp`) și o întrebare directă a clientului despre parcarea în zonă. Fix: descrierea pleacă integral (modificare în pasul 7 de notificare, **după** crearea proformei, în `try/catch` non-fatal — logica de plată, TVA, apelul Oblio și insertul NEATINSE), plafon de afișare 2000 în `notify-admins`, „…" comparat cu lungimea reală, și helper nou `formatMultilineText` care transformă rândurile scrise de client în `<br>` **și le escapează înainte să intre în HTML** (până acum text de la client intra direct în HTML-ul emailului). **⏭️ RĂMAS DESCHIS, NEATINS DELIBERAT:** descrierea de pe **proforma Oblio** se trimite tăiată la `substring(0, 500)` (`creeaza-proforma-oblio/index.ts`, în blocul `products`) — e câmp care ajunge pe un document fiscal, deci e decizie separată; la comanda de azi (316 caractere) nu s-a manifestat. **(3) TOT PE ACEEAȘI COMANDĂ, DESCOPERIT LA FINALUL SESIUNII: plata a fost confirmată de Netopia și facturată de Oblio, dar la noi comanda a rămas `pending_payment` — Oblio nu ne-a apelat webhook-ul.** Diagnostic complet, zero cod atins, task-ul e descris pe larg în secțiunea „⏭️ DE ÎNCEPUT ÎN SESIUNEA URMĂTOARE" de mai jos, marcat 🔴.)
> 28 iulie 2026, seara (**TREI LUCRURI ÎNCHISE ÎN ACEEAȘI SEARĂ, TOATE TESTATE LIVE. Nimic în așteptare din ele.** **(1) BIFA DE NEWSLETTER LA ÎNREGISTRARE — ✅ DEPLOYATĂ ȘI TESTATĂ.** Cele „⏭️ DE DEPLOYAT" din intrarea de mai jos (commit `07413b9`) sunt FĂCUTE: cele 3 fișiere de frontend erau deja pe cPanel (verificat direct pe site-ul live: `register.html` conține bifa `newsletterOptin`, `js/register.js` scrie `newsletter_optin` în metadata, `profile-edit-new.html` apelează modul `verified_signup`), iar edge function-ul `newsletter-subscribe` a fost redeployat de Lucian pe 28 iulie seara. **Verificat că s-a urcat exact codul din repo:** fișierul din `C:\Users\lucia\supabase\supabase\functions\newsletter-subscribe\index.ts` are același hash de conținut cu cel din repo (diferă doar CRLF vs LF). **Testat live:** cont nou cu bifa pusă → a venit UN SINGUR email, exact cum a fost proiectat. **⏭️ SINGURUL LUCRU NEVERIFICAT ÎNCĂ:** că rândul apare `confirmed` (nu `pending`) cu sursa `register` în `/admin-newsletter.html` — se vede la orice înregistrare nouă cu bifa pusă; dacă apare `pending`, edge function-ul n-a prins deploy-ul. **(2) TEMPLATE „CONFIRM SIGNUP" REFĂCUT PE BRAND — commit `25c00b5`, pushat, ✅ APLICAT ÎN SUPABASE ȘI TESTAT LIVE de Lucian („a venit mailul pe brand, merge").** Descoperit din screenshotul emailului real de la testul de mai sus: emailul de confirmare a contului venea încă pe design-ul vechi (albastru `#2563eb`, buton portocaliu, „🏠 Apartamentual" fără TU, Arial, poziționarea veche „platforma care te ajută să găsești terenuri"). Era semnalat ca datorie încă din 24 iulie și rămăsese nefăcut. **ATENȚIE PENTRU VIITOR: template-ul NU stă în repo, stă în Supabase** → Authentication → Emails → „Confirm signup"; fișierul `email_templates/sign_up.html` e doar sursa de copiat, un commit pe el NU schimbă nimic în emailurile reale până nu se lipește manual în dashboard. Refăcut pe structura din `reset_password.html` (crem `#faf8f3`, teracotă `#c2604a`, header „apartamen**TU**al / by LTFB studio", buton ink). Subject pus: „Confirmă-ți contul pe ApartamenTUal". **Două adăugiri de conținut, nu doar cosmetică:** casetă „**După confirmare mai ai un pas**" care explică completarea profilului — exact locul unde se pierdeau cei 11 utilizatori din analiza de pe 24 iulie, iar emailul ăsta e singurul punct în care li se poate spune înainte să se piardă; și explicația concretă pentru linkul mort („apasă-l oricum: pagina pe care ajungi are butonul «Trimite-mi din nou emailul»"), verificată în `frontend/js/profile-edit-new.js:178-192` — prima formulare era vagă și Lucian a semnalat pe bună dreptate că nici lui nu-i era clar cum ceri alt link. `{{ .ConfirmationURL }}` păstrat în buton și în linkul de rezervă (⚠️ dacă cineva îl schimbă pe `{{ .SiteURL }}`, TOȚI userii ajung pe homepage în loc de pagina de profil). **(3) CAMPANIA „ZONE FĂRĂ GRUP" — trimisă, detalii mai jos.**)
> 28 iulie 2026 (**CAMPANIA „ZONE FĂRĂ GRUP" — ✅ TRIMISĂ pe 28 iulie, ora 21:20. Commit `76417b2`, pushat pe `main`. REZULTAT: 36 de emailuri, TOATE reușite, 0 eșecuri, 0 reîncercări (nimeni n-a lovit limita de rată Resend), 29 de secunde în total, 36 de adrese distincte deci nimeni n-a primit de două ori. 36 și nu 37 fiindcă Lucian a scos-o pe Rox (`rox.brustur@yahoo.com`) cu `--fara=`, având o cerere de alăturare la „Parcul Circului" încă neaprobată de George. Plus 3 emailuri de test pe `apartamentual@ltfbstudio.ro` = 39 din cele 100 gratuite pe zi. Jurnalul complet: `scripts/emailuri-zone/local/trimise-2026-07-28.json` (negitat, conține adrese reale). **⏭️ DE URMĂRIT ÎN ZILELE URMĂTOARE:** răspunsurile cu „stop" ajung pe `apartamentual@ltfbstudio.ro` și trebuie notate manual (nu există flag de consimțământ pe `profiles`) — la campania următoare se trec la `--fara=`; în Resend → Emails se văd deschiderile și respingerile; dacă cineva pornește un grup, vine notificarea obișnuită pe email + Slack. Scriptul rămâne refolosibil pentru orice campanie viitoare pe același model.** Mai jos, cum e construit.<br>**SCRIPTUL DE TRIMITERE.** Închide singurul lucru rămas din analiza zonelor (vezi intrarea de mai jos, unde scria „trimiterea: NEÎNCEPUTĂ"). ZERO atingeri în platformă: nimic din `frontend/`, nicio edge function, niciun SQL, niciun RLS, zero zonă de plăți. Rulează LOCAL și singurul serviciu extern atins e API-ul Resend.** **CE E:** `scripts/emailuri-zone/trimite-emailuri-zone.js` (472 linii, fără dependențe — Node 24 are `fetch` inclus) + `scripts/emailuri-zone/README.md` cu pașii. Citește CSV-ul exportat din `db_schema/analiza-zone/5-merge-multizona.sql` și compune un email personalizat per persoană, cu zonele ei fără grup. **TREI TREPTE, în ordine:** (1) `--mod=dry` (implicit) nu trimite nimic, scrie toate emailurile pe disc + o pagină `local/previzualizare.html` de răsfoit; (2) `--mod=test` trimite 3 emailuri reprezentative DOAR pe `apartamentual@ltfbstudio.ro`, cu `[TEST]` în subiect (cel cu cele mai multe zone, cel cu cele mai puține, unul din mijloc); (3) `--mod=live --confirm-trimit` — fără al doilea steag refuză să pornească. **PROTECȚII:** cheia API doar din `RESEND_API_KEY` (niciodată în linia de comandă, niciodată scrisă pe disc); pauză 600ms între trimiteri + reîncercare la 429/5xx cu așteptare crescătoare și `retry-after` respectat; **jurnal `local/trimise-<data>.json` scris după FIECARE email** → o re-rulare sare peste cei deja trimiși și reîncearcă doar eșecurile (⚠️ nu șterge jurnalul în ziua campaniei, altfel trimite a doua oară); oprire dacă CSV-ul are adrese duplicate sau invalide; parser CSV propriu, fiindcă `zone_pentru_email` are ghilimele și rânduri pe mai multe linii (un `split(',')` ar fi rupt fișierul); antet `List-Unsubscribe` ca `mailto:`. **DECIZII DE CONȚINUT (Lucian, 28 iulie), puse ca implicit în script:** subiectul e **varianta 2** — „În {zona_1} nu s-a pornit încă niciun grup" (fără cifră în subiect, cifrele apar în corp; `SUBIECT_IMPLICIT`); **`vlad.radu@gmail.com` („DeathArrow", 21 de zone bifate) e SCOS din lot** (`EXCLUSI_IMPLICIT`) → rămân **37 de destinatari**, nu 38. **MODIFICĂRI DE TEXT cerute după prima citire:** rând nou înainte de buton („Grupul apare imediat pe platformă, iar ceilalți care caută în {zona_1} îl văd și pot cere să intre. Primești o notificare la fiecare cerere."); **toate em-dash-urile scoase** (inclusiv din lista de zone în varianta text: `Tineretului: 20 de oameni`); „cinci familii" → „câteva familii"; ultimul paragraf se oprește la „În schimb, toți banii tăi rămân în apartamentul tău." — **fără nicio referire la marja dezvoltatorului**, decizie explicită a lui Lucian (profitul dezvoltatorului implică și un risc pe care aici îl preiau familiile, nu vrem antipatii). **REPETIȚIE REPARATĂ CONDIȚIONAT:** „Și încă N zone bifate, tot fără niciun grup" și „În niciuna dintre ele nu există încă vreun grup" spuneau același lucru, dar apăreau împreună doar la cei cu peste 3 zone; ștergerea seacă a celei de-a doua ar fi lăsat pe cei cu 2-3 zone fără nicio propoziție care să spună că nu există grup → acum a doua apare DOAR când lipsește prima. **DE ȘTIUT:** textul care se trimite efectiv e în funcția `continut()` din script; `email_templates/email-porneste-grup-zona.md` e versiunea de citit, actualizată la fel (dacă schimbi într-un loc, schimbă în ambele). `first_name` e gol la toți → numele vine din `pseudonym`, capitalizat automat dacă e scris cu mic („cristian" → „Cristian"). `scripts/emailuri-zone/local/` e în `.gitignore` (previzualizările și jurnalul conțin adrese reale). **⏭️ RĂMÂNE DE FĂCUT, în ziua trimiterii:** (a) re-rulează interogarea 5 în Supabase SQL Editor și exportă CSV proaspăt — **cifrele se schimbă de la o săptămână la alta**; (b) proba → citește 2-3 emailuri în `local/previzualizare.html`; (c) `$env:RESEND_API_KEY="re_..."` (cheie nouă, doar „Sending access") apoi `--mod=test`; (d) `--mod=live --confirm-trimit`, durează ~25 de secunde; (e) golește cheia din mediu și șterge-o din Resend. Plan gratuit Resend = 100 emailuri/zi, lotul e de 37. **DE DECIS ÎNAINTE:** cererea lui **Rox** (rox.brustur@yahoo.com) încă îl așteaptă pe George să o aprobe din 27 iulie, iar ea e pe listă — ar fi ciudat să primească „nu s-a pornit niciun grup în Aviației" cu o cerere în așteptare; se scoate cu `--fara=rox.brustur@yahoo.com` sau se aprobă întâi cererea. **DE SEMNALAT (nerezolvat):** cele 8 fișiere CSV netrackate din `screenshots/` conțin adresele reale ale utilizatorilor — **nu le comite**, eventual adaugă-le în `.gitignore`.**)
> 28 iulie 2026 (**BIFĂ DE NEWSLETTER LA ÎNREGISTRARE — commit `07413b9`, PUSHAT pe `main`. ⏭️ DE DEPLOYAT: 1× edge function + 3× cPanel (frontend), apoi test. FĂRĂ migrație SQL. ZERO atingeri plăți/RLS/DB.** Cerere Lucian: la înregistrare să apară o bifă, NEBIFATĂ implicit, pentru abonarea la newsletter, cu mesaj despre episoadele experienței Județului Housing. **DECIZIA CARE A MODELAT TOT (aleasă explicit de Lucian dintre două variante):** newsletterul are double opt-in, deci abonarea de pe site trimite mereu un al doilea email de confirmare — dacă bifa ar fi apelat direct `newsletter-subscribe` la înregistrare, omul primea DOUĂ emailuri deodată („confirmă contul" + „confirmă abonarea") și rămânea neabonat dacă îl ignora pe al doilea. S-a ales varianta cu **UN SINGUR email**: bifa nu abonează pe loc, ci se salvează ca `newsletter_optin` în metadata contului (`options.data` la `signUp`), iar abonarea efectivă se face pe pagina de bun-venit, în blocul `?welcome=1` din `profile-edit-new.html`, DUPĂ ce omul a apăsat linkul de confirmare a contului. În acel punct adresa e dovedită de Supabase Auth + bifa e consimțământ explicit = abonare validă GDPR fără al doilea email. **MOD NOU `verified_signup` în `supabase/functions/newsletter-subscribe/index.ts`:** scrie direct `status='confirmed'` + `confirmed_at` și creează contactul global în Resend (aceeași logică idempotentă ca în `newsletter-confirm`), fără email de confirmare. **Blindat, fiindcă sare peste double opt-in:** acceptat DOAR dacă tokenul din headerul `Authorization` e JWT de utilizator real (`supabase.auth.getUser(jwt)` pe clientul service_role), cu `email_confirmed_at` setat, ȘI adresa cerută e chiar a acelui utilizator — altfel 401/403. În plus: dacă adresa e deja `unsubscribed`, **NU o reabonează** (îi respectă decizia, returnează `respected_unsubscribe`); dacă e deja `confirmed`, întoarce `already`; dacă era `pending` (se abonase de pe site fără să apese linkul), o confirmă și păstrează `consent_at` original ca dovadă. Apelul e idempotent, deci reintrarea pe pagina de bun-venit nu strică nimic. Refactor minor pe drum: notificarea internă și crearea contactului Resend extrase în `notifyNewSubscriber()` / `createResendContact()`, folosite din ambele trasee. **Fluxul normal (formularele din footer + banda de pe homepage) e NEATINS** — rămâne double opt-in exact ca înainte. Sursa apare `register` în `/admin-newsletter.html`. **BONUS, un bug preexistent găsit în timpul verificării vizuale în browser:** în `register.html` bifa OBLIGATORIE de termeni stătea **sub** butonul „Continuă", adică sub marginea ecranului la rezoluție normală — omul apăsa fără s-o vadă, iar mesajul de eroare „Trebuie să accepți termenii" apărea tot în afara ecranului, deci părea că butonul pur și simplu nu face nimic. Ambele bife au fost mutate ÎN interiorul `<form>`, înaintea butonului (verificat în browser pe server local: eroarea e acum vizibilă, validarea JS neschimbată). **TEXTUL BIFEI (cerut fără „mic" și fără em-dash):** „Vreau să primesc newsletterul: episoadele din construcția Județului Housing, blocul ridicat colaborativ prin ApartamenTUal, plus terenuri noi și pași practici. Fără spam, te dezabonezi oricând." **4 FIȘIERE:** `frontend/register.html` (bifa + mutarea blocului de bife), `frontend/js/register.js` (citește bifa → `newsletter_optin` în metadata), `frontend/profile-edit-new.html` (apelul de abonare, best-effort, cu `console.warn` la eșec), `supabase/functions/newsletter-subscribe/index.ts` (modul `verified_signup`). **NEATINSE:** plățile (Oblio/Netopia), `newsletter-confirm`, `newsletter-unsubscribe`, `notify-admins`, RLS, schema. **DE ȘTIUT:** tabelul `newsletter_subscribers` n-a avut nevoie de migrație — `confirm_token` are `default gen_random_uuid()`, deci pe traseul nou nu se setează deloc. Cele două funcții `signUp()` din `js/app.js` și `js/profile-view.js` sunt **cod mort în engleză** (niciun handler nu le cheamă), lăsate neatinse — dacă cineva le reactivează cândva, bifa NU merge acolo. **⏭️ DE DEPLOYAT:** (a) fișierul e deja copiat în `C:\Users\lucia\supabase\supabase\functions\newsletter-subscribe\index.ts` → din `C:\Users\lucia\supabase`: `npx supabase functions deploy newsletter-subscribe` — **deploy NORMAL, fără `--no-verify-jwt`**, funcția are nevoie de JWT-ul utilizatorului ca să verifice cine e; (b) cPanel: `frontend/register.html`, `frontend/js/register.js`, `frontend/profile-edit-new.html`. ⚠️ edge function + frontend merg ÎMPREUNĂ. **TEST după deploy:** cont nou cu bifa pusă → trebuie să vină **UN SINGUR** email → clic pe link → în `/admin-newsletter.html` rândul apare direct `confirmed`, sursa `register`; contact în Resend; notificare pe `apartamentual@ltfbstudio.ro` + Slack. **DIAGNOSTIC dacă nu merge:** rând `pending` în loc de `confirmed` = edge function-ul NU a fost redeployat; niciun rând = deschide consola pe pagina de bun-venit, motivul e într-un `console.warn` („Newsletter (înregistrare)"). **Cod COMPLET, rămâne deploy-ul + testul.**)
> 28 iulie 2026 (**ANALIZA ZONELOR FĂRĂ GRUP + CAMPANIE EMAIL CĂTRE 38 DE OAMENI (pregătită, NETRIMISĂ) ȘI, pe parcurs, UN BUG DE NOTIFICĂRI GĂSIT ȘI REPARAT. Commituri `e9b961c` (fix) + `d157df4` (analiza), PUSHATE pe `main`. Fixul e DEPLOYAT din cPanel și TESTAT LIVE de Lucian pe 28 iulie — merge. ⏭️ RĂMÂNE UN SINGUR LUCRU: scriptul de trimitere pentru cei 38 — NEÎNCEPUT, sesiune dedicată. ZERO atingeri plăți/DB/RLS/edge functions.**
> **PARTEA 1 — ANALIZA (interogările sunt commise în `db_schema/analiza-zone/`, se reia fără reconstrucție).** Întrebarea lui Lucian: cine are zone comune dar nu se unește în grupuri, ca să le trimitem mail să pornească grup în zonele cele mai căutate. **Structura DB relevantă:** `profiles` (account_type, account_status, pseudonym, is_demo, is_admin, is_super_admin) × `user_preferred_zones` × `zones(city_id)` × `cities`; grupurile: `grupuri` (is_demo, status, oras, **plus o coloană text `zona` folosită doar la afișare**) × `grup_preferred_zones` × `grup_membri`. **Definiții folosite (identice cu pagina publică /utilizatori.html):** utilizator real = `account_type='activ'` + `pseudonym` completat + `is_demo=false` + (`account_status` null sau 'active'); grup real = `is_demo=false` + `status<>'arhivat'`. **CLASAMENTUL FINAL (27–28 iulie, după toate excluderile), zone FĂRĂ niciun grup:** Tineretului 20, Primăverii / Dorobanți 16, Domenii 14, Aviației 12, Uranus 11, **Zona Centru Nord 11** (lipsea complet din lista inițială a lui Lucian), Iancului 10, Obor 9, apoi Kiseleff și Politehnica cu câte 8. **Față de analiza de acum câteva zile:** Tineretului 17→20, Primăverii/Dorobanți 13→16, Aviației 9→12, Domenii 13→14, Uranus 10→11; în scădere: Iancului 11→10, Obor 11→9. **DESCOPERIRI care au schimbat cifrele:** (1) conturile-exemplu SUNT marcate `is_demo` și erau deja excluse — creșterea e reală, nu artefact; (2) echipa + prietenii cu cont de test NU sunt `is_demo`, deci intrau în numărători → excluderea se face acum **după EMAIL**, nu după pseudonim, fiindcă „Lucian LM" e scris cu spațiu și scăpase (lista: liviu.fabian, lucianluta, luta.lucian.m + toate aliasurile `+testN`, cotofana.carmen, carmen2000ro, raluca.ivanov26, tiberiu.abc.maxim, livia.dila, plus flagurile `is_super_admin`/`is_admin`); (3) contul Ralucăi are `account_status='deleted'`, deci pica oricum; Livia și al doilea cont de Carmen au pseudonimul gol și 0 zone; (4) **`first_name` e GOL la toți cei 38** → în email numele vine din `pseudonym` (trimmed în SQL: în DB sunt „Iulian ", „Mihai ", „Adrian " cu spațiu la final); (5) interogarea 2b (grupuri cu zona doar ca text, fără rânduri în `grup_preferred_zones`) a ieșit GOALĂ → clasamentul e curat. **DECIZIE DE CONȚINUT, luată după ce datele au arătat problema:** regula „un email per persoană, pentru zona cu cererea maximă" (interogarea 4) dădea **Obor 0 emailuri**, Aviației/Uranus/Iancului câte unul — pentru că aproape toți au bifat și Tineretului și ajungeau în lotul lui; practic o campanie Tineretului cu rămășițe. → S-a ales varianta **multi-zonă** (interogarea 5): un singur email per persoană, dar cu TOATE zonele lui fără grup (primele 3, cu numerele lor), acordul corect în română calculat în SQL („20 **de** oameni" vs „9 oameni"). Rezultat: 38 de destinatari distincți (din 103 poziții în clasament), Obor apare acum în 3 emailuri, Iancului în 5, Zona Centru Nord în 5. **Praguri, ambele reglabile în `5-merge-multizona.sql`:** eligibilitate 9+ oameni (cine primește email), afișare 5+ (ce zone îi arătăm). **DRAFT EMAIL:** `email_templates/email-porneste-grup-zona.md` — mesajul central „un grup nu începe cu un teren, începe cu o conversație", referință la Județului Housing, mențiune explicită că fondatorul aprobă cine intră, onestitate despre durată (ani, nu luni), ZERO procente de economie, linie de opt-out („răspunde cu «stop»" — nu există flag de consimțământ pe `profiles`, doar la newsletter). Are și o variantă scurtă pentru cei 4 cu o singură zonă (Andra, Cristian S, Ion, Rox). **VERIFICAT în cod:** CTA-ul `grup-nou.html` pentru nelogat NU face redirect — afișează „Autentificare necesară" cu modal de login pe aceeași pagină, deci omul rămâne pe formular după conectare. **DE ȘTIUT înainte de trimitere:** cifrele trebuie RE-RULATE în ziua trimiterii (se schimbă săptămânal); „DeathArrow" are 21 de zone fără grup bifate (a bifat jumătate de București) — mesajul „zonele tale" sună fals pentru el, cel mai puțin probabil să pornească un grup; similar, mai moderat, Petru și Silviu cu câte 11. **⏭️ TRIMITEREA — NEÎNCEPUTĂ, PLANUL CONVENIT:** script local Node care citește CSV-ul de la interogarea 5, în trei trepte (dry-run care afișează cele 38 de emailuri compuse → trimitere doar către Lucian → lotul întreg). Broadcast Resend NU se mai poate folosi (fiecare om are lista lui de zone, trebuie merge real per persoană). Cheie API nouă, doar „Sending access", dată ca variabilă de mediu la rulare și ștearsă după. Rate limit Resend 2 cereri/secundă → pauză între trimiteri, ~25s pentru tot lotul; pe plan gratuit sunt 100 emailuri/zi. Antet `List-Unsubscribe` ca `mailto:` (nu e setare de dashboard). **CONFIRMAT de Lucian: `apartamentual@ltfbstudio.ro` primește și trimite mesaje**, deci răspunsurile la „stop" și cele de la oamenii interesați ajung unde trebuie. În Resend nu e nimic de configurat în plus (domeniul e verificat de mult, Audiences/Broadcasts nu ne trebuie).
> **PARTEA 2 — BUG NOTIFICĂRI, găsit în mijlocul analizei și reparat (commit `e9b961c`).** **SIMPTOM (Lucian):** o cerere de alăturare la „Parcul Circului" apărea în Resend și în Slack, dar copia nu ajungea pe `apartamentual@ltfbstudio.ro`; iar emailul primit de George arăta urât. **CAUZA:** `frontend/js/grupuri.js:597` (butonul „Cere alăturare" din **pagina de listare** a grupurilor) trimitea evenimentul vechi `membership_request`, în timp ce **pagina grupului** (`grup-details.html:2420`) folosea deja `join_request`. Același buton, două rezultate, în funcție de unde apăsa omul. Cu evenimentul vechi: subiect englezesc „👋 New Membership Request", corp monospace care expunea `Group ID` și numele evenimentului, fără buton către grup — și, decisiv, `membership_request` NU e în `SUPERADMIN_CC_ALWAYS` (acolo e doar `join_request`), iar frontendul trimite destinatar explicit (`admin_email`), deci lista se oprea la George. **CONFIRMAT cu dovezile lui Lucian din Resend:** 25 iulie (cererea lui CristianH, venită din pagina grupului) = subiect în română + TO cu „+1", ambii destinatari „Delivered", email pe brand cu buton „Vezi cererea în grup"; 27 iulie noaptea (cererea lui Rox, venită din listare) = subiect englezesc, un singur destinatar, iar în corp scria chiar `Event Type: membership_request`. **FIX (1 fișier, `frontend/js/grupuri.js`, 10+/5−):** `event_type` devine `join_request`; `grup_id`/`grup_nume` redenumite `group_id`/`group_name` — **funcția construiește linkul butonului din `data.group_id`, cu numele vechi butonul ar fi dus pe homepage**; scos `status: 'pending'`, nefolosit de formatarea nouă. Verificat cu `node --check`. NEATINSE: inserarea cererii în `grup_membri`, edge function-ul, schema, RLS, plățile. Verificat că e **singurul** apel legacy rămas în tot frontendul (`membership_approved`, `user_signup`, `teren_updated`, `profile_updated` există în edge function dar nu le mai cheamă nimeni). **✅ DEPLOYAT + TESTAT LIVE (28 iulie, Lucian):** emailul a ajuns și la adminul grupului, și pe `apartamentual@ltfbstudio.ro`, cu „+1" vizibil în Resend. **Capcană la testare, de reținut:** la grupurile `is_demo` cardul afișează „Pornește un grup ca acesta" în locul butonului (`grupuri.js:496-499`), deci testul NU se poate face pe un grup exemplu așa cum e. Soluția folosită: scoți temporar comutatorul „Demo" din Admin → Grupuri, ceri alăturarea dintr-un cont de test care nu e membru, apoi pui comutatorul la loc și respingi cererea rămasă `pending`. Butonul mai cere: utilizator logat, `account_type='activ'`, nemembru, fără cerere în așteptare, și loc liber sub `max_membri`. **DESCHISE, marginale:** numele grupului e „Parcul Circului**,**" cu virgulă în câmpul `nume` din DB (apare peste tot unde se afișează grupul, se scoate din admin); cererea lui Rox (rox.brustur@yahoo.com, din 27 iulie noaptea) încă îl așteaptă pe George să o aprobe.**)
> 26 iulie 2026 (**EMAILURI DUPLICATE LA ÎNREGISTRARE + EMAILURI PIERDUTE — 4 probleme, toate rezolvate. Commituri `9a00d07`, `284c6f6`, `1743a56` + docs `85d5fb3`, `b828fd3`, PUSHATE pe `main`. ⏭️ SINGURUL PAS RĂMAS: cPanel → Git Version Control → 1. Update from Remote → 2. Deploy HEAD Commit (publică toate cele 3 commituri de cod deodată), apoi Ctrl+F5. ZERO plăți. SQL rulat deja, edge function deployat deja.** ⚠️ **Detaliile complete sunt în `handoff/handoff-emailuri-duplicate-si-pierdute.md` — citește-l pe ăla, aici e doar rezumatul.** Pornit de la: „petru.birgoveanu s-a înregistrat și am primit 2 mailuri în loc de unul". **PROBLEMA 1 — dublu email la înregistrare (commit `9a00d07`, `frontend/js/profile-edit-new.js`):** butonul „Salvează" din profil nu era blocat cât dura salvarea (~1s, 6 apeluri Supabase) → dublu-click = două salvări în paralel = două notificări `new_user`. Fix: steag `isSaving` + `setSaving()` care dezactivează vizibil butonul, blocarea pornește DUPĂ validare. Efect secundar verificat: a doua salvare rula din nou `delete+insert` pe `user_tags`/`user_preferred_zones` → profilul lui Petru verificat manual, 15 taguri + 16 zone, intact. **PROBLEMA 2 — 18 emailuri pierdute la Resend:** la aprobarea a 4 membri în „Parcul Circului" (26 iul, 08:48–08:50 UTC), 50 încercate / 32 trimise / 18 eșuate, 13 dintre ele într-o fereastră de 93ms → limitare de rată (aceleași adrese reușeau cu milisecunde înainte/după). Cauza: broadcasturile trimit **un apel per membru, fără `await`** → 15 membri = 15 POST-uri simultane la Resend. Fix (commit `284c6f6`, `supabase/functions/notify-admins/index.ts`, **DEPLOYAT deja**): `sendEmailWithRetry` — până la 4 încercări, pauze 0,4/0,8/1,6s **+ până la 300ms aleator** (esențial, altfel toate invocările paralele revin fix în același moment), respectă antetul `retry-after`, reîncearcă doar 429 și 5xx. **PROBLEMA 3 — erorile nu erau diagnosticabile (tot `284c6f6`):** `notification_log` avea doar `status`, fără text. Migrația `supabase/migrations/035_notification_log_error.sql` (coloană `error`) **RULATĂ deja** de Lucian; edge function-ul scrie codul HTTP + mesajul Resend + nr. de încercări; `frontend/admin-notificari.html` afișează motivul sub badge-ul roșu (escapat — textul vine de la un serviciu extern). ⚠️ Dacă se reface undeva: întâi SQL-ul, apoi deploy la funcție. Rândurile dinainte de 26 iulie rămân cu `error = NULL`, informația aia e pierdută definitiv. **PROBLEMA 4 — două aprobări din patru n-au declanșat NIMIC (commit `1743a56`, rezolvată în sesiunea de după):** pentru CristianH și gherasim, în jurnal exista DOAR confirmarea personală — lipseau broadcastul către membri și ambele apeluri către superadmin, **zero rânduri, nici măcar cu status „Eroare"**. Lucian a primit 1 email de superadmin din 4. **CAUZA:** `notifyAdmins` (`frontend/js/nav.js`) lansează o cerere de rețea **fără `await`**, iar aprobarea făcea `setTimeout(reload, 1000)`. Reîncărcarea **anulează** cererile care n-au apucat să iasă → nu ajung la edge function → edge function-ul nu rulează → **nu scrie nimic în jurnal** (jurnalul e scris de funcția care n-a fost niciodată apelată). De ce exact ei doi: confirmarea personală pleacă imediat după update, dar broadcastul + apelurile către superadmin vin după încă DOUĂ interogări Supabase; aprobând 4 oameni în ~o secundă, reload-ul programat de PRIMA aprobare a căzut peste pașii 2–3 ai ultimelor două. **Departajat de ipoteza „s-au făcut, dar nu s-au jurnalizat":** pasul care notifică superadminul e ÎN AFARA oricărui try/catch (nicio eroare JS nu-l poate sări — doar plecarea de pe pagină); `grup-details.html` e SINGURA pagină care emite `member_approved`/`member_joined`; și dacă apelurile s-ar fi făcut, Lucian ar fi primit 4 emailuri. Precedent în propriul cod: pe 24 iulie, la linkul WhatsApp, se scrisese deja explicit „`Promise.all` AȘTEPTAT ca reload-ul de la 1s să nu taie cererile în zbor" — același bug, reparat atunci într-un singur loc. **FIX (2 fișiere, ZERO DB/RLS/plăți/edge, nicio interogare Supabase modificată):** `frontend/js/nav.js` — contor global de operațiuni în zbor (`notifyAdmins` îl incrementează SINCRON la pornire, deci prinde și apelurile fără `await`) + `reloadWhenIdle()`/`navigateWhenIdle()` care amână plecarea de pe pagină până nu mai e nimic în zbor, cu plasă de siguranță de 8s ca pagina să nu rămână blocată pe date vechi; `frontend/grup-details.html` — toate cele 13 reîncărcări/navigări (aprobare, respingere, alăturare, invitație, plecare din grup, transfer admin, voturi) trec prin `reloadPage()`/`navigatePage()`, deci nicio acțiune nu mai taie emailurile alteia; în aprobare notificările se adună într-o listă și se AȘTEAPTĂ înainte de reîncărcare; aprobarea e marcată `beginOp()`/`endOp()` pe toată durata ei. Sintaxa verificată cu Node pe ambele fișiere. **TEST după deploy:** pe un grup de test cu 2–3 cereri, aprobă-le rapid una după alta → în admin → Notificări trebuie să apară fluxul COMPLET pentru fiecare (confirmare personală + broadcast + 2 rânduri către superadmin), nu doar confirmarea personală la ultimele din rafală. **LIMITĂ RĂMASĂ, onestă:** dacă tabul e închis manual imediat după aprobare, emailurile tot se pot pierde — nimic client-side nu repară asta complet; reparația durabilă e difuzarea pe server. **⏭️ DESCHISE:** (1) **trimitere în lot la Resend** (`/emails/batch`) — rădăcina problemei 2 și jumătate din 4, un singur apel cu lista de destinatari în loc de 15 apeluri; merită sesiune dedicată; (2) 4 avertismente de tipare preexistente în `notify-admins/index.ts` (`error.message` unde `error` e `unknown`) — `deno check` le semnalează, funcția se deployează normal; (3) emailurile neretrimise de la 10:48 (10 broadcasturi + 5× „Grup actualizat") — se pot retrimite manual invocând `notify-admins` cu `payload` din `notification_log`. **Cod COMPLET, rămâne DOAR deploy-ul cPanel + testul.**)
> 24 iulie 2026 (**PIERDEREA UTILIZATORILOR NOI DUPĂ CONFIRMAREA EMAILULUI — cauză găsită și reparată. ⏭️ DE DEPLOYAT: 2× cPanel (frontend). ZERO DB/RLS/plăți.** Pornit de la o cifră a lui Lucian: **43 ating formularul de înregistrare, 32 ajung la editarea profilului, 11 se opresc — aceiași 11 cu profil gol.** Ipoteza lui: după confirmarea emailului ajung pe homepage și nu înțeleg că mai au un pas. **CONFIRMAT cu SQL** (rulat de Lucian în SQL Editor, doar SELECT, join `auth.users` × `profiles` pe `email_confirmed_at` + `pseudonym`): cei 11 sunt în **stadiul 2 = „a confirmat emailul, dar profilul a rămas GOL"** → deci NU e problemă de livrare/spam, se pierd la ATERIZARE. **VERIFICAT ȘI ELIMINAT ca ipoteze:** (a) `frontend/js/register.js:452-454` setează CORECT `emailRedirectTo = origin + '/profile-edit-new.html?welcome=1'` (sau linkul invitației, dacă vii din `?redirect=`) — intenția codului e bună; (b) funcția veche `signUp()` din `js/app.js`, care NU trimite `emailRedirectTo` (deci ar fi dus pe Site URL = homepage), e **COD MORT** — niciun `onclick`/handler din HTML n-o apelează (register.html folosește `#auth-form` → register.js). **CAUZA REALĂ — `frontend/js/profile-edit-new.js:23-27`:** pagina pe care aterizează omul din email făcea `getUser()` imediat și, dacă nu găsea sesiune, `window.location.href = '/index.html'` **MUT** — fără mesaj, fără login, fără nimic. Orice hopa (token consumat de scanerele de email care preîncarcă linkurile, link expirat, al doilea click, `redirect_to` nepermis) devenea exact simptomul observat: om aterizat pe homepage, convins că a terminat. Contrast intern: `reset-parola.html` (făcut pe 23 iulie) trata deja corect aceeași situație (ascultă evenimentul de auth + plasă 1.5s + stare explicită „link invalid sau expirat"); pagina de profil nu avea nimic din toate astea. **FIX 1 — `frontend/js/profile-edit-new.js`:** funcție nouă `resolveUserOnArrival()` = `getUser()` → dacă null, `getSession()` (sesiunea LOCALĂ, ca un semnal prost de rețea să nu ceară degeaba autentificare cuiva deja logat) → dacă tot null ȘI URL-ul are `?welcome=1` sau „bagaj" Supabase, ascultă `onAuthStateChange` max **2s** cu plasă de siguranță pe `getSession()`. `getAuthErrorFromUrl()` citește `#error=`/`error_code=` (ex. `otp_expired`) și nu mai așteaptă degeaba când linkul e deja mort. `renderArrivalProblem()` ascunde conținutul paginii și afișează, în locul redirectului: **„Linkul de confirmare nu a mai funcționat"** (explică inclusiv că unele servicii de email deschid automat linkurile înaintea omului) + câmp email + buton **„Trimite-mi din nou emailul"** (`auth.resend({type:'signup'})` cu redirect tot spre pagina de profil, mesaj GENERIC anti-enumerare, ca la resetarea parolei) + buton „Am confirmat deja — autentifică-mă" (`window.openLoginModal()`, fallback `/index.html?login=1`). Pentru vizitatorul nelogat care ajunge pe pagină FĂRĂ link de email (sesiune expirată, alt browser, link vechi salvat): ecran „Trebuie să fii autentificat" + buton login, tot în loc de redirect mut. **Utilizatorii logați nu văd niciodată aceste ecrane** — garda găsește sesiunea și încarcă formularul ca înainte. Adăugat și eveniment Plausible (`Confirmare link esuat` / `Profil fara sesiune`) ca să se vadă în cifre dacă drumul mai e lovit după deploy (inofensiv dacă nu e configurat ca goal). **FIX 2 — `frontend/profile-edit-new.html` (blocul inline „welcome"), defect SEPARAT găsit pe același drum:** făcea `getUser()` după **100ms** (comentariu în cod: „sb session is ready almost immediately" — presupunere care cade exact la aterizarea din email) și, dacă nu găsea sesiune, ieșea în tăcere → **`account_type` nu se mai sincroniza din metadata în `profiles`, iar la agenții se pierdea `account_status = 'pending'`**. Acum folosește aceeași `resolveUserOnArrival()`. Notificarea `new_user` către superadmin NU era afectată (pleacă la salvarea profilului, nu aici). **⏭️ DE VERIFICAT DE LUCIAN ÎN SUPABASE (nu se poate din repo):** (1) Authentication → Emails → template „Confirm signup" trebuie să folosească `{{ .ConfirmationURL }}` — dacă cineva a pus link pe `{{ .SiteURL }}`, TOȚI ajung pe homepage, nu doar 11; ⚠️ fișierul din repo `email_templates/sign_up.html` e VECHI și în afara brandului (albastru `#2563eb`, „Apartamentual" fără TU) — de refăcut cândva pe brand, ca `reset_password.html`; (2) URL Configuration → Redirect URLs: `https://apartamentual.ro/**` — **și varianta `www.` dacă site-ul răspunde și acolo**, fiindcă `emailRedirectTo` se construiește din `window.location.origin`; (3) durata de expirare a linkului de confirmare. **IPOTEZĂ RĂMASĂ pentru DE CE ajung linkurile consumate:** scanere/filtre de email care preîncarcă linkul de unică folosință; dacă după deploy mulți ajung pe ecranul „linkul nu a mai funcționat", soluția reală e trecerea pe **cod OTP** în loc de link de click (discuție separată). **⏭️ DE DEPLOYAT din cPanel:** `frontend/js/profile-edit-new.js` + `frontend/profile-edit-new.html`. **TEST după deploy (2 min):** deschide `/profile-edit-new.html?welcome=1` **nelogat** → trebuie să apară ecranul explicativ, NU homepage; apoi **logat** → formularul normal cu bannerul de bun venit. Fluxul real de confirmare NU s-a putut testa local (cere cont nou + emailul Supabase). **Cod COMPLET, rămâne deploy-ul + testul.**)
> 24 iulie 2026 (**NOTIFICARE EMAIL CĂTRE MEMBRI CÂND ADMINUL SALVEAZĂ LINKUL DE WHATSAPP AL GRUPULUI — commit `f463f15`, PUSHAT pe `main`. ⏭️ DE DEPLOYAT: 1× edge function + 2× cPanel (frontend).** Cerere Lucian: membrii unui grup să afle pe email când adminul pune/schimbă linkul conversației de WhatsApp (până acum linkul apărea doar ca buton pe pagina grupului, nimeni nu era anunțat). ZERO migrații DB, ZERO RLS, ZERO atingeri în zona de plăți — folosește coloana EXISTENTĂ `grupuri.link_whatsapp`. **(1) EDGE FUNCTION `supabase/functions/notify-admins/index.ts`** (edge de NOTIFICĂRI, nu plăți): eveniment nou `whatsapp_link_shared` cu formatter propriu pe `buildEmailHtml` (brand crem/teracotă). Titlu + text DIFERITE după flag-ul `is_update`: link pus prima dată („💬 Grupul „X" are acum o conversație pe WhatsApp") vs. link schimbat („💬 Link WhatsApp actualizat — grupul „X"" + avertisment că linkul vechi poate să nu mai meargă). CTA „Intră în grupul de WhatsApp" + linkul scris și în clar în footer (unii clienți de email strică butoanele) + notă că linkul e doar pentru membri. Gardă de siguranță: doar linkurile `^https?://` ajung ca `href` (un `javascript:` scris din greșeală în câmpul din admin cade pe linkul paginii de grup). Flag `also_updated` → adaugă o notă în email că în aceeași salvare s-au schimbat și alte detalii ale grupului. Adăugat în `SUPERADMIN_CC_IF_NO_RECIPIENT` → frontendul face N apeluri cu `admin_email` (câte unul per membru) + 1 apel dedicat FĂRĂ `admin_email`, deci Lucian primește O SINGURĂ copie, nu N+1. Restul evenimentelor NEATINSE. **(2) `frontend/grup-details.html`** (secțiunea Administrare → „Link WhatsApp" → Salvează): `saveWhatsApp()` reține `previousLink` din `currentGroup.link_whatsapp` ÎNAINTE de update și notifică DOAR dacă `link && link !== previousLink` — ștergerea linkului sau re-salvarea aceleiași valori NU trimit niciun email. Helper nou `notifyMembersWhatsAppLink()`: folosește array-ul global `members` (conține deja doar membri `activ`, cu profilul atașat și fără conturile soft-deleted), exclude adminul care a salvat, trimite cu `Promise.all` AȘTEPTAT ca reload-ul de la 1s să nu taie cererile în zbor; numele adminului = `pseudonym` din `currentUserProfile`, fallback pe partea din email dinaintea lui @. **(3) `frontend/grup-edit.html`** (formularul de editare grup — AL DOILEA loc de unde se salvează linkul, ușor de ratat): un SINGUR email per salvare, prin `if/else` — dacă linkul e nou/schimbat pleacă DOAR `whatsapp_link_shared` (cu `also_updated` calculat comparând nume/descriere/oraș/max_membri/status cu `currentGroup`), altfel rămâne `group_updated` exact ca înainte. ⚠️ Prima variantă trimitea AMBELE emailuri; Lucian a cerut ajustarea ÎNAINTE de commit — deci comportamentul final e „un email, nu două". **PREVIEW ÎNAINTE DE COMMIT:** generat local cu Node, rulând codul REAL din `index.ts` (script în scratchpad: citește fișierul, taie `logNotification`+`serve` și sintaxa TS, evaluează formatterul în `vm` → HTML cu cele 3 variante). Deno NU e instalat pe mașină (Node v24 da) — de reținut pentru viitoare preview-uri de emailuri. Aprobat de Lucian („arată bine"). **LIMITĂ CUNOSCUTĂ (inofensivă):** în `grup-edit.html` blocul de notificare e imbricat în `if (memberIds.length > 0)`, deci dacă adminul e singurul membru nu pleacă nici emailul dedicat superadminului; din `grup-details.html` pleacă mereu. **⏭️ DE DEPLOYAT:** (a) copiază `supabase/functions/notify-admins/index.ts` în `C:\Users\lucia\supabase\supabase\functions\notify-admins\index.ts`, apoi din `C:\Users\lucia\supabase`: `npx supabase functions deploy notify-admins` (fără Docker); (b) cPanel `frontend/grup-details.html`; (c) cPanel `frontend/grup-edit.html`. ⚠️ edge function + frontend merg ÎMPREUNĂ — fără edge, emailul cade pe formatul generic „🔔 Site Event". **TEST LIVE (pe un grup cu UN singur alt membru, ca să nu spamezi):** pagina grupului → Administrare → lipește link → Salvează → membrul primește emailul, Lucian primește copia de superadmin; re-salvezi aceeași valoare → NICIUN email; golești câmpul → NICIUN email. **⏭️ DESCHISE (neatinse, sesiuni separate):** email către George (plafon 15 + link WhatsApp); email către Alin (Rond Coșbuc, flux resetare parolă); pagina secundară `grup-detail.js:598` (status vechi `'approved'`); opțional trigger DB anti-race la aprobare. **Cod COMPLET, rămâne deploy-ul + testul live.**)
> 23 iulie 2026 (**FIX FILTRU „ORAȘ" pe pagina Utilizatori — commit `38aa518`, PUSHAT pe `main`. ⏭️ DE DEPLOYAT din cPanel (`frontend/js/utilizatori.js`) + DE TESTAT LIVE de Lucian.** Sursă: handoff-ul `handoff/handoff-fix-filtru-oras-utilizatori.md` (task cu faze + STOP-uri). ZERO atingeri DB/interogare/RLS/plăți — pur JS pe filtru. **SIMPTOM:** București + Tineretului afișa 5 utilizatori în loc de 20 (17 reali + 3 exemple); zeci de profiluri reale ascunse la filtrarea după oraș. **CAUZĂ (confirmată în audit, `utilizatori.js` `applyFilters` ~linia 299):** filtrul compara numele ZONEI cu numele ORAȘULUI prin `includes()` → treceau doar userii cu o zonă al cărei nume conținea literal orașul (ex. „București Noi"); cineva cu doar „Tineretului"/„Carol"/„Tei" era eliminat. **AUDIT (Faza 1):** `ORASE_CARTIERE` (orase-cartiere.js) = `{ „București": [nume zone...], „Cluj-Napoca": [...] }` (chei orașe = valoarea din dropdown, valori = liste de nume de zone identice cu tabelul `zones`); `user.zones` = `[{id,name}]` FĂRĂ `city_id`; `preferred_city_id` e adus în `loadUsers` dar NEfolosit și fără mapare oraș→id în client. **DESCOPERIRE nouă (nu era în handoff):** coliziune de nume — „Centru" există în 3 orașe (Cluj/Timișoara/+1), deci o soluție pe NUME poate da fals-pozitive ÎNTRE orașele secundare; București NU e afectat (are „Centrul Vechi"/„Zona Centru Nord", nu „Centru"), deci cazul raportat e 100% rezolvat. **DECIZIE Lucian: Soluția A** (filtrare după apartenența zonei la oraș via `ORASE_CARTIERE[oras]`, fără atingeri DB) + userii FĂRĂ nicio zonă rămân excluși de filtru (ca înainte). **IMPLEMENTAT (Faza 3):** doar blocul de oraș din `applyFilters` — construiește `new Set(ORASE_CARTIERE[filters.oras])` și păstrează userii cu `user.zones.some(z => set.has(z.name))`; fallback pe set gol dacă orașul lipsește. Nimic altceva atins (filtrul de zonă/interese, sortarea, `loadUsers` — neatinse). **TESTE (Faza 4) — logică verificată de mine, CONTORII de confirmat live de Lucian ca NELOGAT:** (1) București+Tineretului = 20 nu 5; (2) București fără zonă = toți din București (~50); (3) fără filtru = neschimbat; (4) Cluj = doar Cluj, fără București (⚠️ dar un user Timișoara/Iași cu „Centru" poate apărea greșit la Cluj — coliziunea acceptată); (5) Resetează = listă completă; (6) fără erori JS. **⏭️ DE FĂCUT:** deploy cPanel `frontend/js/utilizatori.js` + testul de mai sus. ⚠️ handoff-ul zicea „auto-deploy Render” la Faza 5 — GREȘIT, la noi e cPanel MANUAL, nu se deployează singur. **DESCHIS (marginal):** coliziunea „Centru" pe orașe secundare → eventual Soluția B (adaugă `city_id` în select + mapare oraș→id) dacă devine relevant. **Cod COMPLET, rămâne deploy + test.**)
> 23 iulie 2026 (**FLUX „AI UITAT PAROLA?" — COMPLET, DEPLOYAT (cPanel) + TESTAT LIVE de Lucian („merge bine"). Commit `f537d26`, PUSHAT pe `main`. Închide golul descoperit în sesiunea de suport Rond Coșbuc (platforma nu avea deloc flux de resetare).** ZERO atingeri plăți/DB/RLS/edge functions. **3 fișiere:** **(1) `frontend/reset-parola.html` (NOU)** — pagină self-contained pe brand (crem/teracotă), refolosește `js/supabase-config.js` + SDK `@supabase/supabase-js@2.49.4`. Preia sesiunea de recovery din URL (flux IMPLICIT — token în hash; config-ul nu setează `flowType`, deci `detectSessionInUrl` consumă automat linkul). Ascultă evenimentul `PASSWORD_RECOVERY` (+ plasă de siguranță pe `getSession()` după 1.5s, folosind flag-ul sincron `window.__hadRecovery` capturat înainte ca SDK-ul să curețe hash-ul). 4 stări: „se verifică linkul" → formular parolă nouă (min. 8 + confirmare) → `updateUser({ password })` → succes (redirect la `/`) / „link invalid sau expirat". **(2) `frontend/js/login-modal.js`** — link „Ai uitat parola?" sub butonul de login → `handleForgotPassword` cheamă `sb.auth.resetPasswordForEmail(email, { redirectTo: window.location.origin + '/reset-parola.html' })`; mesaj generic de succes (anti-enumerare — nu dezvăluie dacă emailul există). `handleLogin` NEATINS. **(3) `email_templates/reset_password.html`** — rescris pe brand (structură identică cu `buildEmailHtml`: header apartamenTUal cu TU teracotă + „by LTFB studio", buton ink `#1a1a1a`, footer), text corectat („link de unică folosință, ~o oră" în loc de vechiul „24 de ore" greșit); `{{ .ConfirmationURL }}` păstrat (buton + link de rezervă). **PAȘI MANUALI SUPABASE (FĂCUȚI de Lucian):** Authentication → Emails → template „Reset Password": Subject + Message body înlocuit cu conținutul fișierului (SALVAT); Authentication → URL Configuration: Redirect URLs conține `https://apartamentual.ro/**` (CONFIRMAT de Lucian) → linkul din email redirecționează corect spre reset-parola.html. Expirarea linkului (pas C, opțional) NEschimbată — default ~1h, se potrivește cu textul; setarea e greu de găsit în UI-ul nou (Authentication → Providers → Email), dar nu e necesară. **TEST LIVE OK.** **⏭️ FOLLOW-UP:** email către Alin (Rond Coșbuc) că s-a rezolvat + cum se folosește noul flux — DRAFT gata în chat, de trimis manual (sau ca draft Gmail). **DESCHISE (neatinse, sesiuni separate):** email George (plafon 15 + link WhatsApp); pagina secundară `grup-detail.js:598` (status vechi `'approved'`); opțional trigger DB anti-race la aprobare. **Task COMPLET.**)
> 23 iulie 2026 (**SESIUNE GRUPURI — plafon membri: 3 lucruri, commituri `a746534` + `f2faa5b`, PUSHATE pe `main`. ⏭️ DE DEPLOYAT: 2× cPanel (frontend) + 1× edge function.** Pornit de la o observație a lui Lucian: grupul REAL `Parcul Circului` (admin George) avea **11 membri deși max era 8** — și pe pagină scria „Grupul este plin", fără buton „Cere alăturarea". **(1) DIAGNOSTIC:** afișarea „plin" e CORECTĂ (11 > 8 → `hasRoom=false`, grup-details.html:1709); bug-ul real e că `approveRequest()` (grup-details.html:~2415) **nu verifica deloc `max_membri`** la aprobare → adminul putea aproba oricâte cereri. Mecanism: gardul de la „Cere alăturarea" numără doar membrii ACTIVI, nu și `pending`, deci mai mulți oameni au trimis cereri când era „loc pentru 1”, iar George le-a aprobat pe rând fără nicio limită → 11. Asimetrie confirmată: acceptarea unei INVITAȚII (`acceptInvitationBanner`, linia 3342) DA verifica plafonul; aprobarea unei CERERI nu. **(2) FIX IMEDIAT plafon Parcul Circului:** Lucian a rulat manual în Supabase SQL Editor `update grupuri set max_membri = 15 where nume ilike 'Parcul Circului%'` (SUCCESS). Notă: SQL Editor ocolește RLS, nu spune nimic despre ce poate browserul. **(3) FIX editare grup de superadmin — commit `a746534`:** Lucian (superadmin) nu putea edita grupul din admin panel — `grup-edit.html:536` bloca pe oricine nu e creatorul (`admin_id !== user.id → „Doar adminul poate edita”`), fără excepție pt. superadmin. **VERIFICAT RLS (screenshots `screencapture_1784835734529/737436`):** pe `grupuri` există DEJA politica UPDATE `Super admins can update any group` (via `is_super_admin` din profiles), iar `grup_preferred_zones`/`grup_tags` au DELETE/INSERT pe `auth.uid() IS NOT NULL` → **DB permite deja tot; singurul blocaj era JS.** Fix (3 modificări în `grup-edit.html`, ZERO DB/RLS/plăți): variabilă `isSuperAdmin`, citit `profiles.is_super_admin` la auth, gardul devine `admin_id !== user.id && !isSuperAdmin`. `handleSubmit` NEatins. **(4) FIX aprobare peste plafon + email „grup plin” — commit `f2faa5b` (2 fișiere):** în `grup-details.html approveRequest` — gardă: numără activi înainte de aprobare, dacă `>= max_membri` blochează cu toast + repune cererea; iar dacă aprobarea duce grupul EXACT la plafon, trimite `notifyAdmins('group_full', {..., admin_email})`. În `supabase/functions/notify-admins/index.ts` (edge de NOTIFICĂRI, nu plăți): caz nou `group_full` (email teracotă) + adăugat în `SUPERADMIN_CC_ALWAYS` → Lucian primește mereu CC. **Risc rezidual onest:** race la 2 admini care aprobă simultan cereri diferite poate depăși cu 1 (verificare ne-atomică); cazul real (aprobări pe rând) e prins 100%; blindaj total = trigger DB, opțional, separat. **NEATINS restul notify-admins, calea de invitații (avea deja gardă la 3342), și pagina SECUNDARĂ `grup-detail.js:598` care folosește status VECHI `'approved'` (nu `'activ'`) — pare legacy, de verificat separat dacă mai e folosită înainte de a-i pune aceeași gardă.** **⏭️ DE DEPLOYAT:** (a) cPanel `frontend/grup-edit.html`; (b) cPanel `frontend/grup-details.html`; (c) edge function — copiază `supabase/functions/notify-admins/index.ts` în `C:\Users\lucia\supabase\supabase\functions\notify-admins\index.ts`, apoi din `C:\Users\lucia\supabase`: `npx supabase functions deploy notify-admins` (fără Docker). ⚠️ grup-details.html + edge function merg ÎMPREUNĂ (fără edge, emailul `group_full` cade pe format generic). **TEST:** pe grup test cu max mic → aprobă până la limită → următoarea aprobare blocată; la atingerea plafonului → Lucian + adminul primesc emailul „👥 grup plin”. **⏭️ DESCHISE:** emailul pentru George (plafon 15 + opțiune link WhatsApp — gata scris, așteaptă adresa lui); verificat `grup-detail.js` secundar; opțional trigger DB anti-race. **Toate task-urile de cod COMPLETE, rămâne deploy-ul.**)
> 23 iulie 2026 (**SUPORT: utilizator real (Rond Coșbuc) a uitat parola — rezolvat pe loc cu MAGIC LINK; descoperit GOL real: platforma NU are flux „Ai uitat parola?". ZERO cod atins, ZERO commit, NIMIC de deployat.** Context: un membru real (grupul „Rond Coșbuc", admin Alint) și-a făcut contul de pe telefon, a uitat parola, nu poate intra de pe PC, nu vrea alt cont (ar pierde grupul). **Diagnostic în cod:** fereastra de login (`frontend/js/login-modal.js`) are DOAR email+parolă — niciun link „Ai uitat parola?". Există un template de email `email_templates/reset_password.html` (folosește `{{ .ConfirmationURL }}`), DAR fluxul e neconectat: NU există nicio pagină pe site care să primească linkul de recovery și să lase omul să-și tasteze parola nouă (grep după `type=recovery`/`PASSWORD_RECOVERY`/`updateUser`/`resetPasswordForEmail` = 0 în frontend). Deci nu e o parolă „pierdută" recuperabilă printr-un buton existent — butonul lipsește. Clientul Supabase (`frontend/js/supabase-config.js`) e standard, `detectSessionInUrl` activ implicit → orice pagină care încarcă config-ul consumă automat un magic/recovery link. **REZOLVARE IMEDIATĂ (fără cod):** Lucian a trimis din Supabase → Authentication → Users → contul ei → **Send magic link** (a apărut „magic link sent" = email plecat pe adresa contului). Ea deschide linkul PE PC → aterizează pe apartamentual.ro → se loghează automat, cont + grup intacte. Reamintit: să caute în Spam/Promoții; linkul e unică-folosință + expiră (~1h), dacă e cazul reapeși „Send magic link"; să-l deschidă pe PC, nu pe telefon. **VERIFICAT config (screenshot `screenshots/screencapture_1784833273646.png`):** Authentication → URL Configuration → **Site URL = `https://apartamentual.ro`** (CORECT) + Redirect URLs conține `https://apartamentual.ro/**` (CORECT) → magic link-ul VA funcționa. **Minor (necesită curățare cândva, inofensiv acum):** în Redirect URLs mai sunt 4 adrese vechi `https://apartamentual.onrender.com/...` (reziduuri de la vechiul deploy Render; deploy-ul real e cPanel). Nu afectează magic link-ul. **⏭️ DECIZIE DESCHISĂ / OFERTĂ NEPORNITĂ — fluxul „Ai uitat parola?" (recomandat, afectează TOȚI userii):** propus lui Lucian, așteaptă OK explicit înainte de orice cod (zonă auth). Plan: (1) link „Ai uitat parola?" în `login-modal.js` → `supabase.auth.resetPasswordForEmail(email, { redirectTo: '<site>/reset-parola.html' })`; (2) pagină nouă `frontend/reset-parola.html` care preia tokenul de recovery și cheamă `supabase.auth.updateUser({ password })`; (3) conectat template-ul deja existent `reset_password.html` în Supabase → Authentication → Email Templates (pas manual Lucian). **NIMIC de deployat din această sesiune. Task suport COMPLET; fluxul de resetare = de decis separat.**)
> 23 iulie 2026 (**FIX ADMIN PE MOBIL: tabelele nu mai sunt tăiate — commit `022212b`, PUSHAT pe `main`, ⏭️ DE DEPLOYAT din cPanel (frontend). ZERO atingeri plăți/DB/RLS/edge fn/JavaScript — pur CSS/clase Tailwind.** Simptom raportat de Lucian (2 poze în `screenshots/admin mobil/`): pe telefon, paginile de admin apăreau tăiate — la Utilizatori se vedeau doar coloanele Nume + Email, iar Tip cont/Oraș/Înregistrat/**Acțiuni** (butoanele!) erau inaccesibile; la header, „← Admin panel" se suprapunea peste subtitlu. **Cauză:** cardul tabelului avea `overflow-hidden`, care taie tot ce iese din card. Pe desktop tabelul (6–8 coloane) încape; pe ecran ~360px nu se poate strânge sub lățimea minimă a conținutului → iese din card și `overflow-hidden` taie coloanele din dreapta, FĂRĂ scroll posibil. **Fix (7 fișiere frontend, doar clase/CSS):** (1) în cele 6 pagini admin cu tabel (`admin-terenuri`, `admin-utilizatori`, `admin-grupuri`, `admin-newsletter`, `admin-notificari`, `admin-parteneri`): `overflow-hidden` → `overflow-x-auto` pe card + `min-w-[720–1000px]` pe `<table>` (dimensionat după nr. de coloane al fiecărei pagini) → pe mobil tragi tabelul lateral cu degetul, ajungi la toate coloanele; pe desktop arată identic. (2) Header-ul paginii: `flex items-center justify-between` → adăugat `flex-wrap` + `gap-3`, ca „← Admin panel" să treacă pe rândul următor în loc să se suprapună. (3) Padding lateral `px-6` → `px-4 sm:px-6` (recuperezi ~16px pe 360px) + `pb-10` sub tabel (ultimul rând nu mai stă lipit de bara telefonului). (4) `admin.html` (dashboard): în `@media(max-width:768px)` adăugat `.table-card{overflow-x:auto}` + `.admin-table{min-width:560px}` pentru tabelele Top zone/Top terenuri/Articole. **NEATINSE:** nicio interogare Supabase, niciun `onclick`, nicio logică de aprobare/ștergere. **Alternativă NEALEASĂ (propusă pt. viitor):** „carduri în loc de tabel" pe mobil (fiecare rând = card vertical) — mai frumos dar cere rescris HTML-ul generat de JS în 6 pagini (risc mai mare); scroll orizontal rezolvă acum cu risc ~zero. Dacă se vrea, de făcut separat, întâi pe o singură pagină (utilizatori). **⏭️ DE DEPLOYAT din cPanel:** `frontend/admin.html`, `admin-grupuri.html`, `admin-newsletter.html`, `admin-notificari.html`, `admin-parteneri.html`, `admin-terenuri.html`, `admin-utilizatori.html`. După deploy + Ctrl+F5 (cache!): pe telefon la Utilizatori tragi tabelul lateral și ajungi la coloana Acțiuni. **Task COMPLET.**)
> 21–22 iulie 2026 (**SESIUNE GRUPURI: 5 fix-uri UI + curățare grupuri exemplu în DB + analiză platformă + materiale pentru cele 2 grupuri reale.** Toate commiturile PUSHATE pe `main`, ⏭️ DE DEPLOYAT din cPanel (frontend). ZERO atingeri plăți/RLS/edge fn — pur UI/conținut + o ștergere de date făcută manual de Lucian. **CINCI COMMITURI DE COD:** **(1) `4eda696`** — link profil pe cardurile de membri/cereri din `frontend/js/grup-detail.js` (`createMemberItem`): numele devine link către `/profile-view-new.html?id=<user_id>` dacă profilul e valid. ⚠️ ATENȚIE: `grup-detail.js` deservește pagina `grup-detail.html` (accesată DOAR după crearea unui grup + din editarea terenurilor), NU pagina principală. **(2) `a8e4cd6`** — `frontend/admin.html`: bara laterală de admin nu se mai ascunde pe mobil (regula `@media(max-width:768px){.admin-sidebar{display:none}}` ascundea complet navigația către secțiuni pe telefon → superadminul nu ajungea la nimic). Acum bara trece sus ca meniu orizontal care se rupe pe rânduri. Doar CSS. **(3) `c7a8ddf`** — `frontend/js/utilizatori.js`: lista de utilizatori nu mai amestecă demo cu reali. Sortare nouă: (a) reali întâi, `is_demo` mereu la sfârșit; (b) logat → reali după zonele comune cu tine, apoi interese comune; (c) nelogat/egalitate → ordinea înscrierii (`created_at` crescător). Scos vechiul scor de completitudine (folosea câmpuri neexistente în query); adăugate `zoneMatchCount`/`tagMatchCount`. Query Supabase neatins. **(4) `6c967f5`** — `frontend/ghid.html` + `frontend/grup-details.html`: ghidul (secțiunea Grupuri) corectat + completat + linkat din pagina grupului. Corecturi: scos statusul „Deschis" (toate grupurile sunt cu aprobare); „Alăturarea" rescrisă pe fluxul de aprobare; adăugată secțiunea „Comunicarea și colaborarea în grup" (anunțuri, WhatsApp, comentarii pe terenuri, checklist); corectat că checklistul e editabil de TOȚI membrii, nu doar admin (2 locuri). În `grup-details.html` adăugat un link discret „Ce poți face într-un grup? Vezi ghidul" → `ghid.html#grupuri`. **(5) `ec047c5`** — FIX-UL REAL al bug-ului raportat: pe pagina PRINCIPALĂ `grup-details.html` (cea din lista de grupuri, `?id=`), cardul cererii de înscriere afișat adminului (`renderPendingRequests`, linia ~3186) avea numele ca text simplu. Acum e link către `profile-view-new.html?id=<user_id>`. NOTĂ IMPORTANTĂ: primul fix (`4eda696`) fusese pe pagina secundară `grup-detail.html`; bug-ul pe care l-a văzut Lucian era pe `grup-details.html` (cu „s"). Cardurile de membri APROBAȚI de pe grup-details.html erau DEJA clicabile (linia 1773, înfășurate în `<a>`). Există DOUĂ pagini de detaliu grup: `grup-detail.html` (js separat, accesată din creare/editare terenuri) și `grup-details.html` (self-contained, accesată din lista publică `grupuri.js`). **CURĂȚARE GRUPURI EXEMPLU ÎN DB (făcută de Lucian, NIMIC de rulat mai departe):** erau 6 grupuri exemplu (`is_demo=true`), prea multe. Verificat că toate cele 13 tabele-copil au FK `ON DELETE CASCADE` (deci ștergerea grupului curăță automat membri/anunțuri/checklist/etc.). ȘTERSE 3: `Familii Nord Bucuresti` (54f236d7), `Design & Eficiență – Centru București` (9c5a4567), `Cod & Cărămidă - Grupul IT-iștilor` (6ccddc2a, era grupul de test al lui Lucian). PĂSTRATE 3 exemple curate: `Comunitate Verde Băneasa`, `Investiție Inteligentă – Bloc Boutique Central`, `Bloc Eco pentru Medici și Profesioniști`. Profilurile utilizatorilor NEATINSE (cascade doar pe `grup_membri` etc.). Capcană descoperită: `grupuri` NU are coloana `is_disabled` (schema reală ≠ README_GROUPS.md care descrie tabele vechi `grup`/`grup_membership`); lista publică ascunde grupuri prin `status='arhivat'` (grupuri.js linia 174) DAR există un CHECK constraint `grupuri_status_check` care NU permite valoarea `arhivat` → arhivarea din UI e imposibilă (mică inconsistență, NEreparată). De aceea am șters, nu arhivat. **CELE 2 GRUPURI REALE (context viu):** `Parcul Circului` (admin George, 7/8 membri, zone Floreasca+Tei, FĂRĂ teren încă, caută teren lângă parcul Circului) și `Rond Cosbuc – Parc Carol` (admin Alint, 4/5, zone Unirii/Plevnei/Cotroceni/Carol, ARE deja 1 teren favorit). Mihai și Silviu sunt în AMBELE. **MATERIALE LIVRATE (text în chat, NEcomise — Lucian le folosește manual):** (a) 2 emailuri gata de trimis către George și Alint (WhatsApp + șablon regulament + pe cine să invite din analiza de compatibilitate + pentru George: ajutor pe teren via agenții imobiliare); (b) șablon de „regulament intern de grup" (cum comunicăm/decidem/bani/implicare) de pus ca prim anunț; (c) analiză de compatibilitate a utilizatorilor reali (ex: `Alexandra M.` = potrivire nr.1 pentru Rond Cosbuc, are chiar același teren la favorite). Reamintit: funcția „Link WhatsApp" EXISTĂ deja în pagina grupului (admin o completează → membrii văd buton „Grup WhatsApp"). **MEMORIE:** actualizat `conturi-test-carmen-tibs` — Carmen, Tibs, Raluca = conturi interne/test (nu prospecti); Fabian (user) = Liviu Fabian, asociatul. **⏭️ DE DEPLOYAT din cPanel:** `frontend/js/grup-detail.js`, `frontend/admin.html`, `frontend/js/utilizatori.js`, `frontend/ghid.html`, `frontend/grup-details.html`. După deploy + Ctrl+F5: (1) click pe numele dintr-o cerere de înscriere → profilul; (2) admin pe mobil vede secțiunile; (3) lista utilizatori: reali întâi, exemple la coadă; (4) link „Vezi ghidul" pe pagina grupului. **Task COMPLET.**)
> 20 iulie 2026 (**COPY banda „Grupuri" + FIX acord singular la matching — commituri `c3471ae` (copy grupuri) + `88e76b4` (acord singular), PUSHATE pe `main`, ⏭️ DE DEPLOYAT din cPanel.** Două lucruri de conținut/afișare într-o sesiune, ZERO atingeri plăți/DB/RLS/edge fn. **(1) Text bandă informativă pe pagina Grupuri** (`frontend/grupuri.html`, linia 94, `+1/−1`): reformulat mesajul „platforma e la început" ca să distingă clar exemplele de grupurile reale → „Platforma e la început. Grupurile marcate cu „Exemplu" arată cum va funcționa; restul sunt grupuri reale, create de familii care caută vecini. Creează-ți propriul grup din zona ta." (înainte suna ca și cum TOATE grupurile de mai jos ar fi exemple). **(2) FIX dezacord singular/plural la badge-urile de matching** (raportat de Lucian dintr-un screenshot: apărea „1 zone comune" în loc de „o zonă comună"). Cauză: pluralul era hardcodat, fără tratarea singularului. Reparat în 3 fișiere: **`frontend/js/grupuri.js`** (`renderGrupCard`) + **`frontend/js/utilizatori.js`** (`renderUserCard`) — badge-urile de pe carduri folosesc acum „o zonă comună" / „un interes comun" la valoarea 1, pluralul „N zone comune" / „N interese comune" în rest (inclusiv 0, corect gramatical); **`frontend/index.html`** (hero-ul cu vecini de pe homepage, ~linia 2329) — badge + tooltip + aria-label folosesc „interes comun" la singular când un vecin are exact 1 interes comun (`tagShort`/`tagLong`). **Verificate că erau DEJA corecte** (au tratare singular/plural existentă, neatinse): `grup-details.html` (liniile 1539, 1586), `profile-view-new.js` (mesajele „Compatibilitate ridicată" apar doar la 4+, deci pluralul e mereu corect acolo). Diff total: 4 fișiere. **⏭️ DE DEPLOYAT din cPanel** ca să apară live: `frontend/grupuri.html`, `frontend/js/grupuri.js`, `frontend/js/utilizatori.js`, `frontend/index.html`; după deploy + Ctrl+F5: banda corectă pe Grupuri + acordul „o zonă comună / un interes comun" pe cardurile de la Grupuri și Utilizatori și pe hero-ul homepage logat. **Task COMPLET (rămâne doar deploy-ul cPanel).**)
> 18 iulie 2026 (**ANCHOR `#newsletter` pe homepage — commit `1d57924`, PUSHAT pe `main`, ⏭️ DE DEPLOYAT din cPanel (`frontend/index.html`).** Scop: linkul `https://apartamentual.ro/#newsletter` să ducă direct la formularul de abonare din banda „Serialul Județului Housing" (folosit când se trimite newsletterul manual pe email). Handoff sursă: `handoff/handoff-anchor-newsletter.md`. **Discrepanță față de handoff:** homepage-ul NU e la rădăcină (`index.html`) ci la **`frontend/index.html`** — restul a corespuns exact. Audit: secțiunea la linia 1732, NU exista deja `id="newsletter"`, niciun JS nu folosește selectorul `#newsletter` (formularul e țintit prin clasa `.js-newsletter-form`, handler `js/newsletter.js`). Fix = **o singură linie**, doar adăugarea atributului `id`: `<section class="section nl-band" aria-label="Abonare newsletter">` → `<section class="section nl-band" id="newsletter" aria-label="Abonare newsletter">`. Zero atingeri CSS (banda folosește clasa `.nl-band`, nu id), zero JS, zero plăți/DB/RLS/edge fn, niciun alt fișier. Diff: 1 fișier, +1/−1. **⏭️ DE DEPLOYAT din cPanel** (`frontend/index.html`); după deploy: `https://apartamentual.ro/#newsletter` trebuie să sară direct la formular, pe desktop și pe mobil. **Task COMPLET.**)
> 18 iulie 2026 (**FIX text „deschis" + DECIZIE: doar grupuri „cu aprobare" — commit `54b46b8`, PUSHAT pe `main`, ⏭️ DE DEPLOYAT din cPanel (frontend).** Simptom raportat: pe pagina unui grup „cu aprobare", banner-ul de la „Progresul Grupului" zicea „Acest grup este deschis — doar adminul poate edita jurnalul", derutant (grupul e cu aprobare, nu deschis). Cauză: textul din `grup-details.html` (linia 1105) era **hardcodat „deschis"**, nu citea statusul real; banner-ul apare ori de câte ori vizitatorul nu poate edita jurnalul (`!canEditChecklist`, liniile 1613–1614) — Lucian ca superadmin ne-membru îl declanșa. **DECIZIE DE PRODUS confirmată de Lucian:** platforma face DOAR grupuri „cu aprobare" (fondatorul aprobă cine intră, ca să nu intre oricine) — nu se mai fac grupuri „deschise". Context: `grup-nou.html` forța DEJA `cu_aprobare` la creare (câmp hidden, linia 460), dar `grup-edit.html` (liniile 458–477) încă oferea „🟢 Deschis" ca opțiune, chiar bifată default în HTML. Fix (2 fișiere frontend, zonă necritică — text + selector formular, ZERO plăți/DB/RLS/edge fn): **(1)** banner `grup-details.html` → „Doar membrii grupului pot edita jurnalul de progres. Contactează adminul pentru modificări."; **(2)** `grup-edit.html` selector status înlocuit cu bloc fix „🟡 Cu aprobare" (input hidden `value=\"cu_aprobare\"`, identic cu grup-nou.html) — bonus: un grup vechi pe „deschis" migrează automat la „cu aprobare" la prima editare+salvare (JS liniile 559–560 protejat cu `if(statusRadio)`, iar `fd.get('status')` linia 689 ia mereu cu_aprobare). **DECIZIE conexă (întrebată explicit):** cine editează jurnalul de progres într-un grup cu aprobare → **rămâne „toți membrii"** (logica de la linia 1403 NEATINSĂ: `canEditChecklist = isAdmin || (isMember && status==='cu_aprobare')`); fondatorul controlează cine INTRĂ, membrii contribuie apoi la jurnal. Notă: `isSuperAdmin` e calculat (linia 1398) dar NU e folosit în `canEditChecklist`, deci nici superadminul nu poate bifa checklist-ul unui grup în care nu e membru — lăsat așa intenționat. **⏭️ DE DEPLOYAT din cPanel** (`frontend/grup-details.html` + `frontend/grup-edit.html`); după deploy + Ctrl+F5: banner corect + opțiunea „Deschis" dispărută din editare. **Task COMPLET.**)
> 17 iulie 2026 (**FIX Creator + Zona „N/A" în emailul de grup nou — commit `e424963`, PUSHAT pe `main`, edge function `notify-admins` DEPLOYATĂ + VERIFICAT LIVE de Lucian.** Simptom raportat: la crearea unui grup, emailul intern „✨ Grup nou creat" arăta „Creator: N/A" și „Zona: N/A" (doar „Max membri" apărea corect). Cauză = **nepotrivire de nume de câmpuri** între frontend și edge function: `grup-nou.html` (liniile 816–822) trimite `created_by` (creatorul) + `oras` (zona), dar formatterul `group_created` din `notify-admins/index.ts` citea `owner_email`/`creator_email` și `zona` — care nu existau în payload → „N/A". `max_membri` se potrivea, de-aia mergea. Fix (soluția aleasă = backend, un singur loc, prinde și alte pagini care ar crea grupuri): în `notify-admins/index.ts` case `group_created`, adăugat fallback pe numele trimise de frontend — `ownerEmail = data.owner_email || data.creator_email || data.created_by || 'N/A'` și `zona = data.zona || data.oras || 'N/A'` (numele vechi păstrate pentru compat). **Doar text în emailul intern — zero atingeri plăți/DB/RLS.** Deployat de Lucian (fără Docker, din `C:\Users\lucia\supabase`) + confirmat live: Creator + Zona apar acum corect în email. **Task COMPLET.**)
> 17 iulie 2026 (**BLOC FINAL EPISODUL 1: teaser corectat + link automat spre Episodul 2 — commituri `941ee62` (fix) + `0977427` (handoff), PUSHATE pe `main`, ⏭️ DE DEPLOYAT din cPanel (`frontend/news.html`) ideal ÎNAINTE de sâmbătă 18 iulie ora 08:00**, când se publică Episodul 2 și pornesc postările social media; cât Ep. 2 e ciornă deploy-ul e sigur (se vede doar teaserul corectat). Problema: teaserul de deasupra formularului newsletter de la finalul Ep. 1 anunța greșit un episod despre autorizație; Ep. 2 real = „Grupul: cum ne-am găsit (de două ori)", slug ciornă CONFIRMAT de Lucian: `judetului-housing-episodul-2-grupul-cum-ne-am-gasit`. Făcut, un singur fișier (`frontend/news.html`): **(1)** teaser corectat (despre grup, hardcodat în HTML — NU e editabil din admin) cu `id="episodeOutroTeaser"`; **(2)** element nou `#episodeOutroNext` + 3 linii CSS; **(3)** în `showArticle()`, blocul outro: dacă episodul următor (nr. din slug +1, regex `judetului-housing-episodul-(\d+)`) există în array-ul `articles` (care conține DOAR articole publicate) → rând-link „Episodul N e aici: [titlu] →" cu href `#slug`, teaser ascuns, lead „Primește episoadele direct pe email:"; altfel teaser static (afișat DOAR pe Ep. 1 — textul e specific) + lead „Primește-l direct pe email:". **Zero interogări noi spre Supabase** (folosește lista deja încărcată); fallback garantat structural: dacă fetch-ul eșuează, pagina de articol nu se randează deloc → link mort imposibil. Bonus: pe Ep. 0 apare acum linkul corect spre Ep. 1 în loc de teaserul greșit; pe Ep. 2 (după publicare, fără Ep. 3) rămâne doar formularul, fără teaser fals. NEATINSE: formularul newsletter + `js/newsletter.js`, evenimentul Plausible `Newsletter Episod`, navigarea `#episodeNav` („← Episodul 0"), butoanele share, `loadArticles`. Testat cu codul REAL extras din fișier rulat în Node cu DOM simulat: 21/21 verificări (teaser pe ciornă, link cu slug+titlu corecte pe publicat, Ep. 0, non-episod ascuns, fallback listă goală, reguli de limbaj: fără em-dash, fără „autorizație"). **⏭️ DUPĂ publicarea Ep. 2 sâmbătă:** verificare vizuală pe pagina Ep. 1 (linkul apare automat, fără deploy) + o privire pe mobil. **Task COMPLET (rămâne doar deploy-ul cPanel).**)
> 2 iulie 2026 (**2 POZE NOI ALE CLĂDIRII FINALIZATE ÎN CALEIDOSCOPUL DE PE HOMEPAGE — commit `7518c33`, PUSHAT pe `main`, ⏭️ DE DEPLOYAT din cPanel.** Lucian a pus în `frontend/assets/images/judetul-housing/fotografii/` două poze brute de telefon ale Județului Housing FINALIZAT (una fațadă de la stradă `20260615_150751.jpg`, una panoramică aeriană `20260630_104641 (1).jpg`, 4032×2268). Cerință: redenumire + decupare pătrată centrată pe clădirea nouă + adăugare în caleidoscopul de pe homepage (caruselul cu randări + poze șantier). Făcut: **decupate pătrat 1200×1200 centrate pe clădirea nouă** cu PowerShell + GDI‑Plus (`System.Drawing`, fără ImageMagick — nu e instalat; InterpolationMode HighQualityBicubic, JPEG q88; crop din sursa 2268‑înălțime cu srcX 880 fațadă / 910 panoramă) → verificate vizual (fațada: clădirea nouă centrată + vecinul istoric galben la margine + placa „Strada Județului"; panorama: clădirea albă în mijloc). Redenumite `14-fatada-finalizata-strada.jpg` + `15-panorama-aeriana-finalizat.jpg` (următoarele numere după `13-finisaje-fatada`; folderul `fotografii` numerotează independent de `randari` care are r14/r15/r16). Originalele brute ȘTERSE. **`frontend/index.html`:** adăugate în manifestul `window.__imgFallback` ca `s14`/`s15`; băgate în `data-images` a două carduri din `#icGrid` — `s14` în cardul progresiei fațadei (`s01|s05|s09|s13|s14`), `s15` în cardul vederilor aeriene (`s07|s08|s11|s12|s15`). Galeria = 4 carduri `.ic-card` care rotesc prin listă de ID‑uri cu cross‑fade (scriptul carusel de la ~linia 2051); nr. de carduri neschimbat, deci textul „04 imagini" rămâne corect. **Niciun JS de plăți/fetch/RLS/edge fn atins** — doar manifest + 2 `data-images` + 2 imagini noi. **⏭️ DE DEPLOYAT din cPanel** (`frontend/index.html` + cele 2 imagini) ca să apară live; după deploy + Ctrl+F5, cele 2 poze reale se rotesc în caleidoscop lângă randări și pozele de șantier. **Task COMPLET.**)
> 2 iulie 2026 (**CORELARE COMANDĂ ANALIZĂ ↔ TEREN DE PE PLATFORMĂ — commituri `d3fb7e1` (notify-admins, DEPLOYAT de Lucian) + `77674e6` (frontend, ⏭️ DE DEPLOYAT din cPanel), PUSHATE pe `main`.** Cerință Lucian: când o comandă de analiză pleacă de la un teren postat pe platformă, emailul intern arăta doar un link generic „Link anunț teren", fără să distingă un teren PROPRIU de un anunț EXTERN (OLX/Storia) și fără context — Lucian voia să coreleze rapid comanda cu terenul de pe platformă. Context descoperit: id-ul terenului există deja „topit" în interiorul `link_teren` (`.../teren-details.html?id=<UUID>`), pus acolo de `prefillFromContext()`; câmpul `teren_id` e definit în backend dar NEPOPULAT; tabelul `comenzi_analize` NU are coloană `teren_id` (doar `nr_cadastral`, `adresa_teren`, `descriere_teren`, `link_teren`). Soluție aleasă = **varianta rapidă** (doar afișare email + blocare câmp; NU s-a atins zona de plăți/DB). **(A) `notify-admins` (DEPLOYAT de Lucian, fără Docker):** helper nou `buildTerenRow(linkTeren)` — detectează prin regex dacă linkul e INTERN (`teren-details\.html\?id=<UUID 36>`) → rând dedicat **„🟢 Teren pe platformă"** cu link „Deschide terenul pe platformă" + `ID teren: <UUID>` (pentru corelare rapidă în Supabase); altfel **„Link anunț teren (extern)"**. Aplicat în ambele emailuri `comanda_creata` + `comanda_platita` (înlocuit rândul vechi cu `buildTerenRow(linkTeren)`). Doar formatare email — zero atingeri plăți/DB. **(B) `frontend/js/comanda-analiza.js` (⏭️ DE DEPLOYAT cPanel):** în `prefillFromContext()`, când formularul e deschis dintr-un teren de pe platformă (`?teren_id=...`), câmpul `link_teren` se completează automat ȘI devine **readonly** (fundal gri `#f4f4f4`, cursor not-allowed), eticheta → „Teren de pe platformă (completat automat)", indiciu 🔒 „Terenul este preluat de pe platformă. Linkul e completat automat și nu poate fi modificat" — ca linkul să nu poată fi șters/modificat accidental înainte de comandă. Comanda MANUALĂ (fără teren de pe platformă) rămâne neschimbată (câmp editabil). **LIMITĂRI (onest):** (1) readonly oprește ștergerea accidentală de un client normal, dar NU e blindat (bypass posibil din devtools) — blocarea sigură = varianta robustă cu `teren_id` server-side; (2) titlul terenului NU apare în email (doar link + ID), fiindcă asta ar fi cerut o citire din DB în notify-admins. **⏭️ DE DEPLOYAT din cPanel:** `frontend/js/comanda-analiza.js`. **⏭️ DE TESTAT după deploy:** comandă de probă pornită de pe un teren de pe platformă (FĂRĂ plată — emailul „Comandă nouă" pleacă la CREARE, înainte de plată; te oprești la pasul Netopia; efect secundar: creează o proformă reală în Oblio + rând în `comenzi_analize`, de curățat) → în email rândul „🟢 Teren pe platformă" cu link + ID + în formular câmpul link gri/blocat cu 🔒. **⏭️ ORIZONT (opțional, sesiune separată) — varianta ROBUSTĂ:** `teren_id` ca input hidden în formular + în `buildPayload()` + coloană nouă în `comenzi_analize` (migrație) + citit în `creeaza-proforma-oblio` (⚠️zona plăți) + transmis la ambele notificări → legătură garantată, imposibil de ocolit. **Task (varianta rapidă) COMPLET.**)
> 1 iulie 2026 (**IDENTIFICARE TEREN: nr. cadastral SAU adresă + LOGARE NOTIFICĂRI — commituri `1aaff27` + `4aa7e54`, PUSHATE pe `main`, edge functions DEPLOYATE, frontend DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (1 iulie).** Două lucruri într-o sesiune. **(A) OBLIGATIVITATE TEREN la analiza preliminară — „cel puțin unul din două".** Cerință Lucian: numărul cadastral să nu mai fie strict obligatoriu; clientul completează FIE (1) numărul cadastral, FIE (2) adresa exactă a terenului (cu care găsim noi nr. cadastral pe geoportalul ANCPI, `https://geoportal.ancpi.ro/imobile_lookup.html`). Adresa terenului e câmp NOU, complet separat de adresa de FACTURARE. 7 fișiere: **migrația `031_comenzi_analize_adresa_teren.sql`** (coloană `adresa_teren text`, nullable, idempotent, rulată de Lucian); **`comanda-analiza.html`** (nr_cadastral fără `required`/fără nota roșie; câmp nou „Adresa exactă a terenului" cu link ANCPI; notă crem+bordură teracotă „Completează cel puțin unul"; bifa `accept_cadastral` reformulată în „Confirm că numărul cadastral sau adresa terenului… sunt corecte"); **`js/comanda-analiza.js`** (validare either/or: nr_cadastral ≥3 SAU adresa_teren ≥5, în `validateClient` + în `setupLiveValidation` pentru dezactivarea butonului; `adresa_teren` adăugat în `buildPayload`); **`creeaza-proforma-oblio`** ⚠️zona plăți — `adresa_teren` în interfața `Input`, validare either/or, insert DB, payload notificare; nr_cadastral accesat acum cu `?.` fiindcă poate lipsi (logica de preț/TVA/proformă/factură NEATINSĂ); **`notify-admins`** + **`oblio-webhook`** (rând „Adresa teren" în emailurile `comanda_creata` + `comanda_platita`). Aliniere text în alte 2 pagini care mai ziceau „nr cadastral obligatoriu" (`4aa7e54`): **`analize.html`** secțiunea „Ce avem nevoie de la tine" → „Obligatoriu — cel puțin unul dintre" (nr cadastral / adresă), adresa mutată din „opțional", scoasă culoarea roșie de alarmă; **`politica-retur.html`** secțiunea 5 (pagină legală) reformulată la „cel puțin unul dintre numărul cadastral sau adresa exactă". Notă: `analiza-simplificata.html` era DEJA formulată corect. **(B) LOGARE NOTIFICĂRI reparată — pagina admin „Notificări" era înghețată din martie.** Cauză descoperită: pagina `admin-notificari.html` doar CITEȘTE tabelul `notification_log`, dar NIMIC din tot repo-ul (nici în istoricul git) nu mai scrie în el — funcția `notify-admins` trimitea email+Slack fără să logheze. Tabelul avea rânduri doar între 15–17 martie 2026 (probă veche). **Important: notificările reale funcționau perfect** (doar istoricul era gol). Fix: adăugat helper `logNotification()` în `notify-admins` care scrie un rând în `notification_log` (coloane confirmate de Lucian: `id, event_type, channel, recipient, subject, status, payload, created_at`; id+created_at auto) după fiecare trimitere email/slack — BEST-EFFORT, prins în try/catch ca să NU blocheze notificarea reală; folosește REST PostgREST cu `SUPABASE_SERVICE_ROLE_KEY` (fără dependință nouă). **⏭️ DE VERIFICAT (opțional):** (1) test comandă cu DOAR adresa (fără nr cadastral) → rândul „Adresa teren" apare în emailul „Comandă nouă"; (2) pagina `/admin-notificari.html` arată rânduri noi cu data de azi — dacă NU apar, cel mai probabil coloana `payload` nu e `jsonb` (insertul e best-effort, nu strică notificarea) → verifică `select data_type from information_schema.columns where table_name='notification_log' and column_name='payload'`. **Task COMPLET.**)
> 1 iulie 2026 (**FIX număr cadastral la comenzile de analiză — commit `96ce34c`, PUSHAT pe `main`, DEPLOYAT + TESTAT LIVE de Lucian (1 iulie).** Simptom raportat: la o comandă de analiză preliminară plătită cu Netopia, emailul intern „Comandă nouă — așteaptă plată" arăta doar `descriere_teren` (viziunea liberă a clientului), deci nu se știa CARE teren trebuie analizat. Cauză: terenul e identificat prin **numărul cadastral** (`nr_cadastral`, câmp obligatoriu în formular), dar edge function-ul `creeaza-proforma-oblio` NU-l cunoștea — nu era în interfața `Input`, nu se salva în `comenzi_analize`, nu se trimitea la `notify-admins`. Se pierdea complet (nici DB, nici email). Fix în 4 fișiere: **(1) migrația `030_comenzi_analize_nr_cadastral.sql`** — `ALTER TABLE ... ADD COLUMN IF NOT EXISTS nr_cadastral text` (nullable, idempotent), rulată de Lucian în Supabase SQL Editor. **(2) `creeaza-proforma-oblio`** — `nr_cadastral` adăugat în interfața `Input`, în validare (min 3 caractere), în insert-ul DB și în payload-ul notificării (plus `link_teren`, care ERA salvat în DB dar nu se trimitea la email). **(3) `notify-admins`** — rândurile „Nr. cadastral teren" + „Link anunț teren" adăugate în emailurile `comanda_creata` ȘI `comanda_platita`. **(4) `oblio-webhook`** — citește `nr_cadastral` + `link_teren` din DB și le trimite la notificarea de plată confirmată. **Niciun cod de plată atins** (calcul preț/TVA, proformă Oblio, factură, verificare webhook — neschimbate); doar propagarea unui câmp de date prin fluxul existent. Cele 3 edge functions DEPLOYATE (din `C:\Users\lucia\supabase`, fără Docker) + test comandă reală confirmat de Lucian: numărul cadastral apare acum în email. **Notă:** comenzile create ÎNAINTE de fix (ex. `APT-20260701-4f0e06`) nu au numărul cadastral salvat — irecuperabil pentru ele; toate comenzile noi îl au. **Task COMPLET.**)
> 1 iulie 2026 (**PLAUSIBLE ANALYTICS pe tot site-ul — commit `23e1c35`, PUSHAT pe `main`, DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (1 iulie).** Adăugat scriptul de analytics **Plausible** (privacy-friendly, **fără cookies → fără banner de consimțământ**) în `<head>`-ul fiecărei pagini, imediat sub `<title>` (deasupra Open Graph / preconnect / CSS). Site-ul e vanilla, **fără `<head>` comun / layout**, deci același bloc de 6 linii a fost inserat identic în **toate cele 52 de pagini HTML din `frontend/`**: `<script async src="https://plausible.io/js/pa-DV5BzzPUWOAnlwlSZU8LW.js">` + init inline. **Excluse intenționat** (nu-s pagini de site): `email_templates/` (3), `_archive/`, `package-homepage-v9/`, `handoff/`. Diff: **52 fișiere, +312 / −0**, EOL-ul fiecărui fișier păstrat (repo are LF+CRLF mixte). **Niciun cod de plăți/RLS/edge fn/JS/fetch atins** — pur `<head>`. Verificat LIVE: scriptul prezent pe `/`, `/ce-este/`, `/servicii.html`, `/admin.html`, `/terenuri.html`; fișierul JS Plausible răspunde **HTTP 200**. Notă: regula cerea „păstrează `defer`", dar scriptul oficial Plausible folosește `async` — lăsat exact așa cum a fost furnizat. **RĂMÂNE doar (opțional, doar Lucian):** confirmă vizitele în dashboard-ul Plausible.)
> 29 iunie 2026 (**POLITICA DE LIVRARE (cerință Netopia) + microcopy — toate PUSHATE pe `main`, DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (29 iunie).** Verificat live: pagina `politica-livrare.html` se randează complet (7 secțiuni + bloc firmă cu `J2007012417402` + data 29 iunie 2026); link „Politica de livrare" prezent în footer pe homepage (footer inline) ȘI în `footer.js` live (deci pe toate paginile interne). **RĂMÂNE doar pasul în panoul Netopia:** bifează „Politică de livrare comandă" + indică URL `https://apartamentual.ro/politica-livrare.html`. Cinci commituri, niciun cod de plăți/RLS/edge fn atins. **(1) Pagina nouă `politica-livrare.html`** (commit `e221809`): Netopia cere „Politică de livrare comandă" ca document distinct de Politica de retur. Pagina e clonă vizuală 1:1 a `politica-retur.html` (același `<head>`, `<style>` v9, nav `id="navigation"`, footer `id="footer"`, scripturi), cu titlu/meta „Politica de livrare" + 7 secțiuni de conținut (servicii digitale, livrare PDF pe email, termen 3-5 zile lucrătoare preliminară / prin contract detaliată, date insuficiente → suspendare, întârzieri → refund prin Politica de retur, confirmare livrare, contact) + bloc firmă (SC LTFB Studio SRL · CUI RO22004992 · Reg. Com. **J2007012417402** · Str. Popa Petre 23, Sector 2, București · ultima actualizare 29 iunie 2026). Brief sursă: `handoff/politica-livrare.md`. **(2) Link în footer LEGAL** adăugat imediat după „Politica de retur": în `js/footer.js` (sursa unică a footer-ului pe toate paginile dinamice) ȘI în `index.html` (commit `8dede1a` — homepage are footer INLINE hardcodat, NU încarcă footer.js; de-asta linkul „nu apărea" pe homepage la prima rundă). Restul referințelor la „politica-retur" din `analize`/`comanda-analiza`/`servicii`/`termeni-analize` sunt text în corp (consimțământ/FAQ/termeni), NU footer — lăsate neatinse. **(3) Microcopy teren-details** (commits `77c46e3` + `9e828b9`): butonul „Cere o analiză" din `teren-details.html` (logat) → chenar negru proeminent (bg-black, font-semibold, border-2, shadow-md); linkul-text „Cere o analiză" din rândul „Număr apartamente" (nelogat, când N/A, în `js/teren-details.js`) → badge negru încadrat. Doar clase de stil, logica/handlerele neatinse. **(4) Microcopy consultanță** (commit `a1cce44`): eticheta pill „Fezabilitate proiect" → „Cum pornești un proiect colaborativ" în `consultanta.html` (`data-value="proiect"` neschimbat) + referința din răspunsul FAQ `js/faq.js`. **⏭️ DE DEPLOYAT din cPanel** ca să apară live: `politica-livrare.html`, `js/footer.js`, `index.html`, `teren-details.html`, `js/teren-details.js`, `consultanta.html`, `js/faq.js`. După deploy + hard refresh (Ctrl+F5, ca să nu țină footer.js din cache): verifică linkul „Politica de livrare" în footer pe homepage ȘI pe o pagină internă; apoi în Netopia indică URL-ul `https://apartamentual.ro/politica-livrare.html`.)
> 25 iunie 2026 (**REÎNCADRARE EXEMPLE (DEMO) + CARD DE RECRUTARE pe Utilizatori și Grupuri — commit `bf0e889`, PUSHAT pe `main`, DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (25 iunie).** Scop: profilurile/grupurile de exemplu (marcate `is_demo` în DB) creau percepția de „fals" la un vizitator sceptic. Reîncadrate ONEST, fără să atingem datele din producție (`is_demo` rămâne mecanismul; doar template-ul s-a schimbat). Trei piese, 4 fișiere: **(1) Badge redenumit** „DEMO" → **„Exemplu"** în `js/utilizatori.js` (`renderUserCard`) + `js/grupuri.js` (`renderGrupCard`); stil mai calm (scos `text-transform:uppercase` + iconița eprubetă `fa-flask` care citea „test de laborator", culori crem/gri `#f0ece3`/`#6f6a61` în loc de chihlimbar/maro de alertă). Textul „demo" era **hardcodat în template, nu în date** — deci schimbarea e pur vizuală. **(2) Bandă onestă** deasupra grilei (clasă `examples-note`, ton calm, fundal crem + bordură teracotă) în `utilizatori.html` și `grupuri.html`: „Platforma e la început. … sunt exemple, ca să vezi cum vor arăta [anunțurile/grupurile] reale. …". **(3) Card de recrutare** „Locul tău aici" (utilizatori) / „Fii primul grup real" (grupuri), injectat **din JS ca PRIM card** în grilă (nu HTML static — grila e rescrisă la fiecare randare), vizual distinct (contur teracotă punctat, fără avatar), **doar pentru vizitatori NELOGAȚI** (`!currentUser`), CTA → `register.html`. Niciun cod de plăți/RLS/edge fn atins; datele Supabase neatinse. Brief sursă: `handoff/HANDOFF-demo-reframe.md` (untracked, local). **⏭️ DE TESTAT după deploy cPanel:** nelogat pe `/utilizatori.html` + `/grupuri.html` → banda calmă sus + cardul de recrutare ca prim card + badge-urile zic „Exemplu"; logat → cardul de recrutare NU apare; mobil → grila nu se strică. **Pas separat pe DATE (opțional, doar tu):** dacă vreun nume de exemplu seamănă cu o persoană reală, redenumește-l în Supabase în nume clar ilustrativ („Familia M.", „Grup de medici" etc.). **Notă (25 iunie):** verificat contul `Tibs` (electrician, creat 11 mai 2026) — `is_demo = false`, deci apare ca profil REAL, NU primește badge „Exemplu". Lăsat așa intenționat; nu s-a putut stabili cu cheia anon dacă e user autentic sau cont de test nemarcat.)
> 25 iunie 2026 (**OFICIALIZARE J la ONRC — revenire la `J2007012417402`.** Numărul `J2007012417402` a fost între timp **oficializat la ONRC** și e acum cel de pe certificat → înlocuit pe tot site-ul cele 6 afișări reale ale datelor firmei (`index.html` footer homepage, `js/footer.js` footer global, `gdpr.html`, `termeni.html` ×2, `politica-retur.html`) + fișa „bara firmă" din `HANDOFF-integrare-homepage-v9.md`. **Anulează nota de mai jos din 25 iunie** care îl marca drept neoficializat și revenirea la `J40/12417/2007` (commit `97d6367`). Placeholder-ul `ex: J40/1234/2020` din `comanda-analiza.html` rămâne neatins (exemplu generic, nu datele LTFB). Niciun cod de plăți atins — doar text afișat. Commit `330411a`, PUSHAT pe `main`, **DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (25 iunie)** — footer + `termeni`/`gdpr`/`politica-retur` arată `J2007012417402`, zero apariții ale numărului vechi. Verificat de Lucian (25 iunie) că `J2007012417402` e actualizat și în **Oblio (facturare)** + **Netopia** — datele firmei de pe facturi/plăți sunt consistente cu certificatul. **Task COMPLET.**)
> 25 iunie 2026 (**REVENIRE J Reg. Comerțului — commit `97d6367`, PUSHAT pe `main`, DEPLOYAT din cPanel + VERIFICAT LIVE de Lucian (25 iunie).** J-ul nou `J2007012417402` NU a fost oficializat la ONRC; J-ul oficial de pe certificat rămâne **`J40/12417/2007`** (consistent cu Oblio + Netopia). Înlocuit peste tot: 6 afișări reale ale datelor firmei în `index.html` (footer homepage), `js/footer.js` (footer global), `gdpr.html`, `termeni.html` (×2), `politica-retur.html`. În plus, în `comanda-analiza.html` placeholderele de exemplu din formular (CUI + Reg. Com., care erau chiar datele LTFB) → exemple **neutre** (`ex: RO12345678`, `ex: J40/1234/2020`), ca datele firmei să nu apară nici ca exemplu pe un câmp pe care clientul îl completează singur. Și doc-ul intern `HANDOFF-integrare-homepage-v9.md` actualizat. Verificat după: `J2007012417402` = 0 apariții în repo. **Niciun cod de plăți atins** (doar text afișat + un placeholder de formular). **⏭️ DE DEPLOYAT din cPanel** ca să apară live; după deploy verifică footer + paginile legale (`termeni`/`gdpr`/`politica-retur`) că arată `J40/12417/2007`.)
> 25 iunie 2026 (**FIX cerere de parteneriat — commit `7a05bac`, PUSHAT pe `main`. SQL RLS RULAT de Lucian + edge function `notify-admins` REDEPLOYATĂ.** Formularul `devino-partener.html` eșua pentru ORICE vizitator nelogat: tabela `partners` avea RLS pornit dar FĂRĂ policy de INSERT pentru public → cererea era respinsă (42501 „new row violates row-level security policy"), nu ajungea nici în DB nici pe email/Slack; userul vedea doar „Eroare la trimitere. Încercați din nou." Fix în 3 piese: **(1)** migrația `025_partners_public_insert_rls.sql` — policy de INSERT pentru rolurile `anon`+`authenticated`, limitată prin `WITH CHECK` la rânduri NEAPROBATE (`is_active`/`is_verified`/`is_featured` = false), deci nimeni nu-și poate publica singur un partener activ/verificat/recomandat; conține și un bloc OPȚIONAL **comentat** pentru upload logo în `public-assets/partner-logos/` — NErulat momentan (logo-ul îl pune adminul după aprobare, până se decide dacă merită riscul mic de upload public). **(2)** `notify-admins` case `partner_application` — emailul de notificare arată acum **categorie lizibilă + telefon, oraș, website, descriere** (înainte website lipsea complet, categoria apărea ca slug brut `arhitectura`, iar „Mesaj" era mereu N/A). **(3)** `devino-partener.html` trimite și telefon/oraș/descriere către notificare (înainte se pierdeau). Niciun cod de plăți/Oblio/Netopia atins; din `notify-admins` doar blocul `partner_application`. Test live cu cheia anon a confirmat înainte bug-ul (INSERT respins) și nu a lăsat rânduri de test. **⏭️ DE TESTAT după deploy cPanel al `devino-partener.html`:** trimite o cerere de test **NELOGAT** (incognito) → ecran „Cerere trimisă! ✅", rândul apare `Inactiv` în `admin-parteneri.html`, notificare Slack `#app_events` + email pe `apartamentual@ltfbstudio.ro` cu toate câmpurile; șterge rândul de test din admin după.)
> 24 iunie 2026 (**SHARE + OPEN GRAPH + conținut homepage — toate PUSHATE pe `main`, NEDEPLOYATE încă (deploy manual cPanel).** Patru livrări de conținut/feature, niciun cod de plăți/RLS/edge fn atins: **(1) Titlu hero homepage** (commit `f46ebab`): „Un apartament se poate și construi, nu doar cumpăra" — „apartament" în `<span class="title-tau">`, culoarea dinamică ciclează prin scriptul inline existent. **(2) Banda newsletter redenumită „serialul"** (commits `6020489` + `2e5ac5b`): titlu „Serialul Județului Housing: povestea reală, pe episoade", subtitlu nou (avizele, vecinii, banii, șantierul — fără „fără retușuri", scos ulterior), link discret „Începe cu episodul 0" → `/news.html` (clasa nouă `.nl-prelink`, culoare terracotta existentă). Formularul/handler-ul newsletter NEATINSE. **(3) SHARE + OPEN GRAPH** (commit `da9a44f`, 7 fișiere, sursă `handoff/HANDOFF-share-og.md`): butoane share **Copiază link · WhatsApp · Facebook** (a) pe articol în `news.html` la finalul articolului, URL canonic `apartamentual.ro/news.html#<slug>`, legate în `showArticle()`; (b) pe `teren-details.html` — am ȘTERS butonul mort „Trimite unui prieten" (era fără handler, în zona `#user-action-buttons` login-gated) și am pus grup **PUBLIC** vizibil oricui, URL `apartamentual.ro/teren-details.html?id=<id>`, script inline care citește `id` din URL (nu am atins `js/teren-details.js`). „Copiază link" = `navigator.clipboard` + feedback „Link copiat" 2s. **Open Graph + Twitter Card la nivel de PAGINĂ** pe 6 pagini (`index`, `news`, `terenuri`, `ce-este/index`, `servicii`, `povestea-noastra`) cu imagine comună `https://apartamentual.ro/assets/images/judetul-housing/randari/15-randare-fatada-curte.jpg` (pătrată 1200x1200, temporar — va fi decupată pe cardurile mari). **Limitare cunoscută (NU bug):** OG e per-PAGINĂ, deci toate articolele/terenurile distribuite arată ACEEAȘI previzualizare brandată; previzualizări UNICE per articol/teren = task viitor (URL reale + pre-randare server/edge). **Grupuri = neatins** (detalii login-gated → share de grup rămâne task viitor pt. logați). **(4) Card News real pentru episodul 0 pe homepage** (commit `759085d`): înlocuit placeholderul gri „ÎN CURÂND / Județul Housing" cu `<a>` real → `news.html#judetului-housing-episodul-0`, titlu card „Serial Județului Housing: episodul 0", `data-article-slug` setat → imaginea articolului se încarcă AUTO din Supabase (scriptul existent de la ~linia 2112). Slug episod 0 = **`judetului-housing-episodul-0`** (curat, fără sufix; verificat prin query anon la `articles`). **⏭️ DE TESTAT după deploy cPanel:** (a) Facebook Debugger pe homepage + WhatsApp — **confirmă că imaginea OG se încarcă** de la `/assets/images/judetul-housing/randari/15-randare-fatada-curte.jpg`; dacă dă 404, path-ul `/assets/` de pe cPanel diferă și trebuie ajustat în cele 6 fișiere; (b) butoane share pe un articol publicat (URL corect cu `#slug`) și pe un teren **fără login** (URL cu `?id=`); (c) cardul episod 0 pe homepage are imagine + link. **TODO viitor opțional:** imagine OG dedicată **1200x630** (peisaj, fără decupaj); previzualizări OG unice per articol/teren.)
> 23 iunie 2026 (**NEWSLETTER double opt-in — commit `276bb5d`, PUSHAT pe `main`. Migrațiile SQL 023+024 RULATE. Edge functions DEPLOYATE (23 iunie, fără erori). Frontend DEPLOYAT din cPanel (LIVE, confirmat). **AJUSTĂRI după prima rundă (commit `0b5f232`, pushat):** (a) numele expeditorului din emailul de confirmare → `ApartamenTUal by LTFB Studio` (notă: dacă apare „Apartamentul tău de studioul LTFB" e auto-translate din clientul de email al destinatarului, nu de la noi); (b) pagina `newsletter-confirmat.html` + banda de pe homepage explică acum diferența **cont vs. fără cont** — cu cont primești personalizat (terenuri din zona ta de interes + potriviri cu utilizatori/grupuri), fără cont doar terenuri noi neselectate; pe pagina de confirmare butonul principal e „Creează cont". **(c) DEZABONARE DIN ADMIN (commit `71d14a2`):** edge fn nouă `newsletter-unsubscribe` (verifică superadmin via JWT, marchează `unsubscribed` în Supabase + PATCH contact `unsubscribed:true` în Resend, best-effort cu raportare `resend_ok`); buton „Dezabonează" pe coloana Acțiuni în `admin-newsletter.html`. Fără SQL nou (scrierea trece prin service_role). **(d) FIX auto-traducere brand în email (commit `21f98c0`):** `translate="no"`/`notranslate` pe logo+tagline+mențiuni „ApartamenTUal" în emailul de confirmare. **TOTUL DEPLOYAT ȘI LIVE, testat de Lucian — newsletter COMPLET.**) Newsletter complet, cu double opt-in. Arhitectură: **Supabase `newsletter_subscribers`** = sursa de adevăr pentru consimțământ/GDPR (status `pending|confirmed|unsubscribed`, RLS activ cu ZERO policy de scriere = doar service_role scrie); **Resend Contacts globale** (model nov. 2025, fără `audience_id`) = lista de trimitere efectivă, broadcasturile se trimit din dashboard-ul Resend. Flux: formular → edge fn `newsletter-subscribe` (scrie `pending`, anti-abuz 2 min, trimite email confirmare via Resend) → user apasă link → edge fn `newsletter-confirm` (`status=confirmed` + creează contactul global în Resend; **deploy cu `--no-verify-jwt`** fiindcă e accesată din linkul de email, fără auth) → redirect la `newsletter-confirmat.html`. Notificare internă: event nou `newsletter_signup` în `notify-admins` (singura modificare la fn existentă: un `case` + o intrare în `SUPERADMIN_CC_IF_NO_RECIPIENT`) → Slack `#app_events` + email la **`apartamentual@ltfbstudio.ro`** (recipient explicit din `newsletter-subscribe`, deci fără CC superadmin). Captură frontend: **bandă pe homepage** între galeria Județului Housing și secțiunea echipă (fundal crem deschis + buton secundar — intenționat mai ușoară ca benzile de webinar) + **formular discret în footer** injectat global prin `footer.js` (care încarcă dinamic `js/newsletter.js`, cale derivată din propriul src ca să meargă și în `/ce-este/`). Handler comun `frontend/js/newsletter.js` (delegare submit, validează email+checkbox consimțământ, `sb.functions.invoke('newsletter-subscribe')`). **Admin:** pagina nouă `admin-newsletter.html` (read-only: filtre pe status, contoare, export CSV confirmați; pază `is_super_admin`) + link în sidebar-ul `admin.html`. SQL: **023** creează tabelul (în `handoff/HANDOFF-newsletter.md`), **024** = policy SELECT pentru superadmin via `public.is_super_admin()`. Niciun cod de plăți atins. 11 fișiere, +1112. **⏭️ DE DEPLOYAT:** (1) 3 edge functions din `C:\Users\lucia\supabase` — `newsletter-subscribe`, `newsletter-confirm --no-verify-jwt`, `notify-admins` (re-deploy); fișierele sunt deja copiate în folderul de deploy. (2) frontend din cPanel. **⏭️ DE TESTAT după deploy:** bandă homepage → email confirmare → link → „Gata, ești pe listă." + rândul devine `confirmed` în Supabase + contact în Resend; notificare pe `apartamentual@ltfbstudio.ro` + Slack; footer pe o pagină internă (ex. `/terenuri.html`); `/admin-newsletter.html` afișează abonatul.
> 19 iunie 2026 (**Titlu hero nou pe HOMEPAGE — commit `098433a`, PUSHAT, NEDEPLOYAT încă (deploy manual cPanel)**). Un singur fișier, `frontend/index.html` (+1/−1): în hero-ul de marketing (nelogați, `data-variant="marketing"`) titlul „Cauți demult un apartament în zonă bună, la preț corect." → **„Încă nu ai găsit apartamentul potrivit?"**. Păstrată clasa `.hero-title` (font/înălțime) și accentul `<span class="title-tau">` mutat pe „apartamentul" → culoarea dinamică ciclează în continuare (scriptul inline existent). Doar conținut — niciun JS/fetch/plăți/alt fișier atins. **⏭️ DE DEPLOYAT din cPanel** ca să apară live.
> 19 iunie 2026 (**CTA „Creează cont" în caseta „Ești gata să începi?" pe `/ce-este/` — commit `eb031b3`, PUSHAT, NEDEPLOYAT încă (deploy manual cPanel)**). Un singur fișier, `frontend/ce-este/index.html` (+2/−1): în blocul `.cta-final` adăugat butonul principal „Creează cont" → `/register.html` (link folosit consecvent pe tot site-ul); „Vezi grupuri" și „Vezi terenuri" coboară la `cta-secondary`. Doar conținut/HTML — niciun JS/fetch/plăți atins. **⏭️ DE DEPLOYAT din cPanel** ca să apară live.
> 16 iunie 2026 (**„Strada Județului" → „Județului Housing" la referințele de PROIECT pe homepage — commit `7fcad33`, pushat, NEDEPLOYAT încă (deploy manual cPanel)**). Modificare minoră de conținut, doar `frontend/index.html` (6 înlocuiri): title video YouTube, atribuire testimonial Tiberiu M., aria-label + eyebrow + caption galerie, butonul „Vezi proiectul…". Lăsate INTENȚIONAT neatinse referințele **geografice/adresă** (sună natural doar cu strada): „blocul **de pe** Strada Județului", „blocului **din** Strada Județului", meta+H1 din `povestea-noastra.html` („…de pe strada Județului"), alt-ul hărții, câmpul „Locație: București, Strada Județului" din `ce-este/testimoniale.html`. Niciun JS/fetch/plăți atins.)
> Sesiunea 15 iunie 2026 (**HERO PE ROLURI pe homepage — commit `a24466e`, local, NEDEPLOYAT încă**). Zona de sus a homepage-ului se adaptează după sesiune+rol, FĂRĂ rebuild: hero-ul de marketing (nelogat) rămâne neschimbat, înfășurat acum în `<div class="hero-stage">` cu `data-variant="marketing"`; logat-utilizator → Varianta A (cluster cu max 4 vecini care au cele mai multe **interese comune** — count tags+zone client-side ca pe pagina Utilizatori, badge „N interese" NU procent, stare goală blândă dacă profilul n-are interese/zone); logat-agenție (`account_type='profesional'`) → Varianta B (chips „X aprobate / Y în așteptare" din `terenuri` pe `created_by_user_id` + micro cu ultima propunere, **FĂRĂ avataruri** — respectă restricția agențiilor). „User în grup" = AMÂNAT, face fallback la Varianta A. Bara de apartamente de sub hero: SVG-ul `#squaresSvg` (cu stroke/linii) înlocuit cu `.aptbar` (pătrate ponderate separate prin **gap 5px**, `border-radius:2px`, fără borders). Tot CSS-ul nou e **scopat pe `.hero-stage`** (zero modificări în `:root`); 2 redenumiri ca să evit coliziuni reale cu tokenii site-ului: `--bg-tile` (altfel pătratele ieșeau crem din cauza lui `--bg`) și `hero-floaty`. Scriptul „HERO PE ROLURI" e pus DUPĂ `supabase-config.js`, cu retry pe `sb` ca nav.js; toate interogările sunt READ-only (`select` pe `profiles`/`terenuri` + `auth.getUser()`), niciun write, nimic la plăți. Un singur fișier atins: `frontend/index.html` (+480/−120). Handoff sursă: `handoff/HANDOFF-hero-roluri-bara-apartamente.md`. **⏭️ DE TESTAT după deploy:** logat ca user cu interese (cluster cu 4 vecini), user nou fără interese (cluster gol cu mesaj+link profil), cont agenție (chips pe nr. real / pe 0, fără micro fals), nelogat (marketing identic), responsive (mobil: clusterul devine rând, max 2–3 pătrate). Rute CTA folosite: A → `/terenuri.html` + `/grupuri.html`; B → `/terenuri-propune.html` + „propunerile mele" → `/profile-view-new.html?id={userId}`.)
> Sesiunea 10 iunie 2026 (favicon nou „TU" aliniat la v9 — commit 1ebf868, **deployat din cPanel, LIVE, confirmat de Lucian**: vechiul „A" alb pe bleumarin #0f172a cu punct portocaliu #f97316 → semnătura „TU" cărămizie #c2604a pe ink #1a1a1a, fidelă logo-ului; regenerat tot setul svg/png 16-32-48/apple-touch 180/ico, „TU" desenat vectorial ca paths ca să nu depindă de font la rasterizare; niciun HTML atins — nume de fișier identice. Generare raster: sharp + png-to-ico instalate temporar într-un folder din afara repo-ului.)
> Sesiunea 9 iunie (4 lucruri): (1) galerie cu miniaturi pe `teren-details`, commit c7c3cb4; (2) fix upload — nume unic per imagine, pozele se suprascriau în storage, commit bcc097f; (3) aliniere v9 a esteticii emailurilor — `notify-admins` + `oblio-webhook`, commit 6fdca71, **deployat cu succes, fără erori**; (4) fix teren `pending` care nu se încărca din linkul de email de aprobare — race auth + `.single()`, commit f6152f6. Sesiunea 8 iunie: clusterul „analize" complet + 2 fix-uri nav. **Aliniere v9 a întregului site = ÎNCHEIATĂ.**

---

## ⏭️ DE ÎNCEPUT ÎN SESIUNEA URMĂTOARE (după /clear)

🟠 **TASK NOU (1 august) — LISTA DE MEMBRI A ORICĂRUI GRUP E CITIBILĂ FĂRĂ CONT.** Sesizat de Lucian imediat după ce s-a închis gaura cu emailurile, pe bună dreptate: „nu e în regulă să poți intra nelogat pe grupuri și să vezi membri". **MĂSURAT azi cu cheia anon, nu presupus** — din tabelele paginii de grup, un vizitator fără cont citește: `grup_membri` **34 de rânduri** (toate grupurile: cine, în ce grup, cu ce rol, ce status, din ce dată). Restul întorc 0: `grup_checklist`, `grup_checklist_notes`, `grup_checklist_files`, `grup_anunturi`, `grup_teren_comments`, `grup_invitations`, `grup_kick_ballots`, `grup_kick_votes` — ⚠️ dar **0 poate însemna și „tabelă goală", nu doar „RLS blochează"**; se confirmă sigur comparând cu o citire logat (relevant mai ales pentru jurnalul de progres, semnalat ca public în memoria `securitate-jurnal-progres-grupuri`). **Interfața nu minte, doar e incompletă:** cardul de grup pentru nelogat n-are buton „Vezi", ci „Creează cont pentru detalii" (`js/grupuri.js:435`) — dar `grup-details.html?id=…` rămâne accesibilă direct pe URL, și oricum datele se iau direct din API. **⚠️ CAPCANA REPARAȚIEI:** dacă îi tai lui `anon` accesul la `grup_membri`, se strică numărul de membri de pe `/grupuri.html` (`3/12`), care se calculează chiar din tabela aia. Deci nu e o singură politică RLS, ci **politică + o cale prin care numărul să vină de pe server** (un view cu `membri_count`, pe modelul lui `profiles_public`). **DECIZIE DE PRODUS ÎNAINTE DE COD, a lui Lucian:** grupuri complet private (public rămâne doar vitrina: nume, zonă, descriere, număr de membri) SAU lista de membri vizibilă doar celor logați (ajută la recrutare: „vezi cine e deja înăuntru"). **Zonă de RLS pe date reale — SQL-ul îl rulează Lucian manual.**

<details><summary>✅ ÎNCHIS pe 1 august — expunerea publică a emailurilor (păstrat pentru context)</summary>

🔴 **TASK PRIORITAR (de la 31 iulie) — ÎNCHIDE EXPUNEREA PUBLICĂ A EMAILURILOR: FAZA 2 (cod) + FAZA 3 (revocare).** Contextul complet e în intrarea de 31 iulie din header. Pe scurt: `public.profiles` e citibilă anonim în întregime (80 de rânduri cu email, telefon, nume real, notițe), din cauza politicii `profiles_select_all` (`qual = true`, rol `public`). **FAZA 1 E DEJA RULATĂ** — view-ul `profiles_public` există (rezultat `80 / 13 / 1`). **GAURA E ÎNCĂ DESCHISĂ până se face restul.** **FAZA 2 — 3 MODIFICĂRI DE COD, exact astea:** (1) `frontend/js/profile-view-new.js:90` — `select('*')` pe `profiles` → citește din `profiles_public`; ramura „profilul meu" (`isOwnProfile`) și cea de superadmin rămân pe `profiles`, altfel se pierd `notes`/`phone`/`age` de pe propriul profil; (2) `frontend/grup-details.html:1385` — scoate `email` din select-ul pentru cererile pending; (3) `frontend/grup-details.html:1406` — scoate `email` din select-ul pentru membri. **EFECT SECUNDAR DE ȘTIUT ȘI DE ACCEPTAT CONȘTIENT:** `displayName()` (`grup-details.html:3720`) cade pe partea din față a emailului când lipsește pseudonimul, deci membrii cu profil necompletat vor apărea ca **„Utilizator"** în loc de „radulesc". Dat fiind că oricum vrem să blocăm join-ul fără profil completat, e probabil mai corect așa. **APOI deploy din cPanel** (nu Render — vezi memoria `deployment-cpanel-not-render`). **FAZA 3, ABIA DUPĂ CE CODUL E LIVE:** rulează `db_schema/securitate-profiles/3-revoca-coloanele-sensibile.sql` (revocă `SELECT` pe tabelă de la `anon`, îi dă înapoi doar 20 de coloane nevinovate). **⚠️ ORDINEA E OBLIGATORIE:** view (făcut) → cod pe cPanel → revocare. Inversată, paginile publice rămân fără date și arată gol. **VERIFICAREA FINALĂ:** `GET /rest/v1/profiles?select=email&limit=5` cu cheia anon din `frontend/js/supabase-config.js` trebuie să răspundă cu eroare „permission denied for column email"; înainte de reparație răspundea `HTTP 206` cu `Content-Range: 0-4/80` și emailuri reale. Fișierul de SQL are proba și în variantă `SET LOCAL role anon`. **CE RĂMÂNE NEÎNCHIS CHIAR ȘI DUPĂ:** rolul `authenticated` citește în continuare toate emailurile — oricine își face cont ajunge la ele. Închiderea cere ca `notify-admins` să primească `user_id`-uri și să rezolve emailurile pe server cu service role, pentru că azi paginile de grup adună emailurile în browser. **Sesiune separată, e lanțul de notificări.** **⚠️ ZONĂ DE DREPTURI PE BAZA DE DATE REALĂ, cu 80 de utilizatori reali — se lucrează cu aprobare pas cu pas, SQL-ul îl rulează Lucian manual.**

**REZULTAT (1 august): FĂCUT TOT, verificat din exterior cu cheia anon — vezi intrarea din capul fișierului. Commituri `76bf2ed` + `6b195a9`.** Singura abatere de la planul de mai sus: regula finală e „nelogat → view, logat → tabelă", nu „doar propriul profil + superadmin pe tabelă", fiindcă varianta strictă rupea invitațiile din pagina de profil, emailul de aprobare a cererilor și anunțul cu linkul de WhatsApp.

</details>

🟡 **TASK CONEX (de la 31 iulie) — BLOCHEAZĂ JOIN-UL LA GRUP FĂRĂ PROFIL COMPLETAT.** Decizie de produs luată de Lucian: cine nu și-a completat profilul e ca și cum nu s-ar fi înregistrat, doar și-a confirmat un mail. **Pragul NU trebuie inventat** — formularul cere deja pseudonim + camere + suprafață + oraș (`required` în `profile-edit-new.html`) + ≥1 zonă (`profile-edit-new.js:587`) + ≥1 tag (`:592`). **DE FĂCUT:** o funcție SQL `profil_complet(uuid) returns boolean` care codifică cele șase condiții într-un singur loc, apoi (1) **politică RLS pe `grup_membri`** care refuză `INSERT` dacă `profil_complet(auth.uid())` e fals — **asta e singura barieră reală**, pentru că oricine poate insera direct prin API, ocolind pagina; (2) butonul din `grup-details.html` înlocuit cu „Completează-ți profilul ca să te poți alătura" + link, ca omul să înțeleagă de ce nu poate; (3) aceeași verificare în `accept-invite.html`, a doua cale prin care se creează apartenență. **⚠️ DE VERIFICAT:** aprobarea unei cereri de către admin trece tot prin `grup_membri` — asigură-te că un `UPDATE` de status nu cade sub aceeași politică și nu blochează adminul. **DECIZIE ÎNCĂ NELUATĂ:** ce se întâmplă cu `radulesc@gmail.com`, singurul membru de grup cu profil necompletat din platformă (rămâne cu etichetă / e ascuns până completează / iese cu email de înștiințare). **DE ȚINUT MINTE:** bariera singură nu rezolvă pâlnia — 16 conturi sunt goale pentru că înregistrarea doar creează contul și redirecționează spre `profile-edit-new.html?welcome=1`, iar oamenii nu duc treaba la capăt.

🔵 **TASK PRIORITAR (de la 30 iulie) — PROBA PE VIU A WEBHOOK-ULUI OBLIO: O COMANDĂ REALĂ DE 99 RON, PLĂTITĂ DE LUCIAN.** Mecanismul e reparat, deployat și înregistrat (vezi intrarea de 30 iulie din header). **Ce NU știm încă, și de asta e nevoie de probă:** **(a)** dacă Oblio chiar ne apelează acum; **(b)** ce formă are payload-ul lui — **Oblio NU o documentează nicăieri**, deci `detectPaid` e construit pe presupuneri; **(c)** dacă emailul „Comandă confirmată" pleacă efectiv — **acel cod nu a rulat niciodată cu succes**, nici la prima comandă reală (webhook neapelat), nici la reparația manuală (făcută prin SQL tocmai ca să NU trimită email). **CUM SE FACE:** comandă normală de analiză preliminară de pe site, plătită cu cardul, 99 RON reali (se autofacturează). **APOI, ÎN ORDINE:** (1) Supabase → Edge Functions → `oblio-webhook` → **Logs** — acolo apare linia `Oblio webhook [topic]:` cu payload-ul brut, primele 2000 de caractere; **copiază-l integral, e singura sursă de adevăr despre forma lui**; caută și linia `Comanda ... găsită prin ...`, care spune pe care dintre cele trei trepte de lookup a prins; (2) verifică în DB că a trecut pe `paid` cu `paid_at` corect și cu seria facturii fiscale; (3) verifică în `comenzi_analize_log` fluxul complet (`netopia_callback_received` + `status_changed` + `email_user_sent` + `email_admin_sent`); (4) verifică emailul primit efectiv pe adresa clientului + notificarea pe Slack `#app_events`. **DACĂ NU VINE NIMIC:** logurile funcției sunt goale ⇒ Oblio nu a apelat — verifică `node scripts/oblio-webhook/oblio-webhooks.mjs list` (topicurile trebuie să fie acolo) și ia în calcul că `Collect/Inserted` s-ar putea declanșa doar la încasări înregistrate manual, nu la cele venite prin Netopia; atunci plasa e `Invoice/SaveDraft`. **DACĂ VINE DAR NU MARCHEAZĂ `paid`:** payload-ul are altă formă decât presupunem — corecția e în `detectPaid`, câteva minute, dar **e zonă de plăți**. **DACĂ MARCHEAZĂ DAR NU PLEACĂ EMAILUL:** caută `email_failed` în `comenzi_analize_log`, mesajul conține răspunsul brut de la Resend. **⚠️ CAPCANĂ CARE NE POATE MUȘCA PESTE LUNI:** `oblio-webhook` se deployează **OBLIGATORIU** cu `--no-verify-jwt`. Un redeploy normal readuce blocajul, tăcut: Oblio e respins de poarta Supabase, nu apare nimic în loguri, iar plățile nu mai sunt confirmate — exact simptomul din 29 iulie. **NECOMISE la finalul sesiunii din 30 iulie:** corecția din `oblio-webhooks.mjs` (acceptă `201`, maschează cheia) + SQL-ul de reparare + acest handoff. Commit-ul `c7c0f1c` e local, **nepushat**.

<details><summary>Arhivă — diagnosticul din 29 iulie (rezolvat, păstrat pentru context)</summary>

🔴 **TASK PRIORITAR (29 iulie, seara) — PRIMA COMANDĂ REALĂ PLĂTITĂ NU A FOST MARCATĂ `paid`: OBLIO NU NE-A APELAT WEBHOOK-UL.** Diagnostic COMPLET, cauza izolată, **nimic încă reparat, ZERO cod atins**. **CE S-A ÎNTÂMPLAT:** comanda `APT-20260729-80d946` (Alin Tortolea, `atortolea@yahoo.com`, 0721292100, 99 RON, teren Str. Constantin Bosianu nr. 32, Sector 4). Clientul a plătit pe 29 iulie. **PARTEA LOR E PERFECTĂ:** Netopia arată `CONFIRMATĂ`, 99,00 RON, tranzacție `#249373379`; Oblio arată proforma `APT 0013` cu eticheta **`FACTURATA`**, factura fiscală generată **`LTFB0205`**, notă „Proforma a fost incasata cu Cardul prin Netopia. Tranzactie: #249373379 (29.07.2026)". **PARTEA NOASTRĂ N-A AFLAT NICIODATĂ:** în `comenzi_analize`, comanda e încă `status = 'pending_payment'`, `paid_at = null`; în `comenzi_analize_log` există **DOAR 3 rânduri, toate de la creare** (`order_created`, `oblio_invoice_request`, `oblio_invoice_response`, toate la 17:47) — **niciun `netopia_callback_received`**. Deci `oblio-webhook` **nu a fost apelat deloc**, nu a fost apelat și a eșuat. **CONSECINȚE REALE:** clientul a plătit și **nu a primit de la noi emailul „Comandă confirmată"** (a primit doar factura de la Oblio), Lucian n-a primit notificarea de plată, iar în panoul de admin comanda arată în continuare neplătită. **CAUZA CEA MAI PROBABILĂ (de confirmat prima):** webhook-ul **nu a fost niciodată înregistrat în Oblio**. Funcția `oblio-webhook` există în repo și e deployată, dar comentariul din capul fișierului zice „Configurare webhook în Oblio: se face printr-un singur API call (descris în README)" — **acel README NU EXISTĂ** în `supabase/functions/oblio-webhook/` (singurul fișier e `index.ts`) și înregistrarea nu apare făcută nicăieri în handoff. Oblio pur și simplu nu știe pe ce adresă să bată. **⚠️ AL DOILEA BUG, LATENT — DE REPARAT ÎN ACEEAȘI SESIUNE, ALTFEL FIXUL NU ȚINE:** `oblio-webhook` caută comanda cu `.eq('oblio_factura_serie', serie).eq('oblio_factura_numar', numar)`, iar la creare noi salvăm acolo **numărul PROFORMEI** (`APT` / `0013`). Dacă Oblio trimite webhook cu topicul `invoice`, payload-ul va purta seria/numărul **FACTURII** (`LTFB` / `0205`) → lookup-ul nu găsește nimic și funcția răspunde „Document not found in DB, ignored", tăcut, cu 200. Doar un webhook pe topicul `proforma` s-ar potrivi. De verificat ce shape trimite Oblio de fapt și de făcut lookup-ul tolerant la ambele. **DE FĂCUT ÎN SESIUNEA NOUĂ, ÎN ORDINE:** (1) verifică în Oblio (Setări → API / webhook-uri) dacă e înregistrat vreun webhook către `https://glbvbbgmcobtswwlktic.supabase.co/functions/v1/oblio-webhook`; (2) verifică logurile edge function `oblio-webhook` din Supabase (dacă sunt goale pe 29 iulie, confirmă că n-a fost apelat); (3) înregistrează webhook-ul + repară lookup-ul; (4) testează cu o comandă reală de 99 RON plătită de Lucian. **⚠️ ZONĂ DE PLĂȚI + configurare în serviciu extern (posibil chei API Oblio) — se lucrează cu aprobare pas cu pas.** **REPARAREA COMENZII ACESTEIA e separată de fix:** trebuie trecută manual pe `paid` (+ salvat `LTFB0205`), dar **un UPDATE în DB NU declanșează emailurile** — alea pleacă doar din interiorul funcției `oblio-webhook`, deci clientului trebuie scris manual. **Dovezi în `screenshots/20260729/`:** poza Netopia (`...211123.png`), poza Oblio cu `FACTURATA` + `LTFB0205` (`...211900.png`), CSV-urile cu starea comenzii și cu logul (`...(15).csv`, `...(16).csv` — gitignorate, conțin date personale).

</details>

**În rest, niciun task obligatoriu în desfășurare — alege unul dintre cele de pe orizont.** Ultimele sesiuni (flash header, curățare cod mort, clusterul analize, fix-uri terenuri, estetica emailuri v9) sunt închise și împinse.
⏭️ **PENDING DE DEPLOY (21–22 iulie) — SESIUNE GRUPURI (5 fix-uri UI):** commituri `4eda696` + `a8e4cd6` + `c7a8ddf` + `6c967f5` + `ec047c5`. Fișiere: `frontend/js/grup-detail.js`, `frontend/admin.html`, `frontend/js/utilizatori.js`, `frontend/ghid.html`, `frontend/grup-details.html`. **RĂMÂNE DE DEPLOYAT din cPanel.** După deploy + Ctrl+F5: (1) click pe numele dintr-o cerere de înscriere → profil; (2) admin pe mobil vede secțiunile; (3) lista utilizatori: reali întâi, exemple la coadă; (4) link „Vezi ghidul" pe pagina grupului. Curățarea grupurilor exemplu în DB e DEJA făcută de Lucian (nimic de rulat). Materialele pentru George/Alint (emailuri + regulament) sunt text în chat, nu în repo. Detalii în header-ul de sus.
⏭️ **PENDING DE DEPLOY (20 iulie) — COPY bandă Grupuri + FIX acord singular matching:** commituri `c3471ae` + `88e76b4`. Fișiere: `frontend/grupuri.html` (text bandă), `frontend/js/grupuri.js`, `frontend/js/utilizatori.js`, `frontend/index.html` (acord „o zonă comună / un interes comun"). **RĂMÂNE DE DEPLOYAT din cPanel** ca să apară live. După deploy + Ctrl+F5: verifică banda pe Grupuri + un card cu exact 1 zonă/1 interes comun. Detalii în header-ul de sus.
⏭️ **PENDING DE DEPLOY (18 iulie) — ANCHOR `#newsletter` pe homepage:** commit `1d57924`. `frontend/index.html` (+1/−1, doar `id="newsletter"` pe banda de newsletter). **RĂMÂNE DE DEPLOYAT din cPanel** ca `https://apartamentual.ro/#newsletter` să ducă direct la formularul de abonare (util la trimiterile manuale de newsletter). După deploy: test pe desktop + mobil. Detalii în header-ul de sus.
⏭️ **PENDING DE DEPLOY (2 iulie) — 2 POZE NOI ÎN CALEIDOSCOP:** commit `7518c33`. Cele 2 poze ale clădirii finalizate (`14-fatada-finalizata-strada.jpg` + `15-panorama-aeriana-finalizat.jpg`, pătrate 1200×1200) + `frontend/index.html` (manifest `s14`/`s15` + carduri galerie). **RĂMÂNE DE DEPLOYAT din cPanel** ca să apară live în caruselul de pe homepage. Detalii în header-ul de sus.
⏭️ **PENDING DE DEPLOY (2 iulie) — CORELARE COMANDĂ ↔ TEREN DE PE PLATFORMĂ:** `notify-admins` (`d3fb7e1`) e DEPLOYAT de Lucian; **RĂMÂNE DE DEPLOYAT din cPanel** `frontend/js/comanda-analiza.js` (`77674e6`) ca să apară live câmpul link blocat. Emailul de comandă distinge acum „🟢 Teren pe platformă" (cu link + ID teren) de „Link anunț teren (extern)". După deploy: test comandă de probă de pe un teren de pe platformă (fără plată). **Orizont opțional:** varianta robustă cu `teren_id` salvat separat (atinge zona de plăți). Detalii în header-ul de sus.
✅ **DEPLOYAT + VERIFICAT LIVE (1 iulie) — IDENTIFICARE TEREN nr. cadastral SAU adresă + LOGARE NOTIFICĂRI:** commituri `1aaff27` + `4aa7e54` LIVE. Migrația 031 rulată; edge functions `creeaza-proforma-oblio` + `notify-admins` + `oblio-webhook` deployate; frontend deployat din cPanel (formular + `analize.html` + `politica-retur.html`), confirmat de Lucian. Terenul se identifică acum prin nr. cadastral SAU adresă exactă (link ANCPI). `notify-admins` loghează din nou în `notification_log` (pagina admin „Notificări"). **RĂMÂNE (opțional):** test comandă cu doar adresa + verificare rânduri noi în `/admin-notificari.html`. Detalii în header-ul de sus. **Task COMPLET.**
✅ **DEPLOYAT + TESTAT LIVE (1 iulie) — FIX număr cadastral la comenzile de analiză:** commit `96ce34c` LIVE. Migrația 030 rulată; edge functions `creeaza-proforma-oblio` + `notify-admins` + `oblio-webhook` deployate. Emailul de comandă arată acum „Nr. cadastral teren" + „Link anunț teren". Test comandă reală confirmat de Lucian. **Task COMPLET** — nimic de făcut. Detalii în header-ul de sus.
✅ **DEPLOYAT + VERIFICAT LIVE (1 iulie) — PLAUSIBLE ANALYTICS pe tot site-ul:** commit `23e1c35` LIVE pe `apartamentual.ro` (verificat pe `/`, `/ce-este/`, `/servicii.html`, `/admin.html`, `/terenuri.html`; JS Plausible HTTP 200). Scriptul e în `<head>`-ul tuturor celor 52 de pagini din `frontend/`, fără cookies → fără banner. **RĂMÂNE doar:** confirmă vizitele în dashboard-ul Plausible. Detalii în header-ul de sus.
✅ **DEPLOYAT + VERIFICAT LIVE (29 iunie) — POLITICA DE LIVRARE (Netopia) + microcopy:** commiturile `e221809` + `8dede1a` + `77c46e3` + `9e828b9` + `a1cce44` sunt LIVE pe `apartamentual.ro`, confirmat: pagina `politica-livrare.html` completă (7 secțiuni + J `J2007012417402`) + link „Politica de livrare" în footer pe homepage și în `footer.js` live (toate paginile interne). **RĂMÂNE doar:** în panoul Netopia bifează „Politică de livrare comandă" + indică URL `https://apartamentual.ro/politica-livrare.html`. Detalii în header-ul de sus.
✅ **CERERE PARTENERIAT — REPARAT (25 iunie), commit `7a05bac` PUSHAT.** SQL RLS (migrația 025) rulat de Lucian; edge function `notify-admins` redeployată. **⏭️ RĂMÂNE DE DEPLOYAT din cPanel** doar `frontend/devino-partener.html` (intră oricum la următorul deploy, încarcă tot folderul). După deploy: test cerere NELOGAT (vezi nota din header). TODO opțional: dacă se vrea upload logo direct de la aplicant, decomentează blocul storage din migrația `025` și rulează-l separat.
✅ **DEPLOYAT + VERIFICAT LIVE (25 iunie):** commit `bf0e889` (reîncadrare exemple „Exemplu" în loc de „DEMO" + bandă onestă + card de recrutare pe Utilizatori și Grupuri) — LIVE pe `apartamentual.ro`, confirmat vizual de Lucian (nelogat/logat/mobil OK).
✅ **DEPLOYAT + VERIFICAT LIVE (25 iunie):** commit `97d6367` (revenire J Reg. Comerțului la `J40/12417/2007` + placeholdere neutre în formularul comanda-analiza) — LIVE, confirmat: footer + `termeni`/`gdpr`/`politica-retur` arată `J40/12417/2007`, zero apariții ale J-ului nou; placeholdere `ex: J40/1234/2020` / `ex: RO12345678`.
⏭️ **PENDING DE DEPLOY (24 iunie):** commiturile `f46ebab` + `6020489` + `2e5ac5b` + `da9a44f` + `759085d` (titlu hero, banda „serialul", SHARE + OPEN GRAPH, card episod 0) sunt **pe `main`, nedeployate** (deploy manual cPanel). După deploy: testează previzualizările link în Facebook Debugger (confirmă că imaginea OG nu dă 404) + butoanele de share pe articol/teren + cardul episod 0 pe homepage. Detalii complete în header-ul de sus.
✅ **NEWSLETTER — COMPLET ȘI LIVE (23 iunie), testat de Lucian.** SQL 023+024 rulat; toate edge functions deployate (newsletter-subscribe, newsletter-confirm `--no-verify-jwt`, notify-admins, newsletter-unsubscribe); frontend live. Funcționează: abonare (bandă homepage + footer global), double opt-in pe email, pagina de confirmare cu mesaj cont vs. fără cont, notificare Slack + email la `apartamentual@ltfbstudio.ro`, admin `/admin-newsletter.html` (listă + filtre + export CSV + dezabonare care marchează unsubscribed în Supabase ȘI Resend). Ultim fix (`21f98c0`): brandul din emailul de confirmare are `translate="no"`/`notranslate` ca Gmail să nu mai traducă „apartamenTUal/by LTFB Studio" în „apartamentul tău/de studioul LTFB" (rezolvat pe Gmail web; pe clienți rari care ignoră atributul ar mai putea apărea tradus — fallback posibil: logo ca imagine). **TODO viitor (Faza 2, neînceput):** webhook Resend `contact.unsubscribed` → oglindește în Supabase dezabonările făcute de user din footer-ul broadcastului; segmentare pe `zone_interest`.
⚠️ **PENDING DE DEPLOY (15 iunie):** commit `a24466e` (hero pe roluri + bară apartamente) e **doar local pe `main`, nepushat și nedeployat**. La următorul deploy cPanel intră și el. De testat stările după deploy (vezi nota din header). **Task amânat conex:** dashboard dedicat pentru „utilizator ÎN grup" (acum face fallback la Varianta A) — de construit separat când se decide ce conține.
🔴 **SIMPTOM DESCHIS (15 iunie, de reluat după restart):** după primul deploy, hero-ul nou „încă nu se încarcă". Lucian repornește și reverificăm. Piste de investigat în sesiunea următoare, în ordine: (1) **a ajuns commit-ul `a24466e` în deploy?** — push pe GitHub + „Update from Remote" + „Deploy HEAD Commit" în cPanel; verifică în sursa paginii live că există `<div class="hero-stage">` și scriptul „HERO PE ROLURI"; (2) **cache** — hard refresh (Ctrl+F5) / cache cPanel/CDN; (3) dacă HTML-ul nou e prezent dar varianta nu comută: deschide consola pe homepage logat și caută warning-urile `hero ...` (auth/A/B) — posibil `sb` nu devine disponibil sau interogarea pică. Nelogat trebuie oricum să arate hero-ul de marketing (default vizibil), deci dacă NICI marketing-ul nu apare, e problemă de deploy/cache, nu de JS.
✅ **DEPLOY cPanel (10 iunie):** Lucian a făcut deploy din cPanel — favicon-ul nou (`1ebf868`) e LIVE, confirmat. Un deploy manual din cPanel încarcă tot folderul frontend, deci commit-urile pending anterioare (a8a149a + 0cd4096 + 7d460fc + bcc097f + f6152f6 + docs) au intrat foarte probabil în același deploy — **de confirmat vizual la nevoie**, nu mai e blocant.
✅ **EDGE FUNCTIONS DEPLOYATE** (commit `6fdca71`, estetica emailuri v9) — Lucian a rulat deploy 9 iunie, fără erori. (Deploy edge functions: din `C:\Users\lucia\supabase`, `npx supabase functions deploy <nume>` — **fără Docker**, CLI 2.x; vezi memoria `edge-functions-deploy-no-docker`.)
Recomandate, în ordinea valorii:

1. **✅ Aliniere v9 — ÎNCHEIATĂ.** Tot site-ul e migrat. Ultimul cluster, „analize" (`analize`, `analiza-simplificata`, `analiza-detaliata`, `termeni-analize`), a fost migrat în sesiunea din 8 iunie (commits 0cd4096 + 7d460fc) — reskin strict vizual (paleta rece slate+portocaliu `#f97316` → v9 crem/ink/teracotă `#c2604a`, DM Sans → Mona Sans, nav.css → apartamentual-v9.css), fără atingere de JS/preț/flux. (DONE complet: `ce-este/*`, `povestea-noastra`, `parteneri`, `gdpr`, `termeni`, `politica-retur`, `contact`, `register`, `consultanta`, `devino-partener`, `ghid`, `news`, `terenuri`, `utilizatori`, `teren-details`, `profile-view-new`, `comanda-analiza`, `grupuri`+`grup-nou`+`grup-edit`+`grup-details`, **`analize`+`analiza-simplificata`+`analiza-detaliata`+`termeni-analize`**.)

   **⏭️ DE TESTAT după deploy:** paginile din clusterul analize ating zona „analize/plăți". De verificat vizual modalele (ofertă analiză detaliată pe `analize`/`analiza-detaliata`, lightbox-ul de imagini pe paginile-exemplu) și un flux de comandă real. Convenție v9 aplicată: buton primar pe hero ÎNTUNECAT = alb + text ink (ink-ul ar dispărea pe fundal închis); butoanele pe fundal deschis = ink. Notă: rămâne un TODO vechi separat „Patch analiza-simplificata.html (înlocuire pop-up vechi cu link la /comanda-analiza.html)" — decizie de conținut, NErezolvată în reskin.

   **🔑 LECȚIE din clusterul grupuri (commit 0c8a727):** spre deosebire de terenuri/utilizatori, `grupuri.css` (838 linii) NU folosea variabile CSS — avea paleta **hardcodată** (slate `#475569` + coral `#f97316`). Migrarea = **mapare culoare-cu-culoare prin sed** (slate→neutre v9, coral→teracotă `#c2604a`), aplicată identic pe `grupuri.css` ȘI pe blocurile `<style>` inline din cele 4 pagini (aceeași paletă). **Butoanele pline → ink** (`#1a1a1a`, hover `#333`) prin edituri țintite DUPĂ sed (sed le făcuse teracotă); teracota rămâne doar accent (badge/chip/count). Cele 4 faze de proiect din grup-details → tonuri aprobate (ardezie/verde-soft `#2f8f5b`/teracotă/cărămiziu), ca diagrama cum-functioneaza. grup-details (4345 linii) s-a făcut în aceeași sesiune cu restul fără probleme de context (maparea sed e ieftină, nu citești fiecare linie).

   **🔑 PATTERN-UL DE MIGRARE pt. pagini-listă cu CSS extern mare (folosit pe terenuri + utilizatori, commit `aaedeba`):** fișierele astea își țin stilul prin **variabile CSS** (`--slate-*` scală albastru-rece, `--coral*` portocaliu). Reskin = **redefinești VALORILE variabilelor în `:root`** + corectezi cele câteva culori hardcodate. NU atingi niciun nume de clasă, structură DOM, JS sau fetch Supabase. Mapare agreată cu Lucian: scala slate → **neutre calde v9** (slate-50=`#faf8f3` … slate-800=`#1a1a1a` ink); coral portocaliu → **teracotă `#c2604a`** (accent: iconițe/badge/like/filtre/focus); **butoanele pline rămân ink** (override separat, nu prin variabilă, fiindcă variabila coral e și accent); font `'DM Sans'` → Mona Sans; `#navigation{margin-bottom:0}` pt. hero full-bleed dark; verdele respins `#16a34a` → `#2f8f5b`. În head: `css/apartamentual-v9.css` se pune **ÎNAINTE** de CSS-ul paginii (ca pagina să câștige la `body`/`.hero-title`/`#navigation`). footer.js/nav.js își aduc singure stilul (independent de v9 css).

   Strategie agreată: **C (hibrid)** — fundație CSS comună + rafinare pagină cu pagină. Un task = o sesiune; ia 2-3 pagini odată, nu toate. **Pattern-uri refolosibile pt. celelalte tipuri:** (a) pagini-document Tailwind text lung → `css/document.css` (clasa `.legal-doc`); (b) pagini Tailwind cu accente colorate → bloc `<style>` în-pagină scoped sub `.v9-page` pe `<main>` (vezi `contact`/`register`); (c) pagini cu `<style>` propriu → reskin în-pagină (vezi `consultanta`/`politica-retur`); (d) **pagini-listă cu CSS extern mare → redefinire variabile (vezi terenuri/utilizatori).** **`comanda-analiza` atinge zona de plăți — de lăsat la final, cu grijă separată.**
2. **Automatizare deploy cPanel** — acum Lucian dă manual „Update from Remote" + „Deploy HEAD Commit" după fiecare push. De automatizat (webhook GitHub→cPanel).

**Notă legată de nav.css (descoperită 6 iunie):** `nav.css` NU e cod mort cât timp paginile nu sunt migrate la v9 — el rezervă înălțimea header-ului (`#navigation { min-height:69px }`) ȘI încarcă fontul DM Sans pe ~25 de pagini. Pe măsură ce o pagină primește `apartamentual-v9.css`, ABIA ATUNCI se poate scoate `<link href="/nav.css">` de pe ea. Deci curățarea referințelor nav.css merge mână în mână cu migrarea v9, nu separat.

---

## Status general

Platformă activă pe https://apartamentual.ro, hostată pe Render, backend Supabase.
Pilot real în curs: **Județului Housing** (București, 5 familii, aproape de mutare).
Lansare publică iminentă, dar nu există dată fixă.

---

## Echipă

- **Lucian Marius Luța** (eu) — co-fondator LTFB Studio și ApartamenTUal
- **Liviu Fabian** — co-fondator LTFB Studio și ApartamenTUal
- **Ina German** — în discuție pentru asociere. Arhitect cu experiență tech și comunicare. **Nu formalizată încă.**

Împărțirea rolurilor între noi trei nu e stabilită. Nu presupune.

---

## Ce e finalizat și stabil

- Sistem de notificări (21 evenimente)
- Flux registrare (signup → verificare email → profile → homepage)
- Admin panel complet
- Integrare Oblio + Netopia (edge functions deployate)
- Database curat, migrațiile prin 029
- **Homepage v9 integrat și LIVE** (28 mai) — design + texte noi (Claude Design), font Mona Sans, portrete reale + carusel Strada Județului, footer cu secțiunile reale (Navigare/Legal) + bara firmă/Netopia/ANPC, FAQ real complet, timelapse în buclă, video cu buton play sub față, preview News din Supabase. Backup vechi în `_archive/index-pre-v9.html`. Detalii: `HANDOFF-integrare-homepage-v9.md`.
- **Header + footer v9 unificat pe toate paginile interioare** (2 iunie, commit `864240c`) — `frontend/js/nav.js` și `frontend/js/footer.js` rescrise integral în stil v9 (Mona Sans, fundal crem, badge BETA, „Povestea noastră" în nav și footer, „Acasă" scoasă din ambele — click pe logo duce acasă). Logica auth (login, avatar+dropdown, mobile menu, blocare cont suspendat) păstrată 1:1. Banda firmă LTFB + Netopia + ANPC păstrată integral în footer. Butoanele flotante „Cere consultanță" și scroll-to-top restilizate v9. CTA-urile page-specifice „Propune teren" / „Creează grup" scoase din header (vor reapărea mai vizibil în corpul paginilor). Pe `parteneri.html` s-a descoperit că `fab-consultanta.js` era de fapt o copie veche a nav.js mascată sub alt nume — scos din pagină. Pe `povestea-noastra.html` lipsea `<script src="js/nav.js">` — adăugat.
- **Polish header v9** (2–3 iunie, commits `d3f72db`, `bf91ebf`, `959953b`, `30b5885`, `0097cb6`):
  - Scos badge BETA de peste tot.
  - Redenumită clasa internă `logo-text` → `logo-mark` (eliminat conflict cu `utilizatori.css .logo-text { font-size:1.5rem }` care făcea logo-ul mai mare doar pe pagina utilizatori).
  - Fix flash de auth: ambele variante (avatar + login) ascunse inițial; `checkAuthState` decide ce afișează → fără mai apare „Intră în cont" → avatar la fiecare load când userul e logat.
  - Fix flash de header vechi pe `utilizatori.html` și `grupuri.html`: nav-ul vechi inline (`<nav class="navbar">` / `<nav class="main-nav">`) eliminat complet, înlocuit cu `<div id="navigation"></div>`. Mai rămân de curățat astfel: `terenuri.html`, `grup-details.html`, `grup-edit.html`, `grup-nou.html`.
  - Homepage trecut la `<div id="navigation"></div>` + Supabase SDK + supabase-config.js + login-modal.js + nav.js (toate scripturile la finalul body, în aceeași ordine ca pe paginile interioare). Header-ul de pe homepage acum identic poziționat cu restul (banda crem full-width).
  - Auth-aware nav pe homepage: când nelogat → „Intră în cont" (link text) + „Creează cont" (buton chenar); când logat → avatar+dropdown.
  - CTA „Cum funcționează" din hero homepage → link la subpagina `/ce-este/cum-functioneaza.html` (înainte trimitea la `/ce-este/#cum-functioneaza`, hash spre o secțiune).
- **Aliniere corp pagini la v9 — secțiunea `ce-este/` (3 iunie)** — pas 1 din migrarea graficii v9 pe restul site-ului. Creat `frontend/css/apartamentual-v9.css` = fundația CSS comună pentru **corpul** paginilor (tokenuri `:root`, tipografie, butoane `.cta-primary/.cta-secondary`, `.section`, `.card`, `.avantaje`, `.steps`, `.faq`, `.cta-final`, footer), extrasă 1:1 din homepage. Migrate toate cele 6 pagini din `ce-este/`:
  - `ce-este/index.html` (hub) rescris complet în v9 (hero, carduri-hub crem, `.avantaje`, `.cta-final`); scos Tailwind + styles.css + stilurile slate/portocaliu.
  - Celelalte 5 (`prezentare-generala`, `cum-functioneaza`, `exemple-europa`, `legislatia-romania`, `testimoniale`): `ce-este/ce-este.css` rescris în paletă v9 (hero crem, secțiuni soft, tabele/tip-uri/stat-uri ink) + strat override scoped `.ce-content` pentru rămășițe Tailwind (bg-orange/amber, buton portocaliu, numere colorate). Head curățat: scos `/nav.css`, adăugat `apartamentual-v9.css`, scos `bg-gray-50/text-gray-900` de pe body. Tailwind + styles.css **păstrate** (markup-ul depinde de ele). Diagrama de proces din `cum-functioneaza` recolorată monocrom ink.
  - **Conținut**: corectate 3 promisiuni de procent de economie din `testimoniale.html` („25%", „20% economie") → mesaj conform („toți banii rămân în apartament"). **REZOLVAT (5 iunie)**: proiectul fabricat („Parcul Circului" + stats inventate) înlocuit cu testimonialul real Tiberiu M. (Județului Housing) — vezi blocul „Sesiune polish v9 (5 iunie)" mai jos.
  - Strategie agreată pentru restul: **C (hibrid)** = fundație CSS comună + rafinare pagină cu pagină.
- **Aliniere corp pagini la v9 — povestea-noastra + parteneri (3 iunie)** — pasul 2. Ambele migrate prin reskin în-pagină: scos DM Sans + `nav.css`, legat `apartamentual-v9.css`, blocul `<style>` propriu rescris în paletă v9 (crem/ink), păstrând structura HTML + layout-ul specific + logica.
  - `povestea-noastra.html` (commit `7c17f24`): hero video pe ink, articol long-form, figuri, autori; butoane final → `.cta-primary/.cta-secondary`.
  - `parteneri.html` (commit `175fd3a`): ATENȚIE — cardurile sunt generate din JS, deci numele de clase (`partner-card`, `badge-verified/featured`, `btn-website/email`, `cat-pill`…) NU se schimbă, doar valorile CSS. Hero + banner CTA pe ink.
  - Pattern reutilizabil pt. pagini cu hero full-bleed: `#navigation{ margin-bottom:0 }` (anulează spațiul rezervat din v9).
- **Culori ce-este: pasteluri, NU monocrom (3 iunie, commit `b215ada`)** — Lucian a cerut explicit culoare (pală) pe carduri și pe diagrama fazelor, NU monocrom ink. Schimbat:
  - `ce-este.css`: override-ul Tailwind `bg-*-50` din `.ce-content` (care aplatiza cardurile la crem) → **pasteluri soft din paleta de accent v9** (ardezie `#e9eef3`, salvie `#e9f0ea`, ocru `#f5efe1`, mov pal `#f0eaef`, teracotă `#f6ebe6`, cărămiziu `#f6eae9`), text negru lizibil.
  - `cum-functioneaza.html`: headerele fazelor 1-4 din gradient verde/mov/gri-închis → pasteluri cu text ink; puncte + bara de timeline în tonuri saturate (`#5a7196`/`#b8965c`/`#c2604a`/`#8f5a48`); caseta „Te muți în casa ta!" din fundal închis → pastel cu text ink. (Înlocuiește nota mai veche „diagrama recolorată monocrom ink".)
  - **Regula de culoare a lui Lucian**: fundalurile pot fi colorate dar PALE, ca textul negru să rămână lizibil; NU negru/gri-închis cu text negru; a respins verde `#16a34a`, mov `#7c3aed`, gri-închis pe headere.
- **Toate cele de mai sus sunt pe `main`** (PR #1 fuzionat, commit merge `f098320`). Rămâne doar deploy-ul din cPanel (checkout `main` → Update from Remote → Deploy HEAD Commit).
- **Fix teren `pending` nu se încărca din linkul de email de aprobare (9 iunie) — pe `main`, commit `f6152f6`, DE DEPLOYAT din cPanel**:
  - **Simptom**: superadminul primește email la propunerea unui teren; click pe linkul de vizualizare → „eroare la încărcarea terenului — cannot coerce the result to a single JSON object".
  - **Cauză**: terenul abia propus are `status = 'pending'`. RLS pe `terenuri` (în `000_init.sql`): nelogații (anon, linia 830) văd doar `status='active'`; autentificații (linia 842) văd toate ne-șterse, inclusiv pending. `teren-details.js` interoga terenul cu `.single()` **înainte** să se restaureze sesiunea din localStorage (race condition) → query rula ca anonim → terenul pending invizibil → 0 rânduri → `.single()` arunca eroarea criptică.
  - **Fix**: `await supabase.auth.getSession()` înainte de query (tokenul de login e atașat → adminul logat vede pending); `.single()` → `.maybeSingle()` (null pe 0 rânduri, fără eroare); mesaj clar dacă tot nu apare („Acest teren nu este vizibil public… Autentifică-te"). NEatins: RLS, date, alte query-uri, formular, plăți.
  - **⏭️ DE TESTAT după deploy**: propune un teren nou → din emailul de aprobare, fiind logat ca superadmin, deschide linkul → terenul trebuie să se încarce.
- **Bugfix galerie imagini teren (9 iunie) — pe `main`, commit `c7c3cb4`, DEPLOYAT din cPanel (LIVE)**:
  - **Simptom raportat de un user**: la „Propune teren" putea încărca mai multe imagini, dar când apărea postarea se vedea doar una.
  - **Cauză**: upload-ul era OK — `terrain-form-v2.js` salvează toate pozele în coloana `image_urls` (array) + prima în `image_url` (legacy). Problema era la **afișare**: `teren-details.js` citea doar `image_url` (o singură poză) și afișa un singur `<img>`. Lista (`terenuri.js`) folosea deja corect `image_urls[0]` (o poză per card = intenționat).
  - **Fix (strict vizual, 2 fișiere)**: `getImageUrl` → `getImageUrls` (citește array-ul `image_urls`, fallback pe `image_url` pt. terenuri vechi). Adăugat galerie: imagine principală + miniaturi sub ea (`#teren-thumbnails`, apar doar la 2+ poze), click pe miniatură schimbă imaginea mare (inel teracotă pe cea activă), modal navigabil (săgeți `‹`/`›` pe ecran + taste ←/→ + contor „1 / 4", wrap circular). **NEatins**: upload, formular, DB, RLS, plăți, edge functions. Un teren cu o singură poză arată exact ca înainte.
  - **⏭️ DE TESTAT după deploy**: deschide un teren cu mai multe poze → verifică miniaturile + navigarea în modal; verifică și un teren vechi (o poză) că arată neschimbat.
- **Fix upload imagini — nume unic per fișier (9 iunie) — pe `main`, commit `bcc097f`, NEDEPLOYAT încă din cPanel**:
  - **Simptom**: după fix-ul galeriei, pozele se repetau la fiecare teren (aceeași imagine de N ori), deși user-ul postase poze diferite.
  - **Cauză (în upload, exista dinainte)**: `terrain-form-v2.js` încărca pozele în paralel (`Promise.all`) cu nume `userId/${Date.now()}.ext`. În aceeași milisecundă toate primeau **același nume** → se suprascriau în storage (rămânea fizic o singură poză) și `getPublicUrl` întorcea **același URL** pentru toate → `image_urls = [url, url, ...]`. Galeria afișa corect ce era în DB; problema era data coruptă la upload.
  - **Fix**: nume unic per fișier = `userId/${Date.now()}-${index}-${random}.ext` (index din map + sufix aleator). NEatins: validare, ordine poze, ce se scrie în image_urls/image_url, flux salvare, plăți, DB.
  - ⚠️ **Terenurile de test deja postate au pozele suprascrise fizic (pierdute)** — pt. ele a rămas o singură imagine reală; trebuie re-editate/re-încărcate. Terenurile NOI după deploy stochează corect toate pozele.
- **Estetica emailurilor aliniată la v9 (9 iunie) — pe `main`, commit `6fdca71`, EDGE FUNCTIONS NEDEPLOYATE**:
  - Emailurile foloseau încă paleta veche (slate rece + portocaliu `#f97316`, font DM Sans). Recolorate în v9: crem `#faf8f3`, ink `#1a1a1a`, neutre calde `#555`/`#8a8a8a`, borduri `#e8e3d8`, fundal detaliu `#f3f0e7`, accent teracotă `#c2604a`, font Mona Sans. **Butoanele CTA pline → ink** (`#1a1a1a`), teracota doar accent (nume-grup, linkuri, „TU" din logo) — convenția v9.
  - `notify-admins/index.ts` (zonă sigură): template central `buildEmailHtml` recolorat + 15 accente `#f97316`→`#c2604a` în formattere.
  - `oblio-webhook/index.ts` (⚠️ ZONĂ PLĂȚI): DOAR blocul HTML al emailului de confirmare comandă (hero ink-gradient, crem, teracotă). **NEatins**: logica Oblio/Netopia, `numeClient`, `order_id`, fluxul de salvare, fetch-urile.
  - **Notă onestă**: `'Mona Sans'` (ca și `'DM Sans'` înainte) NU se încarcă în majoritatea clienților de email → cade pe font de sistem; schimbarea vizibilă reală sunt **culorile**.
  - **NEACOPERIT**: emailurile de auth (signup/verificare/reset parolă) NU sunt în cod — sunt template-uri în dashboard Supabase (Auth → Email Templates), de editat separat dacă se dorește.
  - **⏭️ DEPLOY (manual, NU cPanel)**: din `C:\Users\lucia\supabase` cu Docker pornit → `npx supabase functions deploy notify-admins` + `npx supabase functions deploy oblio-webhook`. DE TESTAT după: o notificare reală (ex. invitație grup) + o comandă de test pe oblio-webhook (zonă plăți).
- **Sesiune v9 clusterul analize + fix-uri nav (8 iunie) — pe `main`, împinsă, NEDEPLOYAT încă din cPanel**:
  - `a8a149a` **Două fix-uri nav.** (1) Butonul „Profilul meu" pe `grupuri` + `terenuri` nu mergea: `grupuri.js`/`terenuri.js` aveau un `initNav()` vechi care atașa un AL DOILEA handler pe avatar — două `toggle('show')` se anulau, dropdown-ul se deschidea și se închidea instant. Scos `initNav()` (apel + funcție) + liniile redundante de nav din `checkAuth`; nav.js e singurul proprietar al meniului. (2) Flash-ul iconiței de login la refresh: `nav.js` aștepta bundle-ul Supabase + `getUser()` (rețea) înainte să afișeze starea → zona din dreapta clipea. Adăugat `quickAuthGuess()` (citește sincron tokenul `sb-…-auth-token` din localStorage) + `applyInitialAuthUI()` → starea corectă apare instant; `checkAuthState()` confirmă/corectează după. Doar citire localStorage, zero fetch nou.
  - `0cd4096` + `7d460fc` **Clusterul „analize" migrat la v9** (ULTIMUL cluster → aliniere v9 a site-ului ÎNCHEIATĂ). Reskin STRICT vizual pe `analize`, `termeni-analize`, `analiza-simplificata`, `analiza-detaliata`: head (DM Sans + nav.css → `css/apartamentual-v9.css` + Mona Sans), bloc `<style>` recolorat din paleta rece (slate `#0f172a`/`#1e293b`/`#475569`/… + portocaliu `#f97316`/`#ea580c`) în v9 (crem `#faf8f3`, ink `#1a1a1a`, neutre calde `#555`/`#6f6a61`/`#a8a094`/`#d6cfc1`/`#e8e3d8`, accent teracotă `#c2604a`/`#a84f3c`), hero gradient → `#1a1a1a→#333333`, theme-color → `#1a1a1a`. **Buton primar pe hero ÎNTUNECAT = alb + text ink** (ink-ul dispare pe fundal închis); butoanele pe fundal deschis = ink; teracota doar accent. Casetele CTA portocalii + modalele de ofertă → tinte teracotă (`#f6ebe6`/`#efd9d0`); lightbox-ul de imagini rămâne overlay negru. **NEatins:** tot JS-ul (modal ofertă/mailto, lightbox openModal/closeModal, propagare teren_id/teren_titlu), structura markup, prețurile/textele (promo „149→99 RON" 1:1). Verificat: 88=88 / 88≈86 substituții, zero linii de JS/href/preț schimbate.
  - **⏭️ DE TESTAT după deploy:** modalele (ofertă detaliată, lightbox) + un flux de comandă real pe paginile din analize/plăți.
- **Sesiune polish v9 (5 iunie) — totul pe `main`, împins, NEDEPLOYAT încă din cPanel**:
  - `7b1b452` Testimonial real **Tiberiu M.** (Județului Housing) în loc de cel fabricat („Parcul Circului" + stats inventate) în `ce-este/testimoniale.html`; atribuirea citatului aliniată și pe homepage (T.M. → Tiberiu M.) + formulare identică. (Notă: „Parcul Circului" ca **reper geografic** în `povestea-noastra.html` e legitim — terenul real de pe Str. Județului e lângă Parcul Circului; NU e fabricație.)
  - `1cc05ef` Scos cardul „Testimoniale" din hub-ul `ce-este/` + cele 2 cross-linkuri „Continuă să explorezi" (testimonialul unic nu justifică secțiune proeminentă). Pagina `testimoniale.html` rămâne accesibilă direct prin URL.
  - `39ef02d` „by LTFB Studio" apropiat de logo (gap 10→5px) + aliniat la baseline (în `nav.js`).
  - `71c87a6` Diagrama „Cronologia unui proiect" din `cum-functioneaza`: carduri **verticale** (1→2→3→4) în loc de zigzag cu săgeți, săgeți scoase, numere păstrate; culori per fază mai distincte (1 albastru `#e9eef3`, 2 verde `#e7f0e8`, 3 ocru `#f5efda`, 4 teracotă `#f3e3dc` — toate pale); bara „durată estimată" primește exact aceleași culori ca punctele/cardurile.
  - `ae63f5c` Banda de credibilitate de pe homepage (cele 3 fraze sub grafica cu pătrate) distribuită echidistant (`justify-content:space-between`), scoase separatoarele „·".
  - `1c438b9` **Header fix și identic pe toate paginile**: scoasă excepția de homepage din `nav.js` — zona din dreapta arată aceleași elemente peste tot pentru nelogați („Intră în cont" + „Creează cont"), cu lățime fixă (`min-width:200px`). Nu mai „sare" header-ul între homepage și restul, nici între logat/nelogat (cerculețul-avatar ocupă același spațiu).
  - `0233d86` Eliminată **linia dublă + saltul** header-ului la încărcare: scoasă linia-placeholder `box-shadow inset` de pe `#navigation` (în `apartamentual-v9.css` + `index.html`); `.site-nav` are acum **înălțime fixă 68px** (band = 69px) = spațiul rezervat, deci nu mai depinde de stare/font; rezervare mobil 77→69px.
  - `3c55677` `povestea-noastra`: adăugat SDK-ul Supabase (lipsea → `supabase-config.js` crăpa pe `window.supabase` undefined → `checkAuthState` nu rula → butoanele auth rămâneau ascunse). `terenuri`: nav vechi inline → `<div id="navigation">` (fără flash portocaliu „Propune teren").
  - `24af9c9` Același fix de flash pe `grup-details`, `grup-edit`, `grup-nou` (nav vechi `<nav class="main-nav">` inline → `<div id="navigation">`).
- **Sesiune flash header (5 iunie, seara) — totul pe `main`, împins, NEDEPLOYAT încă din cPanel**:
  - `fecf80c` **Font Awesome pe homepage**: `index.html` era SINGURA pagină din 41 care nu încărca Font Awesome → iconițele injectate de `nav.js` (`fa-user` pe avatar, `fa-arrow-up` pe scroll-to-top, plus dropdown profil + burger mobil) rămâneau invizibile (doar cerculețe goale). Adăugat `<link>`-ul 6.5.1 de pe cdnjs, identic cu restul paginilor.
  - `15f3397` **Eliminat flash-ul de header gol la refresh/navigare**: în `nav.js`, codul care desenează header-ul + butoanele flotante a fost mutat dintr-un handler `DOMContentLoaded` (care se declanșa abia după ce se descărcau TOATE scripturile, inclusiv bundle-ul mare Supabase) într-o funcție `initChrome()` apelată **imediat** (dacă `document.body` există deja — adevărat la finalul body-ului; altfel cade pe DOMContentLoaded). Pe homepage am reordonat și scripturile: `nav.js` primul, înaintea Supabase SDK → header-ul apare aproape instant; starea auth (avatar) se completează prin polling-ul existent după `sb`.
  - `e5246f7` Aceeași reordonare (`nav.js` primul) pe **9 pagini** care aveau Supabase + config + login + nav.js grupate la finalul body-ului: `grupuri`, `terenuri`, `utilizatori`, `analize`, `analiza-detaliata`, `analiza-simplificata`, `comanda-analiza`, `termeni-analize`, `ghid`.
  - ⏭️ **Rămâne** (vezi blocul de la începutul fișierului): paginile cu Supabase în `<head>` + `nav.js` singur la final — header-ul tot apare la finalul parsării body-ului. De abordat la începutul sesiunii următoare.
- **Sesiune v9 pagini legale (5 iunie) — pe `main`, commit `4bfabc9`, NEDEPLOYAT încă din cPanel**:
  - Migrate la design v9: **`gdpr.html`**, **`termeni.html`**, **`politica-retur.html`**. Niciun text juridic atins — doar straturi vizuale.
  - **Fișier nou `css/document.css`** (refolosibil): reskin v9 pentru paginile-document Tailwind, totul scoped sub clasa `.legal-doc` (carduri crem-soft, Mona Sans, linkuri ink). Rescrie utilitarele Tailwind colorate fără să atingă markup-ul: `.legal-doc .bg-blue-50/green-50/...` → pasteluri v9, `.legal-doc .border-blue-500/...` (cele 4 temeiuri GDPR) → cele 4 tonuri aprobate (ardezie `#5a7196`, ocru `#b8965c`, teracotă `#c2604a`, cărămiziu `#8f5a48`). Funcționează indiferent de ordinea față de Tailwind: `.legal-doc .clasă` (0-2-0) bate utilitarele (0-1-0).
  - `gdpr` + `termeni`: în head scos `/nav.css`, adăugat `apartamentual-v9.css` + `document.css`; pe body scos `bg-white text-gray-900` (preia crem/ink din v9); pe containerul `.max-w-4xl mx-auto` adăugat clasa `legal-doc`.
  - `politica-retur` (avea `<style>` propriu slate/portocaliu + DM Sans): reskin în-pagină ca povestea/parteneri — scos DM Sans + `nav.css`, adăugat `apartamentual-v9.css`, blocul `<style>` (clase `pr-*`) rescris în paletă v9 (hero pe ink, secțiuni crem-soft, portocaliul `#f97316` → ink/pasteluri, callout cu accent ink la stânga ca `.family-quote`), `#navigation{ margin-bottom:0 }` pt. hero full-bleed. Tailwind CDN rămâne încărcat dar nefolosit (doar `pr-*`) — curățare opțională ulterioară.

---

- **Sesiune v9 devino-partener/ghid/news (5 iunie) — pe `main`, commit `d249779`, NEDEPLOYAT încă din cPanel**:
  - Migrate la design v9, **doar straturi vizuale** (niciun JS / fetch Supabase / logică formular atinsă).
  - **🐛 BUG reparat — header/footer lipsă din placeholder cu id greșit**: `nav.js` injectează DOAR în `getElementById('navigation')`, `footer.js` DOAR în `getElementById('footer')`. `ghid` avea `<div id="nav">` → fără header; `news` avea `<nav id="main-nav">` → fără header + nu avea deloc `<div id="footer">` → fără footer. Reparat la ambele (`<div id="navigation">` + adăugat `<div id="footer">` la news).
  - `devino-partener` + `news` + `ghid` (toate aveau `<style>` propriu slate/portocaliu) → reskin în-pagină ca `consultanta`/`politica-retur`: hero pe ink, carduri/formulare crem-soft, accente portocaliu/albastru → ink, fundaluri colorate → pasteluri v9 aprobate. Iconițele celor 5 secțiuni din `ghid` + cele 4 faze din checklist mapate pe cele 4 tonuri aprobate (ardezie `#5a7196`, ocru `#b8965c`, teracotă `#c2604a`, cărămiziu `#8f5a48`). `nav.css` → `apartamentual-v9.css`; scos fontul DM Sans (devino, ghid).

- **Sesiune v9 pagini detaliu + comanda-analiza (5 iunie) — pe `main`, commits `c7fc462` + `04f0c1b`, împinse, NEDEPLOYAT încă din cPanel**:
  - Completarea firească a listelor: paginile de detaliu la care duceau erau încă pe stil vechi (inconsistent la click).
  - `teren-details.html` + `profile-view-new.html` (Tailwind + `styles.css`): pattern `.v9-page` (ca la contact/register) — head `nav.css`→`apartamentual-v9.css`, scos `bg-*/text-*` de pe body, clasă `v9-page` pe `<main>`, bloc `<style>` care rescrie utilitarele Tailwind colorate. Pe profil, badge-urile setate din JS (`bg-blue/purple/yellow/green-100`) → pasteluri v9 + text ink; roșul rămâne semantic. `teren-notes.css` (doar profil) recolorat. **Doar vizual** — niciun JS/Supabase/câmp atins. Commit `c7fc462`.
  - `comanda-analiza.html` (**ZONĂ PLĂȚI**, `<style>` propriu `.ord-*`): reskin STRICT vizual — DOAR blocul `<style>` + head. **NEatins**: JS plată Netopia/Oblio, ID-uri/name câmpuri, structură formular, prețuri/texte (promo „149→99 RON" lăsată 1:1). Coral→teracotă (accente), butoane→ink, slate→neutre calde. Commit `04f0c1b`. ⚠️ De testat fluxul de comandă după deploy.
  - Rămâne din fluxul „analize": `analize`, `analiza-simplificata`, `analiza-detaliata`, `termeni-analize` (toate DM Sans + nav.css + `<style>` propriu).
- **Sesiune v9 clusterul grupuri (8 iunie) — pe `main`, commit `0c8a727`, împins, NEDEPLOYAT încă din cPanel**:
  - Migrate `grupuri.html`, `grup-nou.html`, `grup-edit.html`, `grup-details.html` (4 pagini cuplate prin `grupuri.css`). **Doar straturi vizuale** — verificat automat că nicio linie modificată nu e altceva decât culoare/font/link (zero JS / fetch Supabase / structură DOM / nume câmpuri).
  - `grupuri.css` (838 linii) avea paleta HARDCODATĂ (nu variabile ca terenuri/utilizatori): slate `#475569`/`#1e293b`/`#64748b`/`#e2e8f0`/`#f8fafc` → neutre v9 (`#1a1a1a`/`#2a2a2a`/`#555555`/`#e8e3d8`/`#faf8f3`); coral `#f97316`/`#ea580c` → teracotă `#c2604a`/`#a84e3a`; font DM Sans → Mona Sans. Aceeași mapare aplicată prin sed pe blocurile `<style>` inline din cele 3 pagini (grup-nou/edit ~342 linii fiecare, grup-details ~970 linii).
  - **Butoanele pline → ink** (`.btn-submit`, `.btn-login`, `.btn-primary`, `.btn-creeaza-grup`, `.btn-alatura`, `.btn-cta`, `.btn-cere-analiza`, postAnunt, „completează profil") prin edituri țintite după sed; teracota = doar accent (badge admin, chip-uri zonă, count-badge).
  - **grup-details — cele 4 faze de proiect** (albastru `#2563eb` / verde `#16a34a` / amber `#f59e0b` / mov `#7c3aed`) → tonurile aprobate de Lucian: ardezie `#5a7196` / verde-soft `#2f8f5b` / teracotă `#c2604a` / cărămiziu `#8f5a48` (ca diagrama cum-functioneaza). Bara de progres gradient amber→galben → teracotă→teracotă-pal.
  - În head fiecare: scos DM Sans + `/nav.css`, adăugat `css/apartamentual-v9.css` ÎNAINTE de `grupuri.css`. Pe `grupuri.html` (singura cu hero full-bleed dark): `#navigation{margin-bottom:0}`. Status semantic păstrat (galben „în formare", roșu pericol, verde-pal succes). Gri-rece „nu se potrivește" `#9ca3af` → cald `#a8a094`.
- **Sesiune v9 terenuri + utilizatori (5 iunie) — pe `main`, commit `aaedeba`, împins, NEDEPLOYAT încă din cPanel**:
  - Migrate la design v9 prin **redefinirea variabilelor CSS** în `terenuri.css` (1167 linii) și `utilizatori.css` (736 linii) — primul grup de pagini-listă cu CSS extern mare. **Doar straturi vizuale**: niciun nume de clasă, structură DOM, JS sau fetch Supabase atins (cardurile sunt generate din JS).
  - Mapare: scala `--slate-*` (albastru-rece) → neutre calde v9 (slate-800/900 = ink `#1a1a1a`); `--coral*` portocaliu → **teracotă `#c2604a`** (accent ales de Lucian: ink butoane + teracotă pe iconițe/badge „NOU"/like/filtre active/focus/link „vezi profil"). Butoanele pline → ink prin override separat. Pe secțiunea CTA întunecată din terenuri, butonul e alb pe ink (ca `.cta-final`).
  - Head ambele: scos DM Sans + `/nav.css`, adăugat `css/apartamentual-v9.css` ÎNAINTE de CSS-ul paginii; `#navigation{margin-bottom:0}` pt. hero full-bleed dark.
  - Corecții punctuale: glow-uri portocalii → tonuri v9, inel focus → teracotă, link „sursă" albastru → ardezie `#4a5f80`, verdele respins `#16a34a` (indicator „match" utilizatori) → `#2f8f5b`, gri „match-no" → `#a8a094`.
  - Rămâne din clusterul listă: **`grupuri.css`** (4 pagini cuplate) — vezi blocul de la începutul fișierului.
- **Sesiune v9 contact/register/consultanta (5 iunie) — pe `main`, commit `7b53236`, NEDEPLOYAT încă din cPanel**:
  - Migrate la design v9, **doar straturi vizuale** (niciun JS / fetch Supabase / logică de formular atinsă):
  - `consultanta.html` (avea `<style>` propriu slate/portocaliu) → reskin în-pagină ca `politica-retur`: hero gradient slate → ink plin, card formular cu umbră → border subtil v9, info-box slate → crem-soft, inputuri/pills focus portocaliu `#d97706` → ink, buton submit slate → ink, nota „gratuită" + link succes portocaliu → tonuri v9, `#navigation{margin-bottom:0}` pt. hero full-bleed.
  - `contact.html` + `register.html` (Tailwind + styles.css) → **pattern nou `.v9-page`**: `nav.css` → `apartamentual-v9.css`, scos `bg-*/text-*` Tailwind de pe body, clasă `v9-page` pe `<main>`, bloc `<style>` în-pagină care rescrie accentele colorate prin `.v9-page .clasă` (0-2-0 bate utilitarele Tailwind 0-1-0): albastru→ardezie, mov→cărămiziu pal, galben→ocru, verde→salvie, portocaliu→ink. Pe register, culorile inline (checkbox `accent-color`, cerc+plic „verifică email") editate direct → ink.
  - ⚠️ FAQ-ul de pe `contact` e generat de `js/faq.js`, **partajat cu homepage-ul live** (accente portocalii hardcodate în JS) — lăsat NEATINS; recolorarea lui ar schimba și homepage-ul, e decizie separată.

## Ce e în lucru sau pe orizont apropiat

- **Aliniere v9 — A RĂMAS UN SINGUR CLUSTER: „analize"** (`analize`, `analiza-simplificata`, `analiza-detaliata`, `termeni-analize`). Tot restul e migrat (vezi DONE în blocul de sus, inclusiv clusterul grupuri din 8 iunie). Pe fiecare: link `css/apartamentual-v9.css` înainte de CSS-ul paginii, scos DM Sans + `/nav.css`, aliniat corpul la v9. ⚠️ Sunt în zona „analize/plăți" — doar straturi vizuale, NU atinge JS/preț/flux; pe markup generat din JS NU schimba numele de clase (pattern `parteneri`).

- **Automatizare deploy** — apartamentual.ro se deployează prin **cPanel** (NU Render, cum scrie CLAUDE.md): `.cpanel.yml` copiază `frontend/*` în `/home/ar4/app.ltfbstudio.ro/`. Momentan Lucian dă manual din cPanel „Update from Remote" + „Deploy HEAD Commit" după fiecare push. De automatizat (webhook GitHub→cPanel sau remote git direct care rulează `.cpanel.yml` la push).
- **Cod mort + flash header rămas de curățat după unificarea header/footer v9**:
  - **Flash de header vechi — REZOLVAT (5 iunie)** pe `terenuri.html`, `grup-details.html`, `grup-edit.html`, `grup-nou.html` (toate trecute la `<div id="navigation"></div>`). Nu mai sunt pagini cunoscute cu nav vechi inline.
  - `frontend/nav.css` — **NU e complet mort, NU șterge încă (6 iunie).** Clasele `.unified-nav-*` (liniile 1-293) sunt moarte (nav.js generează acum `.site-nav-*`), DAR: (1) blocul `#navigation { min-height:69px; background:#faf8f3 }` de la final rezervă înălțimea header-ului — pe cele ~25 de pagini care includ `nav.css` dar NU `apartamentual-v9.css`, e SINGURA sursă a rezervării (scoaterea → revine saltul vertical al header-ului); (2) `@import` DM Sans + Mona Sans — DM Sans e folosit activ în corpul multor pagini (`terenuri.css`, `utilizatori.css`, `grupuri.css`, inline). Concluzie: scoaterea `nav.css` ține de **migrarea v9** (fiecare pagină primește `apartamentual-v9.css` → abia atunci renunță la `nav.css`), nu de curățarea de cod mort. Notă: blocul `#navigation` din nav.css încă are linia `box-shadow inset` (linia dublă scoasă din v9 prin `0233d86`) — de aliniat când se migrează paginile.
  - `frontend/js/fab-consultanta.js` (227 linii) + `frontend/fab-consultanta.css` (76 linii) — **ȘTERSE (6 iunie)**, 303 linii cod mort. Copie veche a nav.js, neutilizate (referința fusese deja scoasă de pe parteneri.html).
  - În `nav.js`, mecanismul page-specific CTA (`ctaButton` din `getPageConfig`) rămâne dar nicio ramură nu îl mai setează. De curățat când e clar că nu mai e nevoie de el.
- **Patch analiza-simplificata.html** (înlocuire pop-up vechi cu link la /comanda-analiza.html)
- **Solicită aprobare Netopia** + test plată reală
- **Migrare domeniu** apartamentual.onrender.com → apartamentual.ro (DNS + URL-uri în cod și Supabase Auth)
- **Transfer ownership Render** către luta.lucian.m@gmail.com
- **MFA** pe contul Supabase admin

---

## Setup tehnic Claude Code (NOU, 20 mai)

- Instalat pe laptop, versiunea 2.1.144 (update automat a eșuat — TODO: claude doctor sau npm i -g)
- Logat cu contul `office@ltfbstudio.ro` (LTFB Studio Organization, plan Max)
- Username GitHub: `lutalucianm-lang`
- Repo clonat local la: `C:\Users\lucia\proiecte\apartamentual`
- CLAUDE.md în repo (cu reguli de siguranță, echipă, ton, juridic ca trimitere la pagina site)
- NOTES.md în repo (cu 5 observații tehnice din prima sesiune)
- Workflow ales: SQL manual + deploy edge functions manual (prudent la început)

---

## Decizii deschise (de luat în curând)

1. **Forma juridică ApartamenTUal**: SRL nou separat sau sub LTFB? Cum intră Ina? — **Necesită avocat de start-up** (NU de imobiliar)
2. **Organizație GitHub vs. cont personal** — în discuție cu Ina, ea propune să facă ea organizația
3. **Contul Claude Code**: rămân pe LTFB sau trec pe personal? — provizoriu LTFB
4. **Cele 5 observații tehnice din NOTES.md** — abordate când ajung pe rol

---

## Ce să NU faci fără confirmare explicită

- Commit/push pe repo fără diff arătat
- Modificări la logica de plată (Netopia/Oblio)
- Rulare directă migrații DB (eu rulez manual în SQL Editor)
- Decizii despre echipă (cine ce rol)
- Decizii despre forma juridică

---

## Resurse rapide

- Site: https://apartamentual.ro
- Repo: https://github.com/bogdanbatog/apartamentual
- Supabase project: glbvbbgmcobtswwlktic
- Slack: #app_events
- Pilot: Județului Housing (București)
